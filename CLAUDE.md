# Emeneau / слот-движок — точка входа для нового чата

Этот документ — не «шаблон одного примера» (как назывался раньше), а
полная точка входа: если этот чат исчерпается, вставьте этот файл первым
сообщением в новый — он должен заменить необходимость объяснять заново
методологию, архитектуру и текущее состояние проекта.

---

## Часть 1 — Как со мной работать (её прямые требования, не мои привычки)

Каждый пункт ниже — прямое указание, полученное в ходе сессий, не
предположение о хорошем тоне.

1. **Критический подход обязателен.** Не соглашаться ради согласия, не
   останавливаться на первом рабочем варианте — искать слабые места и
   предлагать улучшения без просьбы. Доводить до настоящего завершения,
   не до формального выполнения запроса. Если предложение самого
   пользователя избыточно — прямо возразить и предложить более простой
   путь, не исполнять как есть.
2. **Скорость и минимализм кода — приоритет проекта.** Не добавлять код
   или зависимости без необходимости. Прежде чем писать НОВЫЙ код для
   эффекта/механизма — сначала проверить, нет ли уже готового решения в
   движке (существующий параметр, существующая функция), которое просто
   нужно применить с другими значениями или в зеркальном направлении. Не
   изобретать заново то, что уже решено — цель это унификация всех
   действий, не разрозненные частные решения под каждый пример.
3. **Академический регистр — высшая планка, не «попроще».** Рецензенты —
   лингвисты со степенями. Простой язык пояснений НЕ означает
   примитивизацию: формулировки лингвистически корректны в высшей
   степени, точная терминология (геминация, не «удвоение вдвоём» и т.п.),
   даже в кратких пояснениях к примерам. При этом термин должен быть
   действительно из источника или общепринятой санскритологии, не
   изобретённым — проверять по самому тексту Эменó (PDF — на деле ZIP со
   сканами страниц, без текстового слоя; читать глазами постранично, не
   grep'ать) или по независимым источникам, не полагаться на собственные
   более ранние пересказы.
4. **Пуш — автоматически, если нет открытых вопросов.** Раньше пуш
   откладывался по умолчанию, потому что перенос правок на GitHub был
   ручной работой пользователя (собрать зип, самому вставить команды).
   Это больше не так: пуш выполняется напрямую из этой же сессии
   (git add/commit/push), ручного шага нет — откладывать по старой
   причине незачем. Текущее правило: если по ходу правки не осталось
   вопросов, требующих решения именно пользователя (неоднозначность в
   данных, выбор между вариантами и т.п.), — коммитить и пушить
   самостоятельно, не спрашивая разрешения на каждый пуш отдельно.
   Дожидаться прямого решения нужно только тогда, когда есть что
   реально утвердить — сначала закрыть этот вопрос, потом пушить. Когда
   пуш готовится — по-прежнему сверять с GitHub напрямую (клонировать
   свежую копию, сравнивать файлы), не полагаться на память о том, что
   уже отправлено.
5. **Значимые результаты — фиксировать сухим остатком.** Когда достигнут
   значимый вывод/решение/список открытых пунктов — проактивно предложить
   зафиксировать его (список/таблица, не проза), не оставлять только в
   переписке.
6. **Проверка — численная симуляция, не «на глаз».** Рендера/браузера нет
   вообще — только Node.js. Любое исправление таймлайна, геометрии,
   направления движения проверяется прогоном реального кода (вырезанного
   из самого файла, не переписанного по памяти) на реалистичных данных,
   с явным выводом чисел. Визуальная часть (насколько красиво/читаемо)
   всегда остаётся на подтверждение пользователя — не заявлять «визуально
   готово», когда проверено только численно.
7. **Реестр ведётся проактивно, не постфактум.** Каждое новое
   зарезервированное значение (цвет, число, направление, лимит) — в
   реестр в момент решения, не после того, как ошибка на его отсутствие
   уже произошла на следующем примере.

---

## Часть 2 — ГЛАВНЫЙ МЕТОДОЛОГИЧЕСКИЙ ПРИНЦИП

**Суть примера — во ВЗАИМОДЕЙСТВИИ частей, не в готовом целом.** Закреплено
трижды как исправление уже готового плана (āsīt, śādhi дважды) — должно
применяться СРАЗУ на первом черновике, не после замечания.

Если внутри слова есть морфологическая граница (корень+окончание,
приставка+корень, слово+слово) — часть, участвующая в сандхи, НЕЛЬЗЯ
показывать уже собранной с самого начала. Обе стороны границы
падают/существуют РАЗДЕЛЁННЫМИ (зазором), и само событие сандхи —
СЛЕДСТВИЕ их сближения, не декорация поверх уже готового слова.

Практически: перед `initial`/`ops` ответить — «где граница, и что
физически происходит при соприкосновении сторон?» — заложить сближение
как отдельный видимый шаг ДО события сандхи.

**Уточнение (третий заход, тот же принцип глубже): мало развести и
сблизить — нужен ЗАМЕТНЫЙ момент прибытия.** Если сближающаяся сторона
не просто закрывает исходный зазор, а ЕЩЁ И занимает место
исчезающей/меняющейся буквы — это ДВА отрезка пути, не один. Сплошной
путь одной кривой читается как «проехало насквозь», не «подошло →
реакция → въехало». Нужна явная ПАУЗА между «дошло» и «довершило путь» —
реакция происходит ИМЕННО в паузе. В движке: `approach` с
`midDistance`/`midHoldDur`/`leg2Dur`.

**Момент начала реакции требует отдельного сигнала, не только момент
завершения.** Плавная кривая движения имеет нулевую производную в t=0 —
первые кадры визуально неразличимы. Обязательна вспышка+скачок масштаба
РОВНО в момент, когда реакция начинается (см. `elide`, заход 39).

**Геометрия отстойников — точная формула, не подбор.** Отступ от края
слотов до ближнего края отстойника = `CUBE_SIZE/2` (в любую сторону).
`holdOffset.y = FLOOR_Y − CUBE_SIZE/2 − CUBE_SIZE/2`. При CUBE_SIZE=1.1 →
−1.65 для нижнего. Запас у камеры — отдельно, реальной проекционной
математикой (`THREE.Vector3.project`), не на глаз.

**Направление отстойника — смысловой сигнал, не декоративный выбор.**
ВВЕРХ = превращается, что-то продолжается в новой форме (см. `split`).
ВНИЗ = пропадает без следа (см. `elide`).

**Направление любого бокового смещения — проверять числом, что физически
рядом, не полагаться на словесную интуицию.** Дважды пойманная ошибка:
«отступает от X» ошибочно интерпретировалось как движение в фиксированную
сторону без проверки, кто там физически сидит.

---

## Часть 2а — Источники и обработанный лингвистический анализ

**Обработанный разбор всех 71 правил (114 примеров)** — уже существует,
не нужно делать заново. Файл `rules-1-71-analysis-consolidated (2).md` —
**самая полная и выверенная версия** (13 закрытых открытых вопросов из
16, сверена и с опубликованной книгой 2020г., и с английским оригиналом).
Формат: по каждому шагу **[Тип]** источник→триггер→результат, с W-ссылками
на Уитни. Базовый файл (без номера) и версия `(1)` — более ранние
черновики ТОГО ЖЕ документа (как было раньше с `second-examples-todo.md`)
— можно убрать из project knowledge, чтобы не путать будущие сессии.
Отдельно — `claude/rules-steps-inventory.md`: сводная таблица всех 71
правил по числу шагов и типу операции (ELIDE/MERGE/TRANSFORM/DOUBLE/
DISTANT/INSERT) — дополняет, не дублирует consolidated-файл.

**Английский оригинал** — Emeneau, Van Nooten, *Sanskrit Sandhi and
Exercises*, 2nd rev. ed., University of California Press, 1968. Легитимные
источники (не пиратские):
- WorldCat (библиотечный каталог): https://www.worldcat.org/title/sanskrit-sandhi-and-exercises/oclc/169728
- UC Press (издатель, есть ePub+PDF): https://www.ucpress.edu/books/sanskrit-sandhi-and-exercises-revised-edition
- Google Books (превью/поиск): https://books.google.com/books/about/Sanskrit_Sandhi_and_Exercises.html?id=_9CSzQEACAAJ

Полный текст оригинала в проект не сохраняется (авторское право) —
цитируется только короткими фразами и пересказом, см. дисциплину
цитирования во всём этом документе.

## Часть 3 — Архитектура (что построено)

### Файлы
- `docs/app/lib/slot-engine.js` — точка входа, тонкий barrel-файл (заход 62,
  Стадия 5): `export * from` на каждый из модулей ниже, публичный API
  (что импортируют examples/тесты/приложение) не изменился ни на одно имя.
  Не редактировать реализацию здесь — её больше нет в этом файле.
  - `slot-engine-core.js` — константы, токенизатор, цвета, плавность
    (easing-функции), `setOpacity`. Ничего не импортирует.
  - `slot-engine-cube.js` — фабрика кубика (`makeCube`/`regenMats`/
    `updateShadow`), геометрия/материалы/тень. **Заход 64, унификация
    кубика:** геометрия кубика и тени — ОБЩАЯ на все показы (лениво
    строится один раз, `isSharedResource` защищает её от `unmount()`);
    `matsBlank`/`matsReady`/`matsSignal` — ленивые (строятся только при
    первом реальном обращении через `defineMatsSlot`, не все сразу).
    Новый вариант внешности кубика добавлять ТОЛЬКО через `defineMatsSlot`
    (не голым полем) — иначе он не будет уничтожаться при замене и снова
    потечёт память при каждом превращении буквы.
  - `slot-engine-words.js` — группы слов («вся единица, не одна буква»,
    `computeWordGroups`/`resolveSlotRef`).
  - `slot-engine-steps.js` — шаги (грамматика/правило N), runtime-таймлайн.
  - `slot-engine-validate.js` — `validateExampleData` (Стадия 4).
  - `slot-engine-ops.js` — обработчики операций (все `apply*`) и их прямые
    помощники (project/frontAnchor/spawnPulseRing/spawnWave/
    updateGroupFrame/setFacePulse/flyArcPosition) — самый крупный модуль,
    ~1000 строк. Новую `apply*`-функцию добавлять сюда.
  - `slot-engine-mount.js` — `mountSlotExample`, кадровый цикл, лента
    шагов, инъекция стилей.
- `docs/app/lib/chalk-module.js` — материалы/текстуры кубиков (`paintGlyph`
  с авто-подгонкой шрифта, `buildChalkMaterials`).
- `docs/app/examples/ruleN-name-slots.js` — данные каждого примера
  (`data` + `mount()`), общий для тестовой страницы и приложения.
- `docs/app/test-slot-engine-ruleN.html` — тестовые полигоны, ссылки в
  `#dev-links` внизу справа основного приложения.
- `docs/app/sanskrit-sandhi-app.html` — основное приложение (правая/левая
  панели). Разбиение (по образцу Стадии 5 для slot-engine.js), идёт по
  шагам от безопасного к рискованному:
  - **Заход 67:** CSS (1347 строк) вынесен в `sanskrit-sandhi-app.css`
    (`<link rel="stylesheet">` вместо инлайн `<style>`) — проверено
    побайтовым сравнением, риск нулевой.
  - **Заход 68:** чисто декларативные данные (`SECTIONS` — тексты 71
    правила, `EXAMPLES` — 2D-роли на алфавите, `EXERCISES`, `ALPHA_ROWS` —
    раскладка алфавита; плюс `RULE_GROUP`/`RULE_SUBGROUP`/`GROUP_INFO`/
    `SUBGROUP_INFO`) вынесены в `docs/app/data.js`, `<script>` стал
    `<script type="module">` с одним `import` наверху. Проверено:
    побайтовое сравнение остатка файла (0 расхождений вне вырезанных
    диапазонов), `node --check` на обоих файлах, реальный импорт данных в
    Node (числа ключей/длины совпадают с ожиданием), новый тест
    `tests/dataModule.test.mjs` (форма и целостность данных — все 71
    правило встречаются по одному разу и т.п.), полный прогон
    `npm test`+`lint`+`check-conventions` (112/112). Скрипт был последним
    в `<body>`, поэтому переход на `type="module"` (откладывает
    исполнение до конца парсинга документа) не меняет фактическое время
    выполнения — вся разметка и так уже была распарсена к этому моменту.
    **Побочная находка:** `RULE_GROUP`/`RULE_SUBGROUP`/`GROUP_INFO`/
    `SUBGROUP_INFO` оказались МЁРТВЫМ кодом уже в оригинале — раскраска
    карточек правил на деле идёт через поле `g` прямо в `SECTIONS`
    (`cd.g`), эти четыре структуры нигде не читаются. Не удалены (это
    решение по содержанию, не механический перенос) — только не
    импортированы в основной скрипт, чтобы не тащить мёртвый импорт;
    сами остались экспортированы из `data.js` и покрыты тестом на случай,
    если понадобятся. Стоит решить отдельно: удалить или это задел под
    несделанную фичу группировки.
  - **Заход 69:** подсистема 2D-подсветки ролей на алфавите (источник/
    триггер/результат — SVG-линии, значки, гуна-таблица, лента шагов)
    вынесена в `docs/app/role-demo.js` (284 строки). Наружу — только три
    функции (`renderRoleDemo`/`renderRoleDemoSynced`/`clearRoleDemo`),
    остальное (`svgRoleLine`/`svgBadge`/`activateRoleCell(s)`/
    `renderRoleStep`/гуна-observer) — внутренние детали модуля. Модуль
    делает свои `document.getElementById('alpha-wrap'/'anim-tiles')` —
    те же id, что и в основном скрипте, порядок импорта не важен, элементы
    уже есть в статической разметке. Проверено тем же методом, что и в
    заходе 68 (побайтовое сравнение остатка файла — 0 расхождений,
    `node --check`, 112/112 тестов). **Реальный баг, пойманный ДО пуша:**
    первая версия модуля не объявляла `wrap`/`animTiles` внутри себя (они
    остались только в главном скрипте) — сработало бы `ReferenceError` при
    самой первой строке модуля. Поймано расширением `eslint.config.js` на
    `docs/app/**/*.js` целиком (раньше правило распространялось только на
    `lib/**`/`examples/**` — у `data.js` из захода 68 просто не было ни
    одной ссылки на `document`, поэтому дыра тогда не проявилась). Заодно
    добавлен `MutationObserver` в список глобалов и убран один
    неиспользуемый `catch (e)`.
  - **Заходы 70–71** (один и тот же чат, номер коммита сбился — см.
    Часть 1 п.4 про автоматический пуш; тут просто зафиксировано под
    правильным номером, задним числом): панель алфавита (тултип,
    аудио-заглушка, пин буквы, рендер `ALPHA_ROWS`) вынесена в
    `docs/app/alpha-panel.js` — наружу
    только `hideTooltip` (нужна карточкам правил). Следующим шагом —
    весь оставшийся блок «карточки правил слева + всё, что показывается
    по центру» (парсинг цитаты, зона монтирования 3D-анимации, заглушка
    левой панели, `showCenter`/`hideCenter`) вынесен в
    `docs/app/rule-panel.js` одним куском, не по функции за раз — выбор
    карточки слева полностью управляет центром, это одна связная единица,
    делить её пополам означало бы тащить состояние через границу модуля
    без причины. Наружу ничего не нужно — вводный экран, оставшийся в
    `sanskrit-sandhi-app.html`, с этим модулем по состоянию не пересекается.
    `sanskrit-sandhi-app.html`: 682 → 234 строки (только вводный экран и
    два `import './...js'` для побочных эффектов).
    Заодно удалён мёртвый код, попутно найденный: блок «цитата»
    (`quoteBox`/`showQuote`/`hideQuote` + CSS — не подключён к разметке
    ни разу за всю историю проекта, включая самый первый коммит),
    `getCtx`/`audioCtx`, неиспользуемая `groupStub`, и вся система
    отслеживания позиции мыши (`lastMouseX`/`Y`/`T` — писались, но нигде
    не читались; поймано ESLint'ом при переносе кода в настоящий модуль —
    инлайновый `<script>` в .html им не проверялся, отсюда и не был
    замечен раньше). `applyGroupHighlight`/`clearGroupHighlight` (задел
    под подсветку группы правил 20–26) — тоже не используются, но
    оставлены как есть (реальная, просто ещё не подключённая фича, не
    мусор) с точечным `eslint-disable-next-line`, не общим отключением
    правила на файл.
    **Заодно, тем же заходом:** сверка с верным источником
    (`docs/reference/Эмено2.pdf`, уже лежал в репозитории — см. Часть 2а)
    нашла придуманный, не подтверждённый источником глосс «повелевать»/
    «повелевай!» в `EXAMPLES[15].desc` (реально в источнике только
    «обучать»/«обучи»); из-за расхождения текста с цитатой правила не
    срабатывала склейка одинаковых примеров в `rule-panel.js`
    (`showCenter`, сравнение по префиксу до «→») — один и тот же пример
    показывался в приложении дважды. Исправлено на дословное совпадение
    с цитатой; скриптом проверены все 71 правило — других расхождений
    между `EXAMPLES[n].desc` и `emeno` нет.
    Проверено (оба среза): `node --check`, 112/112 тестов, lint и
    check-conventions чисто, сверка со свежим клоном GitHub после пуша.
  - **Осталось:** только сам вводный экран (`sanskrit-sandhi-app.html`,
    ~20 строк JS) и HTML-разметка (структура `#panel-sandhi`/`#center`/
    `#panel-alpha`, CSS-классы/id) — не тронуты. Разбиение JS-логики
    можно считать практически завершённым; следующий содержательный шаг
    в этом направлении — сама разметка, не JS.
- `package.json` + `tests/` — настоящая тестовая инфраструктура (см. ниже).
- `CHANGELOG.md` (заход 60, Стадия 3) — история разработки по заходам,
  вынесенная из комментариев `slot-engine.js`. Держать в паре с этим
  файлом: сюда — история «что и почему менялось», в CLAUDE.md — текущее
  состояние и методология.
- Старые рукописные модули (`rule3-agnayas.js`, `rule71-vak-asti.js`,
  `docs/effects/rule-assimilation-*.html`) — НЕ удалены, эталоны техник,
  не подключены никуда.

### Визуальный язык 2D-модели ролей (справка, не 3D-движок)

Источник — без рамки. Триггер — пунктирная обводка. Результат — сплошная
белая обводка. Геометрия: место-ассимиляция (правила 37–40, 69) →
результат берёт столбец от триггера + ряд от источника. Озвончение/
оглушение (31–33) → столбец от источника, только звонкость от триггера.
Гуна/вриддхи → сдвиг источника внутри своего столбца, a/ā — катализатор
без координат. Подсветка всегда по клику на конкретный пример, никогда по
клику на карточку правила целиком.

### ⚠️ Неопределённый по статусу файл — `App.txt`

Среди файлов project knowledge есть `App.txt` — код на React + Framer
Motion (`motion/react`), совершенно другая технология, чем текущий проект
(vanilla JS + THREE.js). Судя по содержанию — отдельная анимация
«Деванагари → звук» для вводного экрана (буква проходит три фазы:
деванагари → IAST-слог → чистый звук). НЕ имеет никакого отношения к
`slot-engine.js`/`sanskrit-sandhi-app.html` — ни импортов, ни общих
структур данных. Не смогла определить: заброшенный черновик альтернативного
направления, или отдельный, не связанный с текущей работой проект,
случайно оказавшийся в том же project knowledge. Не переносила его
содержание никуда — при следующей сессии стоит спросить пользователя
напрямую, что это и нужно ли это вообще держать в контексте.

### ⚠️ Предупреждение о противоречии между старыми документами project knowledge

`handoff-engineering-state.md` утверждает, что 2D-подсветка перенесена
только на 5 демо-правил (39, 32, 2, 6, 33) и что клик по чипу «сейчас
ничего не подсвечивает». Это УСТАРЕВШЕЕ состояние — более поздний
`second-examples-todo.md` (с явной пометкой о консолидации от 2026-08-21)
и `handoff-slot-engine-session.md` подтверждают: система реально работает
на 64 из 71 правила, подсветка идёт через `slotstep`-событие, аудит дал
0 проблем. **При противоречии между старыми документами — доверять более
позднему по внутренним меткам времени, не более раннему.** `handoff-
engineering-state.md` стоит явно пометить как исторический (или убрать
из project knowledge), чтобы не запутать будущую сессию тем же образом.

### Профессионализация (заходы 55–62) — ВСЕ 5 СТАДИЙ ЗАВЕРШЕНЫ

Начата по прямому требованию: «хочу чтобы всё было в соответствии с
требованиями программистов». Честная оценка кода (заход 55, до начала
работы) — как учебный инструмент одного разработчика через чат, качество
выше среднего по дисциплине проверки; как код для штатных программистов —
не проходил бы код-ревью без замечаний (нет тестов, один файл на ~2000
строк, отсутствие валидации данных). План — 5 стадий, от безопасного к
рискованному, не переписывание всего разом (риск сломать неотлаженное
визуально — визуальной проверки у Клода нет вообще). Все три исходные
претензии закрыты: тесты (Стадии 1–2, 97 тестов), валидация (Стадия 4),
«один файл» (Стадия 5, разбит на 7 модулей).

**Заход 66 — сверх исходного плана, по итогам отдельной критики кода
«как профессиональный программист»** (три новых пункта: главный файл
приложения не тронут, нет интеграционных тестов, нет автоматической
проверки): добавлены CI (GitHub Actions, `.github/workflows/ci.yml` —
`npm test`+`npm run lint`+`npm run check-conventions` на каждый пуш/PR),
ESLint (`eslint.config.js`, только «рекомендованные» правила — реальные
классы ошибок, не стилистика; исключены старые эталонные модули —
`rule3-agnayas.js`/`rule71-vak-asti.js`/`anim-elements.js`, линтинг
намеренно замороженного кода бесполезен) и `scripts/check-no-zahod.mjs` —
автоматическая проверка, что в `docs/app/lib/*.js` не появляются новые
«заход N» (Стадия 3 — держится ПРОЗОЙ `CLAUDE.md`, не автоматикой, и это
реально подводило: за эту же сессию «заход N» дважды случайно проскочило
обратно в код, включая один раз в код, добавленный специально для этой
самой дисциплины). Заодно — `package-lock.json` перестал игнорироваться
(был в `.gitignore`, `npm ci` в CI требует его) — до этого установка
зависимостей не была воспроизводимой.

Первый прогон линтера нашёл 9 реальных, мелких проблем в активном коде
(неиспользуемые импорты/параметры) — все исправлены; `chalk-module.js`
заодно избавился от параметра `seed` у `makeChalkGeo`, который не влиял
на форму никогда (найдено ещё в заходе 64, теперь убрано из сигнатуры
до конца).

Из трёх пунктов критики один в процессе — `sanskrit-sandhi-app.html`
(был 3240 строк, разбиение начато заходами 67-68, см. описание файла
выше и Часть 6), другой (интеграционные тесты для `mountSlotExample`)
пока не начат.

**Стадия 1 — ГОТОВА.** `package.json` (тип модуля, `npm test` через
встроенный `node:test`, без внешних тест-фреймворков — тот же минимализм,
что и во всём проекте), 15 функций уровня модуля получили `export`
(механическая правка, риск нулевой), 18 тестов — каждый привязан к
КОНКРЕТНОЙ, реально пойманной за сессию регрессии (заходы 46, 48-49, 52),
не абстрактный.

**Стадия 2 — ГОТОВА (заход 60).** Обработчики операций (`apply*`) были
вложены ВНУТРЬ `mountSlotExample` — физически не тестировались напрямую,
только копированием кода в одноразовые скрипты (вся история сессии до
захода 56). Перенос на уровень модуля — по одной функции за раз, не
пачкой (слишком большая поверхность для правки без визуальной проверки).
Единая сигнатура для всех: `applyX(op, elapsed, ctx)` (кроме
`applyStepDim` — `applyStepDim(elapsed, ctx)`, у неё нет привязки к
конкретной операции), где `ctx` — один и тот же объект контекста,
расширялся по мере переноса, не создавался заново под каждую функцию.

**Перенесено — ВСЕ 10 из 10 `apply*`:** `applyTransform` (заход 56, +
симметричные паузы anticipateDur/holdDur в заходе 57), `applyElide`
(заход 58), `applyInfluence` (заход 59) вместе с
`spawnWave`/`updateGroupFrame`/`setFacePulse`/`buildPulseFace`/
`redrawPulseFace`, `applyApproach` (заход 59), `applySplit` (заход 59)
вместе с `flyArcPosition`, `applyArrive` (заход 60), `applyMerge`
(заход 60), `applySettle` (заход 60), `applyDim` (заход 60),
`applyStepDim` (заход 60, последняя). Плюс каскадом (не в изначальном
плане, потянулись за паузами `applyTransform`): `project`/`frontAnchor`/
`spawnPulseRing` (заход 57).

**`ctx` в финальном виде** (расширялся по мере переноса, ни разу не
создавался заново под конкретную функцию): `cubes`, `camera`, `stageEl`,
`labelsEl`, `wordGroupsList` (заход 59, нужен `applyInfluence`/
`applyApproach` для `resolveSlotRef` группы слов), `scene` (заход 59,
нужен `applySplit`/`applyArrive` — оба добавляют новые THREE-объекты на
сцену за кадром), `runtimeSteps`/`data` (заход 60, нужны только
`applyStepDim`).

**Тестов на весь перенос Стадии 2: 75** (27 было в Стадии 1 + 24 в заходе
59 на applyInfluence/applyApproach/applySplit + 24 в заходе 60 на
applyArrive/applyMerge/applySettle/applyDim/applyStepDim). При написании
заходов 59–60 реально поймано и исправлено НЕСКОЛЬКО ошибок В САМИХ
ТЕСТАХ (не в движке): сравнение позиции с `mesh.position.x`, выставленным
вручную в тестовом хелпере, вместо настоящей `slotX()`; вызов ровно НА
границе окна вместо строго после неё (`applyApproach` — код проверяет
`elapsed<=peakStart` как ветку ДО пика, не включая пик); `matsMain` из
голых строк вместо объектов (`setOpacity` падает на `str.opacity=...`);
чтение `cubes[op.at]` после того, как `applySplit` уже переложил кубик
под временный `_srcKey` на первом же кадре. **Урок:** движок прошёл ВСЕ
эти проверки без единой правки кода — расхождения были только в тестовых
допущениях, что само по себе хороший знак: перенос ничего не сломал на
всех 10 функциях подряд, тесты просто должны были точнее повторять то,
что код УЖЕ делает, не то, что кажется логичным на бумаге.

**Что дальше по Стадии 2 (мелкий хвост, не блокирующий):** сами
`setFacePulse`/`buildPulseFace`/`redrawPulseFace` покрыты тестами только
КОСВЕННО (через `applyInfluence`/`applyApproach`) — прямых unit-тестов на
них самих нет, при желании можно добавить отдельно, ценность невысокая
(логика простая, уже многократно проверена косвенно).

**НЕ кандидаты на перенос** (жёстко привязаны к жизненному циклу
конкретного монтирования, тестировать изолированно малоценно):
`updateSteps`, `flashStepBoundary`, `fallY`, `resize`, `frame`, `unmount`.

**Открытые методологические вопросы — ВСЕ ТРИ РЕШЕНЫ (заход 57, прямое
решение пользователя), реализовано в коде, не только записано:**
1 и 3 (были одним решением). *«Нужна ли `transform` пауза-осознание и
пауза-фиксация, симметрично `split`?»* — Да, почти каждый значимый шаг
нуждается в паузе-фиксации для осознания результата. Реализовано:
`anticipateDur` (900мс, дефолт) до вращения — пульс масштабом+два кольца,
БЕЗ смены цвета (тот же принцип, что у `split`); `holdDur` (700мс, дефолт)
после — `op._done` откладывается на это время, не срабатывает мгновенно
по завершении вращения.
2. *«Нужна ли грамматическому шагу стадия „невозможность соседства“,
как approach-отскок у split?»* — Нет. Для гунации: подчёркиваем влияющие
кубики, даём импульс, начинаем гунирование — это уже существующая
последовательность `influence`→`transform`, никакой approach-стадии
дополнительно не требуется.

**НАЙДЕННЫЙ И ИСПРАВЛЕННЫЙ БАГ (заход 57, при переносе paus):** первая
попытка перенести `applyElide` просто пометила `export` НА СТАРОМ МЕСТЕ,
физически всё ещё внутри `mountSlotExample` — `export` внутри вложенной
функции синтаксически недопустим в JS. Поймано `node --check` до сдачи,
не после. Исправлено: блок физически вырезан Python-скриптом (точнее и
надёжнее, чем `str_replace` на таком объёме) и перемещён к началу файла.

**НАЙДЕННЫЙ И ИСПРАВЛЕННЫЙ БАГ (заход 56, при проверке пуша):** старое
правило `.gitignore` (`*.mjs`, «черновики сборки демо») молча исключало
ВСЕ файлы тестов при `git add` — коммит содержал только `package.json` и
`tests/README.md`, сами тесты на GitHub не попадали ни разу, несмотря на
сообщения коммитов. Исправлено точным исключением (`!tests/**/*.mjs`) —
проверено фактическим `git add` + `git status`, не на словах. **Урок:**
при добавлении НОВОГО типа файла в репозиторий — сверять `.gitignore`
заранее, не доверять, что `git add` сделает то, что ожидается.

**НАЙДЕННЫЙ И ИСПРАВЛЕННЫЙ БАГ (заход 59, при первом запуске тестов на
этой машине):** `package.json`'s `"test": "node --test 'tests/**/*.test.mjs'"`
находил 0 тестов через `npm test` на Windows — одинарные кавычки не
раскрываются `cmd.exe` (шелл, через который npm запускает скрипты на
Windows), паттерн передавался буквально, ни один файл не совпадал. Голый
`node --test` без аргументов (встроенный дефолтный поиск
`tests/**/*.test.mjs`) отработал верно. Исправлено на `"test": "node --test"`.
**Урок:** глоб-паттерны в кавычках внутри npm-скриптов — платформозависимы,
не переносимы между bash/cmd без проверки на каждой ОС, где реально
запускается `npm test`.

**ОКРУЖЕНИЕ (заход 59):** на машине, где вёлся этот заход, Node.js не был
установлен вообще (ни в PATH, ни в стандартных папках) — поставлен через
`winget install OpenJS.NodeJS.LTS`. Если в новой сессии `node`/`npm` не
находятся — это НЕ повод считать тестовую инфраструктуру нерабочей,
проверить сначала фактическое наличие node.js на конкретной машине.

**ОСТОРОЖНО (заход 59, реальная авария, не гипотетическая): на этой
файловой системе (NTFS, case-sensitivity для этой папки ОТКЛЮЧЕНА —
проверяется `fsutil.exe file queryCaseSensitiveInfo`) `claude.md` и
`CLAUDE.md` — ОДИН И ТОТ ЖЕ файл, не два.** `git diff`/`diff` между ними
покажет «идентично» даже если это буквально один и тот же файл, сравниваемый
сам с собой — это НЕ подтверждение существования дубликата, только
совпадения путей после case-folding файловой системы. Перед удалением
«дубликата» с иным регистром имени — сначала `fsutil.exe file
queryCaseSensitiveInfo <путь>` и/или `git ls-files | grep -i` (что реально
отслеживает git по точному регистру), не полагаться на то, что раз `ls`/
`git status` показали путь отдельной строкой — это точно отдельный файл.
Этот файл (CLAUDE.md) был случайно удалён именно так и восстановлен из
содержимого, уже прочитанного в диалоге в текущей сессии — если бы
содержимого не было в контексте, восстановление было бы невозможно (файл
никогда не был закоммичен в git, что и обнаружилось при попытке
`git show HEAD:CLAUDE.md`).

**Тестов сейчас: 75** (см. разбивку по заходам 59/60 в разделе Стадии 2
выше) — заход 59 (27→51) запушен и перепроверен клонированием с нуля
(`npm install`+`npm test` на самой GitHub-версии, независимо от локальной
копии); заход 60 (51→75) ещё не запушен на момент этой записи, проверен
только локально (`npm test`, 75/75 зелёных). Каждый привязан к
КОНКРЕТНОЙ, реально пойманной за сессию регрессии или к явно
задокументированному в коде решению, не абстрактный.

**Стадия 3 — ГОТОВА для `slot-engine.js` (заход 60).** Очистка
комментариев: все 110 упоминаний «заход N» в главном файле движка
(`docs/app/lib/slot-engine.js`, счёт вырос со 110 до 0 — было больше, чем
73+ из предыдущей оценки, за счёт заходов 59-60 самих) вынесены в новый
`CHANGELOG.md` в корне репозитория; в коде остаётся только техническая
причина «почему сейчас так», без цитат обратной связи и хронологии
«было X, стало Y». Заодно найден и удалён РЕАЛЬНЫЙ дублирующийся кусок —
полные копии документации ARRIVE/MERGE/ELIDE/SETTLE/DIM (~85 строк)
остались внутри `mountSlotExample` от предыдущего переноса, когда были
заменены только тела вложенных функций, а не сами доккомментарии —
чистый мусор, не служил никакой цели (реальная документация уже жила у
module-level версий этих функций). Заодно почищено единственное
упоминание в `chalk-module.js`.

**Стадия 3 НЕ затронула** (сознательно, меньший по значению хвост,
отдельная задача при желании): `docs/app/sanskrit-sandhi-app.html` (31
упоминание), файлы данных примеров `docs/app/examples/*.js` (2–8 в
каждом), тестовые HTML-полигоны (1–2 в каждом) — эти файлы не входили в
исходную оценку «~2000 строк» (относилась именно к `slot-engine.js`).

**Стадия 4 — ГОТОВА (заход 61).** Валидация данных примера —
`export function validateExampleData(data)` в `slot-engine.js`, вызывается
первой строкой в `mountSlotExample()` и бросает `Error` с полным списком
проблем (не первой найденной), если данные некорректны. Не полная
JSON-схема — только форма и типы полей, которые `apply*`-функции реально
разыменовывают БЕЗ `??`-дефолта (`arr.delay`/`arr.dur` в `split.arrivals`
и `arrive.items`, `op.end` в `dim`, обязательные `toGlyph`/`from`/`at` и
т.п.) — именно эти поля при опечатке в новом `ruleN-*.js` раньше давали
тихий `NaN`/`undefined` где-то в кадровом цикле («кубик просто не
появился», без единой ошибки в консоли), не понятную ошибку при
монтировании. Проверено против ВСЕХ 5 реальных `examples/*.js` — 0 ложных
срабатываний, ни один существующий пример не потребовал правки. 19 новых
тестов (94 всего) — как на реально ловящиеся классы ошибок (неизвестный
`type`, дубль слота, пересекающиеся `steps`, отсутствие обязательных
полей), так и на то, что валидные формы (число/`{word}`/массив для
слот-ссылок, `movers`|`mover`) не бракуются ложно.

**Стадия 5 — ГОТОВА (заход 62, ПОСЛЕДНЯЯ стадия плана профессионализации —
все 5 стадий завершены).** `slot-engine.js` (1973 строки, последняя из
трёх претензий честной оценки захода 55 — «один файл» — после того как
«нет тестов» и «нет валидации» уже закрыты Стадиями 1–4) разбит на 7
модулей по смыслу — см. полный список в разделе «Файлы» выше. Публичный
API не изменился ни на одно имя (barrel `export *`), ни один из
потребителей не потребовал правки импорта — проверено сквозным импортом
через barrel, прямым импортом каждого submodule, и загрузкой всех 5
реальных `examples/*.js` (не только через `npm test`, отдельным
скриптом). 97 тестов (94 прежних не изменены ни на строку + 3 новых
теста-предохранителя на сам barrel — от забытого `export *` или
коллизии имён в будущих заходах).

**Побочная находка (заход 62):** `grep -i` в среде выполнения этой
сессии НЕ умеет регистронезависимо матчить кириллицу (`grep -i "заход"`
пропускал `ЗАХОД`, обычный `grep "ЗАХОД"` находил сразу) — из-за этого
при чистке комментариев в заходе 60 остался незамеченным один экземпляр.
Исправлено; на будущее при поиске кириллицы в этой среде — проверять
варианты регистра явно (`grep -n "текст\|Текст\|ТЕКСТ"`), не полагаться
на `-i`.

### Типы операций движка

| Тип | Назначение |

|---|---|
| `influence` | Дальнодействие причина→цель: текстурный пульс на источнике + бегущие волны (`spawnWave`) + рамка группы, если источников >1 |
| `transform` | Буква меняется НА МЕСТЕ (глиф+цвет), 4 категории — см. реестр ниже |
| `approach` | Физическое сближение; поддерживает двухотрезочный путь с паузой (`midDistance`/`midHoldDur`/`leg2Dur`) и непрерывный текстурный пульс на триггере (`holdPulse`) |
| `split` | Одна буква распадается на ДВЕ РАЗНЫЕ (подъём в отстойник вверх, прилёт результатов по дуге) |
| `merge` | Две буквы сливаются в ОДНУ (едущий кубик, вспышка масштаб+emissive в момент контакта) |
| `elide` | Буква исчезает БЕЗ замены (погружение вниз в отстойник, вспышка в момент начала реакции) |
| `arrive` | Тихий прилёт буквы без слияния — код готов, ни разу не применён в реальном примере |
| `settle` | Финальная волна «всё готово» — всегда автоматическая |

### Зарезервированные значения (не путать друг с другом)

- **Серебро (`SIGNAL_COLOR`/`matsSignal`)** — ТОЛЬКО гунация. Не декоративный
  «что-то происходит», конкретный сигнал «идёт огласовка».
- **Золото** — зарезервировано под вриддхи, ни разу не построена (открытый
  вопрос: нужен ли отдельный `signal:'gold'`, сейчас `op.signal`
  поддерживает только `'silver'`/`'blank'`).
- **`GROUP_COLOR`** — нейтральный, структурные события (вспышки merge/elide,
  рамка группы, сжатые многобуквенные блоки).
- **Категории `transform`** (угол поворота И цвет промежуточной фазы —
  ОДНА система, не два разных факта). **С заходом 54 — именованные
  пресеты в коде** (`export const TRANSFORM_KIND` в `slot-engine.js`),
  не только описание здесь: `{ type:'transform', ..., ...TRANSFORM_KIND.vargaPair }`
  вместо голых чисел вручную в каждом примере — снижает риск ошибки
  (заход 46: `spinTurns` был перепутан именно из-за ручного повторения).
  - Гунация → `spinTurns:1` (360°) + `signal:'silver'` (дефолт движка —
    в `TRANSFORM_KIND` НЕ включена, передавать нечего, agnayas как был
    без явных параметров, так и остался).
  - Вриддхи → `spinTurns:2` (720°) + серебро/золото (см. открытый вопрос).
    В `TRANSFORM_KIND` тоже пока не включена — решить `signal` при первом
    реальном примере, не заранее вслепую.
  - Парная замена внутри варги (k↔g, глухая↔звонкая, придых↔непридых,
    любая↔назальная) → `TRANSFORM_KIND.vargaPair` = `spinTurns:0.5`
    (**180°, противолежащая грань** — геометрически материализованная
    «пара») + `signal:'blank'`.
  - Общая ассимиляция ПОД СОСЕДА (буква вне всех варг — h/ṃ/ḥ — целиком
    меняет место образования) → `TRANSFORM_KIND.assimToNeighbor` =
    `spinTurns:1` (**360°, не 180°** — нет «противолежащей грани», буква
    не парная сама по себе) + `signal:'blank'`.
- **Один кубик = одна фонема** (принцип с самого начала проекта, IAST
  сегментален по построению). Многобуквенный `tr` — ТОЛЬКО для сжатых
  крайних блоков инертного контекста (см. реестр), различать от
  однофонемных многобуквенных записей (придыхательные, дифтонги ai/au) по
  точному списку `MULTI_CHAR_PHONEMES`, не по длине строки.
- **Один цвет для всего не-главного.** Цвет чипа шага определяется ТОЛЬКО
  тем, главный ли шаг (`primary:true`), не его `kind`. Всё не главное
  (грамматика И вспомогательное правило в цепочке) — один и тот же
  стандартизированный охровый (`is-grammar`, `#CDA84E`), не новый цвет.
- **Сжатые крайние блоки — лимит ЖЁСТКИЙ, ровно 3 буквы.** Единственное
  согласованное исключение — 4 буквы, только для одного случая (правило
  62, «yati»), не прецедент для произвольного превышения.
- **Направление бокового раскачивания `transform`** (`op.clearance`,
  дефолт +0.35, вправо) — проверять для КАЖДОГО нового `transform`, кто
  сосед с какой стороны; раскачивание должно идти к пустому месту, не к
  занятому. Отрицательное значение — то же самое поле, зеркально.

---

## Часть 4 — Реестр эталонных эффектов (справочник, не источник для
копирования кода вручную)

**Правило пользования:** прежде чем писать `ops` для нового примера —
сверить каждую ситуацию с этой таблицей. Ситуация уже есть — берётся
готовая техника. Ситуации нет — после того как решена для нового примера,
таблица дополняется В ТОТ ЖЕ МОМЕНТ.

| Ситуация | Обязательная техника | Где реализовано |
|---|---|---|
| Триггер непрерывно воздействует на цель | Текстурный пульс на триггере (`setFacePulse`/`redrawPulseFace`), НЕ отдельный DOM-слой | `approach.holdPulse` (śādhi), `influence` (agnayas, унифицировано заход 43) |
| Конкретный сигнал долетает от триггера к цели (дискретный пакет) | `spawnWave` | `influence`, `approach.holdPulse` — уже единообразно |
| Буква превращается в другую на месте | `transform`, 4 категории — см. Часть 3 | agnayas, rule 71, taddhiraṇyam |
| Сближение вызывает реакцию у цели | Двухотрезочный `approach` с паузой | śādhi |
| Момент начала реакции | Вспышка+скачок масштаба РОВНО в момент начала, не только в конце | `elide` |
| Буква исчезает без замены | `elide`, погружение вниз | śādhi (s→∅) |
| Одна буква становится двумя | `split`, подъём вверх | agnayas (e→a+y) |
| Две буквы сливаются в одну | `merge` | āsīt (a+a→ā, только самоподобие — РАЗНЫЕ звуки не проверены) |
| Морфологическая граница внутри примера | Части разделены с начала, событие — следствие сближения | āsīt, śādhi |
| Внешние сандхи, граница слов не схлопывается | Зазор остаётся навсегда, ничего его не трогает | vāk asti, taddhiraṇyam |
| Длинный пример (>10 фонем) | Сжатые крайние блоки, авто-подгонка шрифта, лимит 3 (искл. 4 для правила 62) | taddhiraṇyam («yam») |
| Цвет вспомогательного/цепочного rule-шага | Стандартный охровый `is-grammar`, не новый цвет | taddhiraṇyam (шаг 2, без primary) |
| Зазор между двумя `rule`-шагами ЦЕПОЧКИ с ОДИНАКОВЫМ activeSlots | Проявление НЕ вставляется — `buildRuntimeSteps` сравнивает activeSlots соседних шагов (`sameActiveSlots`), при совпадении зазор пропускается без волны. Заход 52, реальный найденный баг: короткий зазор (250мс) + широкая раскладка (9 кубиков) → волна проявления (REVEAL_STAGGER×9 > зазора) не успевала докатиться, обрывалась на середине и откатывалась — левый и правый край ряда оказывались на разной стадии прерванной волны (до 0.128 разницы прозрачности у ОДНОЙ и той же буквы в двух позициях) | taddhiraṇyam (заход 52) — проверено численно, расхождение в проблемном окне сведено к 0.000000; agnayas (контроль, activeSlots РАЗНЫЕ между шагами) — проявление сохранилось как было, фикс не задел |
| Направление бокового раскачивания transform | К пустому месту, не к занятому — проверять заранее | agnayas, taddhiraṇyam (оба шага, разные знаки) |
| Чисто позиционный шаг (звук не меняется) | БЕЗ притенения — движение само рассказывает историю | āsīt (шаг 1), śādhi (пауза приближения) |
| Шаг реально меняет звук | Притенение уместно, activeSlots только для участников | agnayas, āsīt, śādhi, rule 71, rule 70 (шаг 2) |
| Одноразовый флаг блокирует продолжающийся хвост анимации (класс бага) | Финализация — под своим guard'ом; хвост/спад — БЕЗ guard'а по тому же флагу | Исправлено трижды: `applyTransform`, `applyMerge` (дважды), `applyElide` |

---

## Часть 5 — Состояние примеров (ДВЕ РАЗНЫЕ системы, не путать)

**Важно: в проекте есть ДВЕ независимые системы иллюстрации правил** — эта
часть документа раньше смешивала их в одну таблицу, что давало ложное
впечатление о масштабе готового.

### 5.1 — 2D-подсветка алфавита (`roleDemo`) — ОСНОВНОЙ охват, готов почти весь

**64 из 71 правила** имеют хотя бы один кликабельный пример с подсветкой
источник/триггер/результат прямо на алфавитной панели — НЕ через 3D-кубики,
через `roleDemo` в `EXAMPLES[n]`. Технический аудит (полный клик-свип по
всем правилам и чипам) дал 0 проблем.

**Подробный, точный статус — в отдельном файле `second-examples-todo.md`,
это НЕ дублируется здесь** (список длинный — 41 правило с точным числом
некликабельных «вторых примеров» по каждому). Держать этот файл среди
project knowledge ОБЯЗАТЕЛЬНО — без него список придётся пересчитывать
скриптом заново (алгоритм пересчёта записан там же).

Коротко, что в нём: 41 из 64 правил имеют ЕЩЁ ХОТЯ БЫ один пример в тексте
цитаты Эменó, который остаётся обычным некликабельным текстом (не ошибка —
сознательно отложено). Отдельная находка того же аудита — правила 19, 24,
43, 45 используют тот же переход i→e/i→ya, что уже подсвечивает таблицу
гуна/вриддхи у правил 2 и 6 (`gunaCells`), но сами этой связи не имеют —
не добавлено по инерции, стоит добавить при следующем заходе по каждому.

**Открытые методологические вопросы — ✅ ВСЕ ТРИ РЕШЕНЫ (заход 57, прямое
решение пользователя, реализовано в коде).** Полный текст решений и
реализации — в Части 3, разделе «Профессионализация», не дублируется
здесь второй раз.

### 5.2 — 3D слот-движок — НОВАЯ, малая по охвату система (эта сессия)

Пять правил, у каждого полноценная 3D-анимация (не просто подсветка
алфавита) — но интегрировано в основное приложение только два из пяти.

| Правило | Слово | Статус |
|---|---|---|
| 3 (agnayas) | agni+as → agnayas | Интегрировано в `EXAMPLES[3]`, текст переписан под академический регистр |
| 7 (āsīt) | a+as+īt → āsīt | Интегрировано в `EXAMPLES[7]`, текст переписан |
| 15 (śādhi) | śās+dhi → śādhi | Интегрировано в `EXAMPLES[15]` (заход 53) |
| 70 (taddhiraṇyam) | tat+hiraṇyam → taddhiraṇyam | Только тестовый полигон, требует ВИЗУАЛЬНОЙ проверки пользователем (последняя версия после исправлений по раскладке/направлению/цвету — заход 51, ещё не подтверждена глазами) |
| 71 (vāk asti) | vāk+asti → vāgasti | Только тестовый полигон, `EXAMPLES[71]` не подключено |

Правило 10 отдельно — только текст пояснения переписан (геминация), 3D для
него не строился и не планировался отдельно (пример короткий, ch↔cch,
2D-подсветки достаточно).

---

## Часть 6 — Что ещё не отработано (гэпы) и планы на будущее

0. **НЕЗАКРЫТОЕ РАССЛЕДОВАНИЕ (заходы 63–65): асимметричная/непоследовательная прозрачность притенённых кубиков.**
   Пользователь заметил: часть кубиков в состоянии «притенён» (dimOpacity,
   по умолчанию 0.22) визуально не выглядит прозрачной вообще, хотя РЕАЛЬНОЕ
   значение `material.opacity` у них — 0.22 (подтверждено напрямую через
   `window.__slotDebug` в консоли браузера, не предположение). **Важно: та
   же самая настройка (0.22) на ДРУГИХ буквах даёт идеальную прозрачность
   — значит, дело не в самом числе (не «0.22 слишком незаметно на тёмном
   фоне»), а в баге, который проявляется через раз, по кубику, а не
   по стороне/позиции.** Проверенные и ОТВЕРГНУТЫЕ гипотезы: (а)
   `depthWrite:false` у материала (заход 63) — не исправил проблему в
   большинстве примеров и добавил новый баг (кубики с поздним номером
   слота рисуются поверх ранних) — отменено в заходе 65; (б) «просто
   слишком тусклое значение» — отвергнуто самим пользователем по прямому
   наблюдению выше. Диагностический люк `window.__slotDebug = ctx`
   (в `slot-engine-mount.js`, сразу после создания `ctx`) оставлен в коде
   НАРОЧНО, по просьбе пользователя, для будущего расследования — не
   убирать не разобравшись. Тестировать ТОЛЬКО во время активного окна
   притенения (не после `settle` — там `frame()` каждый кадр принудительно
   возвращает всем `opacity:1`, любая ручная правка через консоль
   мгновенно затирается на следующем кадре, тест ничего не покажет).
1. **Слияние двух РАЗНЫХ звуков** (не самоподобие) — открыто. `merge`
   проверен только на a+a→ā.
2. **Удвоение (гемина́ция)** — открыто. Один звук → две одинаковые копии,
   `split` рассчитан на разные результаты. В более раннем планировании
   фигурировала под именем **`DOUBLE`** — там же были названы (тоже не
   построены) **`DISTANT`** (взаимодействие не соседних букв — RUKI и
   подобные дальние правила) и **`INSERT`** (появление звука, которого не
   было — эпентеза, не `split` «1→2» и не `arrive` «тихий приход»,
   отдельная категория). Конкретные названия сохранены на случай, если
   понадобится именно такая терминология при проектировании.
3. **Общий механизм ассимиляции «взять букву у соседа автоматически»** —
   открыто. Сейчас решается только жёстким `toGlyph` в данных примера
   (работает, но не масштабируется на «одна формулировка сразу для любой
   варги»).
4. **Групповая цель** — открыто. `influence`/`transform` рассчитаны на
   одну цель, не несколько одновременно от одной причины.
5. **Составные эффекты** (elide+double и т.п.) — открыто.
6. **Вриддхи** — открыто, ни разу не построена. Решить `signal:'gold'`
   заранее, не постфактум.
7. **`arrive`** — код готов, ни разу не применён по-настоящему.
7a. **Упражнения (26 штук) — НЕ НАЧАТО ВООБЩЕ, крупный отдельный пласт.**
    Полный текст всех 26 упражнений есть в чистом PDF Эменó (страницы
    31–60 архива, после самих правил), но база «упражнение → правило →
    разбор» не собрана. Режимы «сделать самому / посмотреть ответ» —
    только название идеи, не спроектировано. Это отдельный, более тяжёлый
    вопрос объёма, не продолжение текущей работы над правилами.
8. **Возврат в 3D правил 61/64/65** (ранее исключённых по длине) — тем же
   механизмом сжатых блоков, что уже применён на taddhiraṇyam, но ещё не
   сделан для этих конкретных правил. Правило 62 не влезает даже со
   сжатием — там нужно ручное исключение (4 буквы «yati»).
9. **Интеграция готовых примеров в `EXAMPLES[]`** — śādhi, rule 70,
   rule 71 построены и проверены на полигонах, но не подключены к
   основному приложению. Открытый вопрос (не решён): интегрировать по
   одному сразу после проверки, или сначала накопить несколько — по
   рассуждению видно, что «сразу после проверки» безопаснее (инженерные
   находки при интеграции — как баг с `text-transform:lowercase` или
   потолок высоты области — распространяются на уже готовые примеры
   быстрее, если интеграция не откладывается).
10. **taddhiraṇyam требует визуальной проверки** — последняя версия
    (заход 51) исправлена по пяти замечаниям, но не подтверждена глазами
    пользователя после исправлений.

### Хороший кандидат на следующий пример
Правило 55 (devā āsate) — явно цепляет правило 7 внутри себя, второй
реальный тест цепочки правил (после taddhiraṇyam), на знакомом материале.

---

## Часть 7 — Как построить новый пример (методология)

1. Заполнить **Поток А** — грамматический разбор правила без оглядки на
   технику: пронумерованные события, что происходит и почему. Каждое
   грамматическое/сандхи-событие — отдельная строка.
2. Для каждого события Потока А сверить с **реестром (Часть 4)** — если
   подходящая техника уже есть, использовать её. Если нет — это пункт в
   гэпах (Часть 6): сначала решить, добавляется ли новый `op`-тип в
   движок или комбинация существующих, и только потом писать данные.
3. Прежде чем писать `initial`/`ops` — явно ответить на вопрос **Главного
   принципа (Часть 2)**: где граница, что физически происходит при
   контакте?
4. Проверить **зарезервированные значения (Часть 3)**: категория
   transform (какой угол/сигнал), направление раскачивания, лимит на
   сжатые блоки, цвет вспомогательных шагов.
5. Только когда каждая строка Потока А имеет пару в технике (или
   осознанно оставлена «данными, не движком») — переходить к числам
   таймлайна. Считать формулами (падение → буфер → длительность операции
   → следующий буфер), не подбирать на глаз.
6. Прогнать **численную симуляцию реального кода** (вырезанного из
   файла) на всём таймлайне — не только на happy path, включая моменты,
   где цель/сосед могут исчезнуть или пересечься.
7. Собрать тестовую страницу (`test-slot-engine-ruleN.html`), добавить
   ссылку в `#dev-links` основного приложения. Ждать визуальной проверки
   пользователем — численная проверка НЕ равна визуальной готовности.
8. После подтверждения — интегрировать в `EXAMPLES[N]`, переписать текст
   пояснения под академический регистр (Часть 1, п.3).

---

## Приложение — agnayas (правило 3) целиком, как образец применения
методологии выше

Формула: **agni + as → agnaye (гуна) → agnayas (правило 3)**.

| # | Что происходит | Разряд |
|---|---|---|
| A0 | Исходная форма: `agni` + `as` | — |
| A1 | Окончание `-as` требует гунации предшествующей i-основы | [Грамм.], не сандхи (Whitney §338a) |
| A2 | `i → e` (гунация) | [Грамм.] |
| A3 | Промежуточная форма `agne`+`as` — несовместимое сочетание гласных на стыке | — |
| A4 | Правило 3: `e + a → a + y + a` | [Сандхи 3] |
| A5 | Итог: `agnayas` | — |

A1–A2 (гунация) и A4 (правило 3) — ДВА разных, независимых события с
разными причинами → два отдельных `step` с независимыми `activeSlots`,
не один растянутый шаг.

**Слоты:** 1=a, 2=g, 3=n, 4=i→e, 5=(пусто, под будущий Y), 6=a, 7=s.

**Таймлайн (мс):**

| мс | Событие |
|---|---|
| 0–2600 | Падение всех 6 букв |
| 2600–6700 | Шаг «грам.»: `influence` от {a,s} к i, `transform` i→e (серебро→серебро→истинный цвет) |
| 6700–8300 | Пауза-проявление (авто) |
| 8300–16150 | Шаг «правило 3»: `approach` (несовместимость, отскок) → `split` (пауза-осознание → подъём E → прилёт A+Y по дуге → сравнение → исчезание E) |
| 16150–17630 | Хвостовая пауза-проявление (авто) |
| 18630–20310 | `settle` (авто-старт = конец проявления + 1с) |

Точные параметры каждой операции — в `docs/app/examples/rule3-agnayas-slots.js`.
