# Emeneau / слот-движок — точка входа для нового чата

Этот документ — не «шаблон одного примера» (как назывался раньше), а
полная точка входа: если этот чат исчерпается, вставьте этот файл первым
сообщением в новый — он должен заменить необходимость объяснять заново
методологию, архитектуру и текущее состояние проекта.

---

## Часть 1 — Как со мной работать (её прямые требования, не мои привычки)

Каждый пункт ниже — прямое указание, полученное в ходе сессий, не
предположение о хорошем тоне.

1. **Критический подход обязателен.** Не соглашаться ради согласия, не
   останавливаться на первом рабочем варианте — искать слабые места и
   предлагать улучшения без просьбы. Доводить до настоящего завершения,
   не до формального выполнения запроса. Если предложение самого
   пользователя избыточно — прямо возразить и предложить более простой
   путь, не исполнять как есть.
2. **Скорость и минимализм кода — приоритет проекта.** Не добавлять код
   или зависимости без необходимости. Прежде чем писать НОВЫЙ код для
   эффекта/механизма — сначала проверить, нет ли уже готового решения в
   движке (существующий параметр, существующая функция), которое просто
   нужно применить с другими значениями или в зеркальном направлении. Не
   изобретать заново то, что уже решено — цель это унификация всех
   действий, не разрозненные частные решения под каждый пример.
3. **Академический регистр — высшая планка, не «попроще».** Рецензенты —
   лингвисты со степенями. Простой язык пояснений НЕ означает
   примитивизацию: формулировки лингвистически корректны в высшей
   степени, точная терминология (геминация, не «удвоение вдвоём» и т.п.),
   даже в кратких пояснениях к примерам. При этом термин должен быть
   действительно из источника или общепринятой санскритологии, не
   изобретённым — проверять по самому тексту Эменó (PDF — на деле ZIP со
   сканами страниц, без текстового слоя; читать глазами постранично, не
   grep'ать) или по независимым источникам, не полагаться на собственные
   более ранние пересказы.
3а. **`roleDemo.text` — обучает, не пересказывает правило.** Прямое
   требование: суть текстового блока под алфавитом не в том, чтобы
   переформулировать формулу сандхи короче, а в том, чтобы читатель
   понял ЧТО происходит, КАК и ПОЧЕМУ. Обязательно называть части речи и
   грамматические категории задействованных слов/морфем (часть речи,
   род, число, падеж, тип основы, класс глагола и т.п.), не только
   фонетический механизм — короткая фонетическая формула («a перед au
   поглощается») без грамматического контекста НЕ считается достаточной,
   даже если формально точна. Применяется к КАЖДОМУ новому примеру фазы
   автоматической постройки (Часть 6), не только особым случаям.
4. **Пуш — автоматически, если нет открытых вопросов.** Раньше пуш
   откладывался по умолчанию, потому что перенос правок на GitHub был
   ручной работой пользователя (собрать зип, самому вставить команды).
   Это больше не так: пуш выполняется напрямую из этой же сессии
   (git add/commit/push), ручного шага нет — откладывать по старой
   причине незачем. Текущее правило: если по ходу правки не осталось
   вопросов, требующих решения именно пользователя (неоднозначность в
   данных, выбор между вариантами и т.п.), — коммитить и пушить
   самостоятельно, не спрашивая разрешения на каждый пуш отдельно.
   Дожидаться прямого решения нужно только тогда, когда есть что
   реально утвердить — сначала закрыть этот вопрос, потом пушить. Когда
   пуш готовится — по-прежнему сверять с GitHub напрямую (клонировать
   свежую копию, сравнивать файлы), не полагаться на память о том, что
   уже отправлено.
5. **Значимые результаты — фиксировать сухим остатком.** Когда достигнут
   значимый вывод/решение/список открытых пунктов — проактивно предложить
   зафиксировать его (список/таблица, не проза), не оставлять только в
   переписке.
6. **Проверка — численная симуляция, не «на глаз».** Рендера/браузера нет
   вообще — только Node.js. Любое исправление таймлайна, геометрии,
   направления движения проверяется прогоном реального кода (вырезанного
   из самого файла, не переписанного по памяти) на реалистичных данных,
   с явным выводом чисел. Визуальная часть (насколько красиво/читаемо)
   всегда остаётся на подтверждение пользователя — не заявлять «визуально
   готово», когда проверено только численно.
7. **Реестр ведётся проактивно, не постфактум.** Каждое новое
   зарезервированное значение (цвет, число, направление, лимит) — в
   реестр в момент решения, не после того, как ошибка на его отсутствие
   уже произошла на следующем примере.
8. **Порядок фаз: сначала порядок в коде, потом лингвистическая проверка,
   и только потом — новое наполнение.** Прямое указание пользователя: не
   перескакивать на content-задачи (новые примеры, новые правила),
   пока не закончена работа над тем, что уже есть. Две фазы, строго по
   очереди, не параллельно и не вперемешку:
   1) **Программная стандартизация** — код должен пройти проверку
      программистами: тесты, линт, отсутствие мёртвого/устаревшего кода,
      ссылок на несуществующие файлы, следов истории чата в комментариях
      («заход N» и т.п.) — понятен и прозрачен для передачи стороннему
      разработчику без контекста этого чата.
   2) **Академическая проверка исходных данных** — только после того, как
      фаза 1 закрыта: переключиться в режим сверки с источником по
      лингвистике/грамматике санскрита. Проверить ВСЕ термины, связи
      источник/триггер/результат и примеры в данных (`data.js`,
      `EXAMPLES[]`, тексты правил `emeno`) против реального текста Эменó
      (Часть 2а), не полагаться на то, что уже проверялось раньше в
      другой сессии.
   Только когда обе фазы закрыты для конкретного куска работы —
   продолжать наполнение (новые 3D-примеры, новые правила и т.п.).

   **Фаза 2 — ГОТОВА для всех 71 правила (заход 78).** Построчная сверка
   `emeno`-текстов, глоссов и примеров `data.js` с реальным текстом
   источника (`docs/reference/Эмено2.pdf`) — построчно, все 71 правило,
   не выборочно. Найдено и исправлено: 4 подтверждённые ошибки (правило 2 —
   «3 pl.»→«3 sg.», bhavet реально 3 л. ед.ч.; правило 16 — han-
   «гнать»→«убивать», han- стандартно значит «бить, убивать»; правило 18 —
   убран выдуманный корень «snih-», источник даёт только druh-/muh-;
   правило 34 — опечатка «dṛmp-»→«dṛṃh-»), 6 неточных переводов приведены
   к источнику (правила 24, 26, 29, 30, 36, 43 — подробности в сообщении
   коммита `036595a`), 2 примера дополнены/заменены на источниковые
   (правила 25, 37). Заодно, при разборе пояснений `roleDemo.text` ко
   всем 64 примерам: опечатка «висургу»→«висаргу», и в правилах 19/24
   шаг гунирования перед `-sya-` был помечен тегом самого сандхи-правила
   вместо `'грам.'` (путал грамматический факт с сандхи-правилом,
   несогласованно с уже принятой в проекте категоризацией [Грамматика]
   vs [Сандхи N] — видно по тому, что agnayas/āsīt в том же файле
   правильно используют `tag:'грам.'` для аналогичных шагов).

   **Обзор формулировок пояснений — завершён.** Прочитаны все 64
   `roleDemo.text`. Помимо уже перечисленных находок (опечатки, теги
   'грам.', v→y), нашёлся один случай смешения понятий: правило 47
   (`vane + iha`) сравнивало e→a с «гуной наоборот», хотя гуна в проекте
   зарезервирована за морфологической ступенью, зависящей от формы
   слова, а не от соседнего звука (риск путаницы). Переписано по
   формулировке самого правила 47 (в `emeno` сравнения с гуной нет
   вообще — простая фонетическая замена). Остальные 63 пояснения уже на
   хорошем академическом уровне, менять не потребовалось.

---

## Часть 2 — ГЛАВНЫЙ МЕТОДОЛОГИЧЕСКИЙ ПРИНЦИП

**Суть примера — во ВЗАИМОДЕЙСТВИИ частей, не в готовом целом.** Закреплено
трижды как исправление уже готового плана (āsīt, śādhi дважды) — должно
применяться СРАЗУ на первом черновике, не после замечания.

Если внутри слова есть морфологическая граница (корень+окончание,
приставка+корень, слово+слово) — часть, участвующая в сандхи, НЕЛЬЗЯ
показывать уже собранной с самого начала. Обе стороны границы
падают/существуют РАЗДЕЛЁННЫМИ (зазором), и само событие сандхи —
СЛЕДСТВИЕ их сближения, не декорация поверх уже готового слова.

Практически: перед `initial`/`ops` ответить — «где граница, и что
физически происходит при соприкосновении сторон?» — заложить сближение
как отдельный видимый шаг ДО события сандхи.

**Уточнение (третий заход, тот же принцип глубже): мало развести и
сблизить — нужен ЗАМЕТНЫЙ момент прибытия.** Если сближающаяся сторона
не просто закрывает исходный зазор, а ЕЩЁ И занимает место
исчезающей/меняющейся буквы — это ДВА отрезка пути, не один. Сплошной
путь одной кривой читается как «проехало насквозь», не «подошло →
реакция → въехало». Нужна явная ПАУЗА между «дошло» и «довершило путь» —
реакция происходит ИМЕННО в паузе. В движке: `approach` с
`midDistance`/`midHoldDur`/`leg2Dur`.

**Момент начала реакции требует отдельного сигнала, не только момент
завершения.** Плавная кривая движения имеет нулевую производную в t=0 —
первые кадры визуально неразличимы. Обязательна вспышка+скачок масштаба
РОВНО в момент, когда реакция начинается (см. `elide`, заход 39).

**Геометрия отстойников — точная формула, не подбор.** Отступ от края
слотов до ближнего края отстойника = `CUBE_SIZE/2` (в любую сторону).
`holdOffset.y = FLOOR_Y − CUBE_SIZE/2 − CUBE_SIZE/2`. При CUBE_SIZE=1.1 →
−1.65 для нижнего. Запас у камеры — отдельно, реальной проекционной
математикой (`THREE.Vector3.project`), не на глаз.

**Направление отстойника — смысловой сигнал, не декоративный выбор.**
ВВЕРХ = превращается, что-то продолжается в новой форме (см. `split`).
ВНИЗ = пропадает без следа (см. `elide`).

**Направление любого бокового смещения — проверять числом, что физически
рядом, не полагаться на словесную интуицию.** Дважды пойманная ошибка:
«отступает от X» ошибочно интерпретировалось как движение в фиксированную
сторону без проверки, кто там физически сидит.

---

## Часть 2а — Источники и обработанный лингвистический анализ

**Обработанный разбор всех 71 правил (114 примеров)** — уже существует,
не нужно делать заново. **ВАЖНО для стороннего программиста: файл(ы)
ниже НЕ входят в git-репозиторий** (см. `.gitignore`,
`docs/reference/rules-1-71-analysis-consolidated*.md` — сознательно
исключены, тот же принцип, что и у сканов источника: не для публичного
распространения, репозиторий приватный, «все права защищены»,
см. `LICENSE`) — они существуют только локально/в project knowledge той
среды, где ведётся работа, и при клонировании репозитория заново их
НЕ БУДЕТ. На диске сейчас реально лежит `docs/reference/rules-1-71-
analysis-consolidated (2) (2).md` (задвоенный суффикс — артефакт
повторной загрузки, не переименовывать без причины, это НЕ два разных
файла). Формат: по каждому шагу **[Тип]** источник→триггер→результат, с
W-ссылками на Уитни (13 закрытых открытых вопросов из 16 на момент
последней сверки, сверена и с опубликованной книгой 2020г., и с
английским оригиналом). Отдельно упоминался файл `claude/rules-steps-
inventory.md` (сводная таблица по числу шагов и типу операции) — этого
файла НЕТ ни в репозитории, ни на диске проекта на момент проверки;
если он понадобится, придётся либо запросить его у пользователя
заново, либо пересчитать эквивалент из `interaction-registry.md`
(тот файл — В git, дополняет ту же информацию).

**Английский оригинал** — Emeneau, Van Nooten, *Sanskrit Sandhi and
Exercises*, 2nd rev. ed., University of California Press, 1968. Легитимные
источники (не пиратские):
- WorldCat (библиотечный каталог): https://www.worldcat.org/title/sanskrit-sandhi-and-exercises/oclc/169728
- UC Press (издатель, есть ePub+PDF): https://www.ucpress.edu/books/sanskrit-sandhi-and-exercises-revised-edition
- Google Books (превью/поиск): https://books.google.com/books/about/Sanskrit_Sandhi_and_Exercises.html?id=_9CSzQEACAAJ

Полный текст оригинала в проект не сохраняется (авторское право) —
цитируется только короткими фразами и пересказом, см. дисциплину
цитирования во всём этом документе.

**Если формулировка Эменó не устраивает (слишком лаконична, кажется
неточной или односторонней) — прямое указание пользователя: идти читать
дальше, не оставлять как есть.** Порядок: Уитни (Whitney, уже основной
рабочий справочник проекта, доступен целиком на Wikisource постранично —
`en.wikisource.org/wiki/Sanskrit_Grammar_(Whitney)/Chapter_N`, WebFetch
на весь текст страницы иногда обрезает/суммирует — при необходимости
точной цитаты выкачать raw HTML и вытащить нужный номер параграфа
скриптом, не полагаться на резюме модели), Бюлер (Bühler), Мюллер
(Müller), Фриш/Фрицше (уточнить точную идентичность при следующем
обращении — не проверено, какая именно фамилия), и сам Панини
(Aṣṭādhyāyī, точный номер сутры) — искать НЕ вместо Уитни, а В ДОПОЛНЕНИЕ,
пока формулировка не станет ясной и обоснованной. Пример применения —
правило 1 (a+au→au): формулировка emeno «a поглощается au» заменена на
Whitney §127 (единая таблица гуна/вриддхи-комбинаций) + Pāṇini 6.1.88
vṛddhir eci (технический термин eka-ādeśa, «единая замена» — оба звука
ВМЕСТЕ заменяются одним результатом, не один поглощает другой).

**Реестр взаимодействий и механик на все 71 правило** —
`docs/reference/interaction-registry.md` (заход 79, в git, не путать с
consolidated-файлом выше). Тот файл — лингвистический разбор
источник→триггер→результат; этот — сведение уже готовой 2D-типизации
(`roleDemo.type`) с 3D-механикой слот-движка (`apply*`) для каждого из
71 правила, включая 6 правил без единого зафиксированного примера
(5, 8, 9, 27, 34, 41). Не дублируется здесь — при выборе следующего
правила для 3D-примера сверяться с таблицей там, не с Частью 4 ниже
(та покрывает только 5 уже построенных правил).

## Часть 3 — Архитектура (что построено)

### Файлы
- `docs/app/lib/slot-engine.js` — точка входа, тонкий barrel-файл (заход 62,
  Стадия 5): `export * from` на каждый из модулей ниже, публичный API
  (что импортируют examples/тесты/приложение) не изменился ни на одно имя.
  Не редактировать реализацию здесь — её больше нет в этом файле.
  - `slot-engine-core.js` — константы, токенизатор, цвета, плавность
    (easing-функции), `setOpacity`. Ничего не импортирует.
  - `slot-engine-cube.js` — фабрика кубика (`makeCube`/`regenMats`/
    `updateShadow`), геометрия/материалы/тень. **Заход 64, унификация
    кубика:** геометрия кубика и тени — ОБЩАЯ на все показы (лениво
    строится один раз, `isSharedResource` защищает её от `unmount()`);
    `matsBlank`/`matsReady`/`matsSignal` — ленивые (строятся только при
    первом реальном обращении через `defineMatsSlot`, не все сразу).
    Новый вариант внешности кубика добавлять ТОЛЬКО через `defineMatsSlot`
    (не голым полем) — иначе он не будет уничтожаться при замене и снова
    потечёт память при каждом превращении буквы.
  - `slot-engine-words.js` — группы слов («вся единица, не одна буква»,
    `computeWordGroups`/`resolveSlotRef`).
  - `slot-engine-steps.js` — шаги (грамматика/правило N), runtime-таймлайн.
  - `slot-engine-validate.js` — `validateExampleData` (Стадия 4).
  - `slot-engine-ops.js` — обработчики операций (все `apply*`) и их прямые
    помощники (project/frontAnchor/spawnPulseRing/spawnWave/
    updateGroupFrame/setFacePulse/flyArcPosition) — самый крупный модуль,
    ~1000 строк. Новую `apply*`-функцию добавлять сюда.
  - `slot-engine-mount.js` — `mountSlotExample`, кадровый цикл, лента
    шагов, инъекция стилей.
  - `slot-engine-generate.js` — **ГЕНЕРАТОР данных примера** (заходы
    81, 83, 84, 87) — часть задуманного с самого начала «конструктора»
    (реестр взаимодействий + реестр механик + быстрая сборка, см. Часть
    6). Строит `{initial, steps, ops}` из декларативной спецификации
    (слова + буквы-участники + категория), не вручную. Покрывает ВСЕ 4
    механики, задействованные в 5 уже построенных примерах —
    `buildInfluenceTransformExample`/`buildInfluenceTransformChain`
    (influence+transform, один шаг или цепочка — voicing/place/часть
    grade), `buildApproachMergeExample` (approach+merge), `buildApproach
    ElideExample` (approach+elide), `buildGunaSplitExample` (guṇa+split,
    самая сложная — две механики подряд). Каждый сверен численно с
    реальным эталоном (раскладка побайтово, тайминги — в пределах
    20–250мс постоянного смещения по буферу после падения). Подробности
    и найденные баги — `docs/reference/interaction-registry.md` (не
    дублируется здесь). **НЕ используется EXAMPLES[] приложения** —
    только новые example-файлы `ruleN-name-generated.js` + тестовые
    полигоны `test-slot-engine-*-generated.html` (ссылки — секция
    «Инструменты разработки» в `docs/index.html`, заход 99, доп.; раньше
    были в `#dev-links` внутри приложения, панель убрана) для визуального
    сравнения с рукописным оригиналом. Реальный найденный баг (заход 87,
    доп.): `{word:N}` в `ops` движок читает как единицей-индексированное
    (`groups[N-1]`), спецификация генератора — нулём-индексированная
    (индекс в `spec.words`) — без `+1` при построении `ops` рамка-группа
    подчёркивала не то слово, approach двигал буквы не туда, движок
    падал. Исправлено, тест добавлен. **agnayas (rule3-agnayas-
    generated.js) — визуально подтверждён пользователем**,
    `buildGunaSplitExample` считается рабочим.
  - `slot-engine-types.js` — JSDoc-типы (`Ctx`, все 9 `SlotOp`, `Cube`,
    `ExampleData`, `Step`/`RuntimeStep`), проверяются `tsc --checkJs`
    (`npm run typecheck`, `tsconfig.json`) — не в барреле выше (ноль
    рантайм-экспортов, нечего реэкспортировать через `export *`).
- `docs/app/lib/chalk-module.js` — материалы/текстуры кубиков (`paintGlyph`
  с авто-подгонкой шрифта, `buildChalkMaterials`).
- `docs/app/examples/ruleN-name-slots.js` — данные каждого примера
  (`data` + `mount()`), общий для тестовой страницы и приложения.
- `docs/app/test-slot-engine-ruleN.html` — тестовые полигоны, ссылки в
  секции «Инструменты разработки» на `docs/index.html` (заход 99, доп. —
  раньше жили в `#dev-links` внутри приложения, панель убрана целиком по
  прямому запросу пользователя).
- `docs/app/sanskrit-sandhi-app.html` — основное приложение (правая/левая
  панели). Разбиение (по образцу Стадии 5 для slot-engine.js), идёт по
  шагам от безопасного к рискованному:
  - **Заход 67:** CSS (1347 строк) вынесен в `sanskrit-sandhi-app.css`
    (`<link rel="stylesheet">` вместо инлайн `<style>`) — проверено
    побайтовым сравнением, риск нулевой.
  - **Заход 68:** чисто декларативные данные (`SECTIONS` — тексты 71
    правила, `EXAMPLES` — 2D-роли на алфавите, `EXERCISES`, `ALPHA_ROWS` —
    раскладка алфавита; плюс `RULE_GROUP`/`RULE_SUBGROUP`/`GROUP_INFO`/
    `SUBGROUP_INFO`) вынесены в `docs/app/data.js`, `<script>` стал
    `<script type="module">` с одним `import` наверху. Проверено:
    побайтовое сравнение остатка файла (0 расхождений вне вырезанных
    диапазонов), `node --check` на обоих файлах, реальный импорт данных в
    Node (числа ключей/длины совпадают с ожиданием), новый тест
    `tests/dataModule.test.mjs` (форма и целостность данных — все 71
    правило встречаются по одному разу и т.п.), полный прогон
    `npm test`+`lint`+`check-conventions` (112/112). Скрипт был последним
    в `<body>`, поэтому переход на `type="module"` (откладывает
    исполнение до конца парсинга документа) не меняет фактическое время
    выполнения — вся разметка и так уже была распарсена к этому моменту.
    **Побочная находка:** `RULE_GROUP`/`RULE_SUBGROUP`/`GROUP_INFO`/
    `SUBGROUP_INFO` оказались МЁРТВЫМ кодом уже в оригинале — раскраска
    карточек правил на деле идёт через поле `g` прямо в `SECTIONS`
    (`cd.g`), эти четыре структуры нигде не читаются. Не удалены (это
    решение по содержанию, не механический перенос) — только не
    импортированы в основной скрипт, чтобы не тащить мёртвый импорт;
    сами остались экспортированы из `data.js` и покрыты тестом на случай,
    если понадобятся. Стоит решить отдельно: удалить или это задел под
    несделанную фичу группировки.
  - **Заход 69:** подсистема 2D-подсветки ролей на алфавите (источник/
    триггер/результат — SVG-линии, значки, гуна-таблица, лента шагов)
    вынесена в `docs/app/role-demo.js` (284 строки). Наружу — только три
    функции (`renderRoleDemo`/`renderRoleDemoSynced`/`clearRoleDemo`),
    остальное (`svgRoleLine`/`svgBadge`/`activateRoleCell(s)`/
    `renderRoleStep`/гуна-observer) — внутренние детали модуля. Модуль
    делает свои `document.getElementById('alpha-wrap'/'anim-tiles')` —
    те же id, что и в основном скрипте, порядок импорта не важен, элементы
    уже есть в статической разметке. Проверено тем же методом, что и в
    заходе 68 (побайтовое сравнение остатка файла — 0 расхождений,
    `node --check`, 112/112 тестов). **Реальный баг, пойманный ДО пуша:**
    первая версия модуля не объявляла `wrap`/`animTiles` внутри себя (они
    остались только в главном скрипте) — сработало бы `ReferenceError` при
    самой первой строке модуля. Поймано расширением `eslint.config.js` на
    `docs/app/**/*.js` целиком (раньше правило распространялось только на
    `lib/**`/`examples/**` — у `data.js` из захода 68 просто не было ни
    одной ссылки на `document`, поэтому дыра тогда не проявилась). Заодно
    добавлен `MutationObserver` в список глобалов и убран один
    неиспользуемый `catch (e)`.
  - **Заходы 70–71** (один и тот же чат, номер коммита сбился — см.
    Часть 1 п.4 про автоматический пуш; тут просто зафиксировано под
    правильным номером, задним числом): панель алфавита (тултип,
    аудио-заглушка, пин буквы, рендер `ALPHA_ROWS`) вынесена в
    `docs/app/alpha-panel.js` — наружу
    только `hideTooltip` (нужна карточкам правил). Следующим шагом —
    весь оставшийся блок «карточки правил слева + всё, что показывается
    по центру» (парсинг цитаты, зона монтирования 3D-анимации, заглушка
    левой панели, `showCenter`/`hideCenter`) вынесен в
    `docs/app/rule-panel.js` одним куском, не по функции за раз — выбор
    карточки слева полностью управляет центром, это одна связная единица,
    делить её пополам означало бы тащить состояние через границу модуля
    без причины. Наружу ничего не нужно — вводный экран, оставшийся в
    `sanskrit-sandhi-app.html`, с этим модулем по состоянию не пересекается.
    `sanskrit-sandhi-app.html`: 682 → 234 строки (только вводный экран и
    два `import './...js'` для побочных эффектов).
    Заодно удалён мёртвый код, попутно найденный: блок «цитата»
    (`quoteBox`/`showQuote`/`hideQuote` + CSS — не подключён к разметке
    ни разу за всю историю проекта, включая самый первый коммит),
    `getCtx`/`audioCtx`, неиспользуемая `groupStub`, и вся система
    отслеживания позиции мыши (`lastMouseX`/`Y`/`T` — писались, но нигде
    не читались; поймано ESLint'ом при переносе кода в настоящий модуль —
    инлайновый `<script>` в .html им не проверялся, отсюда и не был
    замечен раньше). `applyGroupHighlight`/`clearGroupHighlight` (задел
    под подсветку группы правил 20–26) — тоже не используются, но
    оставлены как есть (реальная, просто ещё не подключённая фича, не
    мусор) с точечным `eslint-disable-next-line`, не общим отключением
    правила на файл.
    **Заодно, тем же заходом:** сверка с верным источником
    (`docs/reference/Эмено2.pdf`, уже лежал в репозитории — см. Часть 2а)
    нашла придуманный, не подтверждённый источником глосс «повелевать»/
    «повелевай!» в `EXAMPLES[15].desc` (реально в источнике только
    «обучать»/«обучи»); из-за расхождения текста с цитатой правила не
    срабатывала склейка одинаковых примеров в `rule-panel.js`
    (`showCenter`, сравнение по префиксу до «→») — один и тот же пример
    показывался в приложении дважды. Исправлено на дословное совпадение
    с цитатой; скриптом проверены все 71 правило — других расхождений
    между `EXAMPLES[n].desc` и `emeno` нет.
    Проверено (оба среза): `node --check`, 112/112 тестов, lint и
    check-conventions чисто, сверка со свежим клоном GitHub после пуша.
  - **Осталось:** только сам вводный экран (`sanskrit-sandhi-app.html`,
    ~20 строк JS) и HTML-разметка (структура `#panel-sandhi`/`#center`/
    `#panel-alpha`, CSS-классы/id) — не тронуты. Разбиение JS-логики
    можно считать практически завершённым; следующий содержательный шаг
    в этом направлении — сама разметка, не JS.
- `package.json` + `tests/` — настоящая тестовая инфраструктура (см. ниже).
- `CHANGELOG.md` (заход 60, Стадия 3) — история разработки по заходам,
  вынесенная из комментариев `slot-engine.js`. Держать в паре с этим
  файлом: сюда — история «что и почему менялось», в CLAUDE.md — текущее
  состояние и методология.
- Старые рукописные модули (`rule3-agnayas.js`, `rule71-vak-asti.js`) — НЕ
  удалены, эталоны техник, не подключены к самому приложению
  (`sanskrit-sandhi-app.html`). **Уточнение (заход 88):** `rule71-vak-
  asti.js` до этого захода на деле БЫЛ подключён — `EXAMPLES[71]` ссылался
  на него напрямую (`module:`), эта запись была устаревшей. Найдено при
  интеграции слот-движковой версии (Часть 5.2) — `module` в `EXAMPLES[71]`
  переключён на `rule71-vak-asti-slots.js`, старый файл теперь реально, не
  только по описанию, не подключён никуда.
- `docs/index.html` + `docs/effects/*.html` — landing-страница и семь
  ОТДЕЛЬНЫХ, более ранних прототипов-этюдов (не на слот-движке, каждый —
  свой самостоятельный HTML-файл с собственным Three.js-кодом), которые
  landing-страница собирает в одном месте со статусами. **Уточнение
  (заход 72): вопреки более ранней записи здесь же — эти файлы НЕ
  «нигде не подключены», `docs/index.html` ссылается на все семь.**
  Найдено при прямом указании пользователя изучить их перед дальнейшей
  работой над вриддхи/гуной — не изучены глубоко, только каталогизированы:
  - `effects/rule-guna-agnayas.html` — гуна (agni-+as→agnayas), статус на
    landing «готово».
  - `effects/rule-vriddhi-stauti.html` — **вриддхи уже построена здесь**
    (stu-+-ti→stauti, Эменó упр. 10), статус «реализовано». Использует
    цвет `--c-vrddhi: #F0A24E` (оранжевый) — по описанию на самой
    landing-странице, вриддхи «сразу оранжевеет, жёлтого здесь нет,
    минует гуну» — ДРУГАЯ цветовая схема, чем решённая для слот-движка
    (серебро=гуна/золото=вриддхи, см. Часть 3 «Зарезервированные
    значения»): переносить механику на слот-движок, не цвет буквально.
  - `effects/rule-vriddhi-u-au.html` — вриддхи, изолированный черновик-этюд
    (u→au вне слова), статус «черновик».
  - `effects/rule-assimilation-varga-t-d.html` — ассимиляция по звонкости
    (t→d), статус «реализовано».
  - `effects/rule-assimilation-v2-via-elements.html` — та же ассимиляция,
    черновая проверка идеи общих переиспользуемых элементов
    (`lib/anim-elements.js`); по собственной пометке на landing —
    направление пересмотрено в пользу слот-движка, пройденный этап.
  - `effects/rule-samprasarana-yaj-tvaa.html`,
    `effects/rule-samprasarana-ya-i.html` — ослабление (samprasāraṇa),
    один готовый пример на реальном слове (yaj-+-tvā→iṣṭvā) и один
    изолированный черновик-этюд (ya→i).
  - `effects/effects-map.html` — сводная карта всех взаимодействий из
    упражнений Эменó со статусом реализации по каждому. **С Части 6, гэп
    11б — содержимое слито прямо в `docs/index.html` (раздел 04), сам
    файл больше НЕ является точкой входа с landing-страницы**, оставлен
    на диске как есть (не удалён, не трогать без явного запроса).
  Сама landing-страница ПЕРЕДЕЛАНА (заход 99, доп., дизайн — Часть 6, гэп
  11а) — подробности см. Часть 6, гэпы 11а/11б.

  **Решение по каждому файлу (заход 75, прямой вопрос пользователя «что
  значит заменили» — проверено пофайлово, не на память):**
  только 2 из 7 файлов реально пройденный этап — `rule-guna-agnayas.html`
  (то же слово agnayas, уже пересобрано на слот-движке и подключено) и
  `rule-assimilation-v2-via-elements.html` (сам автор на landing пометил
  «направление пересмотрено», это заброшенный эксперимент с подходом, не
  рабочий пример). `rule-assimilation-varga-t-d.html` — только ПРИЁМ
  (поворот на 180° к противолежащей грани) унифицирован в
  `TRANSFORM_KIND.vargaPair` и переиспользуется в rule70/rule71, но
  конкретно этот абстрактный t↔d этюд (не привязан к слову из источника)
  на слот-движке не пересобирался. Остальные 4 — `rule-vriddhi-stauti.html`,
  `rule-vriddhi-u-au.html`, `rule-samprasarana-yaj-tvaa.html`,
  `rule-samprasarana-ya-i.html` — **НЕ заменены ничем**: вриддхи и
  ослабление (samprasāraṇa) на слот-движке не построены вообще (см.
  Часть 6, гэп 6), это единственная существующая реализация этих техник
  в проекте. **Решение пользователя: ничего не удалять** — тот же подход,
  что уже применён к `rule3-agnayas.js`/`rule71-vak-asti.js` (оставить на
  месте как заморожённый референс механики жеста, не как код для правки).

### Визуальный язык 2D-модели ролей (справка, не 3D-движок)

Источник — без рамки. Триггер — пунктирная обводка. Результат — сплошная
белая обводка. Геометрия: место-ассимиляция (правила 37–40, 69) →
результат берёт столбец от триггера + ряд от источника. Озвончение/
оглушение (31–33) → столбец от источника, только звонкость от триггера.
Гуна/вриддхи → сдвиг источника внутри своего столбца, a/ā — катализатор
без координат. Подсветка всегда по клику на конкретный пример, никогда по
клику на карточку правила целиком.

### ⚠️ Неопределённый по статусу файл — `App.txt`

Среди файлов project knowledge есть `App.txt` — код на React + Framer
Motion (`motion/react`), совершенно другая технология, чем текущий проект
(vanilla JS + THREE.js). Судя по содержанию — отдельная анимация
«Деванагари → звук» для вводного экрана (буква проходит три фазы:
деванагари → IAST-слог → чистый звук). НЕ имеет никакого отношения к
`slot-engine.js`/`sanskrit-sandhi-app.html` — ни импортов, ни общих
структур данных. Не смогла определить: заброшенный черновик альтернативного
направления, или отдельный, не связанный с текущей работой проект,
случайно оказавшийся в том же project knowledge. Не переносила его
содержание никуда — при следующей сессии стоит спросить пользователя
напрямую, что это и нужно ли это вообще держать в контексте.

### ⚠️ Предупреждение о противоречии между старыми документами project knowledge

`handoff-engineering-state.md` утверждает, что 2D-подсветка перенесена
только на 5 демо-правил (39, 32, 2, 6, 33) и что клик по чипу «сейчас
ничего не подсвечивает». Это УСТАРЕВШЕЕ состояние — более поздний
`second-examples-todo.md` (с явной пометкой о консолидации от 2026-08-21)
и `handoff-slot-engine-session.md` подтверждают: система реально работает
на 64 из 71 правила, подсветка идёт через `slotstep`-событие, аудит дал
0 проблем. **При противоречии между старыми документами — доверять более
позднему по внутренним меткам времени, не более раннему.** `handoff-
engineering-state.md` стоит явно пометить как исторический (или убрать
из project knowledge), чтобы не запутать будущую сессию тем же образом.

### Профессионализация — ЗАВЕРШЕНА (все 5 стадий + CI/линт/типизация)

**Полная хронология (что нашли, что чинили, по заходам) — в
`CHANGELOG.md`, заходы 55–77.** Здесь — только то, что durably верно
СЕЙЧАС и нужно знать, чтобы работать с движком дальше:

- **Тесты** — 97+ тестов (растёт с каждым примером), встроенный
  `node:test`, без внешних фреймворков. Все 10 `apply*`-функций
  (`applyTransform`/`applyElide`/`applyInfluence`/`applyApproach`/
  `applySplit`/`applyArrive`/`applyMerge`/`applySettle`/`applyDim`/
  `applyStepDim`) — на уровне модуля, единая сигнатура `applyX(op,
  elapsed, ctx)` (кроме `applyStepDim(elapsed, ctx)` — не привязана к
  конкретной операции).
- **`ctx` (общий контекст, передаётся во ВСЕ `apply*`, не создаётся
  заново под каждую функцию):** `cubes`, `camera`, `stageEl`,
  `labelsEl`, `wordGroupsList` (нужен для `resolveSlotRef` группы слов),
  `scene` (нужен операциям, добавляющим новые THREE-объекты за кадром —
  `split`/`arrive`/`bud`), `runtimeSteps`/`data` (нужны только
  `applyStepDim`).
- **НЕ кандидаты на перенос на уровень модуля** (жёстко привязаны к
  жизненному циклу конкретного монтирования): `updateSteps`,
  `flashStepBoundary`, `fallY`, `resize`, `frame`, `unmount`.
- **Валидация** — `validateExampleData(data)` в `slot-engine.js`,
  вызывается первой строкой в `mountSlotExample()`, бросает `Error` со
  ВСЕМИ найденными проблемами разом (не первой). Проверяет форму и типы
  полей, которые `apply*` реально разыменовывают без `??`-дефолта —
  именно такие опечатки раньше давали тихий `NaN` в кадровом цикле, не
  понятную ошибку при монтировании.
- **Разбиение `slot-engine.js`** на 7 смысловых модулей — см. «Файлы»
  выше. Публичный API не изменился ни на одно имя (barrel `export *`).
- **CI** (`.github/workflows/ci.yml`) — `npm test`+`lint`+
  `check-conventions`+`typecheck`+`audit` на каждый push/PR. ESLint —
  только «рекомендованные» правила (реальные классы ошибок, не
  стилистика), старые эталонные модули исключены из линтинга.
  `scripts/check-no-zahod.mjs` — автоматическая проверка (не прозой в
  CLAUDE.md, прозаическая дисциплина реально подводила — «заход N»
  дважды случайно проскакивало обратно в код), что `docs/app/**/*.{js,html}`
  не содержит «заход N» (кроме `vendor/` и явно замороженных эталонов —
  `rule3-agnayas.js`/`rule71-vak-asti.js`/`anim-elements.js`).
- **Типизация** — JSDoc + `tsc --checkJs`, без сборки/транспиляции.
  `docs/app/lib/slot-engine-types.js` (только `@typedef`, ноль
  рантайм-кода), `npm run typecheck` (в CI тоже). Область — ТОЛЬКО
  `docs/app/lib` (сам движок), не `data.js`/`alpha-panel.js`/
  `rule-panel.js`/`role-demo.js` — сознательный выбор, не забытый
  участок. Барабан-файл (`export * from` каждого модуля) не терпит
  одинаковых `@typedef`-алиасов в двух разных барелящихся модулях —
  держать общий алиас в ОДНОМ модуле, в остальных — полный
  `import('./slot-engine-types.js').X`.
- **Интеграционные тесты UI-слоя** — `alpha-panel`/`rule-panel`/
  `role-demo` через jsdom (см. `tests/README.md`). `mountSlotExample` и
  само монтирование 3D-сцены (THREE.js/WebGL) — НЕ покрыты и вряд ли
  будут: jsdom не даёт WebGL-контекст, нужен headless-браузер вроде
  Playwright.

**Два актуальных предостережения про окружение (не история, реальная
ловушка при повторении):**
1. Если в новой сессии `node`/`npm` не находятся — это НЕ повод считать
   тестовую инфраструктуру нерабочей, сначала проверить фактическое
   наличие Node.js на конкретной машине (`winget install
   OpenJS.NodeJS.LTS`, если нет).
2. **На этой файловой системе (NTFS, case-sensitivity для этой папки
   ОТКЛЮЧЕНА — проверяется `fsutil.exe file queryCaseSensitiveInfo`)
   `claude.md` и `CLAUDE.md` — ОДИН И ТОТ ЖЕ файл, не два.** `git diff`
   между ними покажет «идентично», даже если это один и тот же файл,
   сравниваемый сам с собой — не подтверждение дубликата. Перед
   удалением «дубликата» с иным регистром имени — сначала `fsutil.exe
   file queryCaseSensitiveInfo <путь>` и/или `git ls-files | grep -i`
   (что реально отслеживает git по точному регистру).

### Типы операций движка

| Тип | Назначение |

|---|---|
| `influence` | Дальнодействие причина→цель: текстурный пульс на источнике + бегущие волны (`spawnWave`) + рамка группы, если источников >1 |
| `transform` | Буква меняется НА МЕСТЕ (глиф+цвет), 4 категории — см. реестр ниже |
| `approach` | Физическое сближение; поддерживает двухотрезочный путь с паузой (`midDistance`/`midHoldDur`/`leg2Dur`) и непрерывный текстурный пульс на триггере (`holdPulse`) |
| `split` | Одна буква распадается на ДВЕ РАЗНЫЕ (подъём в отстойник вверх, прилёт результатов по дуге) |
| `merge` | Две буквы сливаются в ОДНУ (едущий кубик, вспышка масштаб+emissive в момент контакта) |
| `bud` | Отпочкование — ЗЕРКАЛО `merge` (не вариант `split`): источник остаётся на месте и вспыхивает В МОМЕНТ ПОЯВЛЕНИЯ клона (не в момент контакта, как у merge), клон появляется РОВНО на позиции источника и едет по прямой (`easeOutBackProgress`, НЕ `easeOutBack` — см. ниже) в свой слот. Первый реальный тест гэпа DOUBLE — источник и клон получают ОДИНАКОВЫЙ `toGlyph` (rule61, n→nn), движок не делает для этого ничего особого |
| `elide` | Буква исчезает БЕЗ замены (погружение вниз в отстойник, вспышка в момент начала реакции). Прозрачность держится ПОЛНОЙ на всём погружении+паузе — угасание (и россыпь искр СОБСТВЕННОГО цвета буквы, `spawnSparkleBurst`) сосредоточено в самом конце (`fade`), не размазано по пути — но ТОЛЬКО когда это исчезновение само является содержанием правила. `quiet:true` — тихий вариант (без искр, без ускоренного сжатия, линейное угасание на весь `fadeDur`) для побочных исчезновений внутри чужого главного события (см. Часть 4, критерий) |
| `arrive` | Тихий прилёт буквы без слияния — код готов, ни разу не применён в реальном примере |
| `resist` | Попытка `elide`-подобного ухода БЛОКИРУЕТСЯ другим правилом-исключением — кубик тем же движением (`easeInOutCubic`) начинает уходить вниз, но не долетает: короткая пауза на пике и пружинистый отскок обратно (`easeOutBack`, тот же характер, что и у отскока `approach`), НЕ исчезает и не превращается. `groupSlots` (опционально) — рамка `GROUP_COLOR` под всей защищающей группой на всё время попытки (переиспользует `updateGroupFrame`, тот же приём, что и у многоисточникового `influence`, смысл зеркальный — не «источник влияния», а «то, что защищает»). Первый реальный пример — rule 8 (suhārt): d пытается отсечься следом за суффиксальным -s, но правило 8 не даёт |
| `settle` | Финальная волна «всё готово» — всегда автоматическая |

**РЕАЛЬНЫЙ НАЙДЕННЫЙ БАГ (при разработке `bud`, rule61): `easeOutBack` — не
настоящая 0→1 функция.** Спроектирована специально под связку
`shift*(1-te)` в `applyApproach` (retreat) — там `f(0)=1`, `f(1)=1`, горб
посередине, это ОБРАТНАЯ форма, не обычный 0→1 прогресс с перелётом у
конца. Прямой `lerp(from, to, easeOutBack(t))` (как понадобилось для
`bud`) даёт кубик, ТЕЛЕПОРТИРУЮЩИЙСЯ сразу в цель на t=0 (`easeOutBack(0)
=== 1`, не 0) — пойман сразу тестами (`tests/applyBud.test.mjs`), не в
браузере. Добавлена ОТДЕЛЬНАЯ `easeOutBackProgress` (настоящая формула
Пеннера, РАЗНЫЕ коэффициенты у квадратичного/кубического члена, `f(0)=0`,
`f(1)=1`) — `easeOutBack` НЕ тронута (используется `approach`, риск
регрессии там не нужен).

### Пилюли-подписи эффектов (`spawnLabelPill`, поле `op.label`)

Прямой запрос пользователя: во время события над (или под) местом действия
плавает короткое слово, называющее сам эффект («Гуна», «Вриддхи»,
«Ассимиляция», ...) — не заменяет текст примера, даёт зрителю сразу
считываемый якорь. Тот же DOM-оверлей язык, что и у колец/искр
(`project()` один раз при создании, дальше чистая CSS-анимация,
самоудаление по `setTimeout`) — НЕ отслеживается покадрово (лёгкий боковой
дрейф кубика во время паузы/вращения для читаемости не критичен, та же
логика, что у колец). Две анимации разом на каждой пилюле: «конверт»
видимости (плавно появилась → плавно исчезла, один раз, на всю
длительность события) и непрерывное «дыхание» (лёгкая пульсация
масштабом, фиксированный период 1500мс, зациклена — не зависит от
длительности самого события) — по прямому запросу «подрагивают, дышат,
привлекают внимание». Визуально — та же скруглённая семья, что и у
`.slot-step-chip` внизу, не новый язык.

**Направление — тот же смысловой регистр, что и у направления отстойника
(см. выше в этой же Части 3): `above=true` (над рядом) для
`transform`/`merge`/`split` — «продолжается в новой форме»; `above=false`
(под рядом) для `elide` — «пропадает». Расстояние от ряда — `CUBE_SIZE×1.6`
(«1-2 кубика», по прямому запросу).**

**АЛГОРИТМ РАЗМЕЩЕНИЯ ПИЛЮЛЬ (прямое требование пользователя, закреплено
после rule50 — две пилюли + прилетающий кубик в одном шаге):**
1. **Не пересекаться с объектами.** Не полагаться на подбор числа по
   Евклидовой 3D-дистанции — на rule50 дважды подряд формальный «запас»
   по такой модели визуально всё равно оказывался пересечением (модель
   не учитывает экранную проекцию/ширину самой пилюли). Где возможно —
   структурная гарантия, не приближение: если траектория (`arrivals`,
   `holdOffset`) МОНОТОННА по одной из осей (проверяется численно, как
   `flyArcPosition`/`easeInOutCubic` реально ведут себя на всём t∈[0,1]),
   размещать пилюлю по ДРУГУЮ сторону этой границы (`labelX`) — тогда
   пересечение исключено по построению, а не численно маловероятно.
2. **Несколько пилюль рядом — на одной прямой.** Если в одном шаге у
   двух и более операций есть `label`, их `labelY` (и, если применимо,
   ось выравнивания) должны совпадать — иначе события читаются как два
   разных, а не одна связная реакция (см. `SplitOp.labelY`/`labelX`,
   `TransformOp.labelY` в `slot-engine-types.js`).
3. Оба поля (`labelX`/`labelY`) — необязательные per-op переопределения,
   НЕ менять дефолт `spawnLabelPill` (`CUBE_SIZE×1.6`, x по `above`) —
   он безопасен для всех остальных, уже подтверждённых примеров
   (agnayas, rule42, rule55, rule70/71). Трогать дефолт для одного
   примера — риск тихой регрессии в другом (см. НАЙДЕННЫЙ БАГ выше,
   rule50/agnayas).
4. **Не прятаться в СОБСТВЕННОЙ вспышке события.** «Не пересекаться с
   объектами» (п.1) недостаточно — пилюля может не перекрываться
   геометрически с кубиком и всё равно теряться, если её высота попадает
   в зону визуальных ЭФФЕКТОВ того же события (`spawnPulseRing` растёт до
   `scale:4.4` от базовых 18px — заметный радиус, не точка). НАЙДЕННЫЙ БАГ
   (rule1/rule2, живая проверка: «не увидела подписи про слияние») —
   `merge`'s `labelY:0.88` численно совпадал с проекцией пика радиуса
   собственного кольца-вспышки слияния — пилюля географически «стояла
   рядом», но визуально тонула в самом ярком моменте своего же события.
   Проверять численно (`project()`, та же техника, что и для геометрии
   кубиков) высоту, на которую реально дотягивается вспышка/кольцо/
   искры ДАННОГО op, не только положение соседних кубиков — щедрый запас
   сверх неё, не точное касание границы.

Поле `op.label` — необязательное, добавляется на КОНКРЕТНЫЙ экземпляр
операции в данных примера, не выводится автоматически из `signal`/
`TRANSFORM_KIND` — движок не пытается угадывать словесную категорию
(`vargaPair`, например, может означать и озвончение, и оглушение, и
назализацию в зависимости от конкретных букв). Момент появления — начало
РЕАЛЬНОГО действия (конец anticipateDur у transform/split, `op.start` у
merge/elide), не раньше паузы-осознания. Длительность — вся видимая жизнь
события (оборот+пауза-фиксация у transform; путь мувера+спад вспышки у
merge; подъём+ожидание+сравнение+угасание у split; rise+hold+fade у
elide — включая `quiet`, тихий elide тоже получает подпись, просто без
искр/сжатия).

**Словарь уже применённых подписей — реестр, не изобретать заново
термин на каждый новый пример.** ПЕРЕВЕДЕНО НА ИАСТ, всегда строчными
(прямое требование пользователя, живая проверка rule50 — прежние
кириллические подписи держали непоследовательный регистр между
примерами; академический регистр требует настоящих санскритских
терминов, не русских калек, см. Часть 1 п.3):

| Механика | Подпись | Где применено |
|---|---|---|
| `transform`, `signal:'silver'`/дефолт | guṇa | agnayas (i→e) |
| `transform`, `TRANSFORM_KIND.vrddhi` | vṛddhi | rule 42 (e→ai) |
| `transform`, `TRANSFORM_KIND.vargaPair`, конкретно ОЗВОНЧЕНИЕ (Pāṇini 8.2.39, jhalāṃ jaśjhaśi — глухая непридыхательная→звонкая непридыхательная перед звонким) | jaśtva | vāk asti (k→g), taddhiraṇyam шаг2 (t→d) |
| `transform`, `TRANSFORM_KIND.assimToNeighbor` (место-ассимиляция h→dh) | ассимиляция *(временно кириллицей, строчными — подтверждённый санскритский термин ещё не найден, открытый пункт)* | taddhiraṇyam шаг1 (h→dh) |
| `merge` (2 источника → 1 результат, ОДИНАКОВЫЕ гласные, Pāṇini 6.1.101 akaḥ savarṇe dīrghaḥ) | savarṇadīrgha | āsīt (a+a→ā) |
| Два `split` параллельно, общий arrival только у одного (2 РАЗНЫХ звука → 1 результат, P6.1.113, НЕ savarṇadīrgha — там строго одинаковые гласные) | слияние *(временно кириллицей, строчными — точный санскритский термин для ЭТОЙ конкретной формулы ещё не найден, открытый пункт)* | rule 50 (-as→o) |
| `split` (1 источник → 2 результата), конкретно «дифтонг+гласный→a+полугласный» — собственное традиционное название правила у Уитни (W131, आयादि, «ay и подобные»: e→ay, o→av, ai→āy, au→āv), тот же тип имени, что и guṇa/vṛddhi (называет ПРАВИЛО, не описывает результат) — решено пользователем в пользу термина, не описательного слова | ayādi | agnayas (e→a+y) |
| `split` (1 источник → 2 результата), конкретно «i/u ОДНОСЛОЖНОЙ основы/корня перед гласным → i/u (короче) + y/v» — НЕ дифтонг, качественно другая пара звуков, чем ayādi выше, поэтому НЕ то же имя. Pāṇini-термин ИМЕННО для этой замены (6.4.77 aci śnudhātubhruvāṃ yvor iyaṅuvaṅau) | iyaṅ | rule 4 (ī→i+y, strī-+ā→striyā) |
| `split` (1 источник → 2 результата), тот же класс, что iyaṅ выше, но ṛ (не i/u) — распадается на короткий гласный (i/u, по предыдущему согласному) + СОГЛАСНЫЙ r, не полугласный. Устоявшийся санскритский термин ИМЕННО для этой замены не найден (Whitney §242 описывает как нерегулярность ограниченного списка корней, не чистое правило) | огласовка ṛ *(временно кириллицей, открытый пункт)* | rule 5 (ṛ→i/u+r, pṛ-+ā→purā) |
| `split` (1 источник → 1 результат, приземляется в тот же исходный слот — НЕ 1→2, остатка нет) — сегмент ПОЛНОСТЬЮ заменяется категориально другим, вращение здесь не годится (см. статус правила 6 в Части 5.2, переклассификация) | yaṇ | rule 6 (ṛ→r, pitṛ+os→pitros) |
| `elide` (исчезновение — само содержание правила ИЛИ `quiet`-побочное, см. Часть 4) — санскритский термин для исчезновения/нулевой замены (используется и в самом тексте правила 15) | lopa | śādhi (s→∅), rule 55 (s→∅) |
| `transform`, `signal:'blank'`, буква становится знаком элизии (не исчезает — заменяется ВИДИМЫМ символом; термин уже в тексте самого правила 50, W175a) | avagraha | rule 50 (a→') |
| `elide`, но результат ЕСТЬ (не нулевая замена) — два звука ВМЕСТЕ заменяются одним общим результатом (Pāṇini 6.1.84 ekaḥ pūrvaparayoḥ, адхикара на 6.1.72–6.1.111 целиком, сюда же попадают 6.1.87 ād guṇaḥ и 6.1.88 vṛddhir eci) — НЕ `lopa`: lopa (P. 1.1.60 adarśanaṃ lopaḥ) означает замену на НОЛЬ, здесь же результат виден. У rule1 результат совпадает по написанию с одним из исходных (au); у rule2 — genuinely новый глиф (e), но САМ АКТ тот же — a не переживает событие отдельно от результата | ekādeśa | rule 1 (a+au→au), rule 2 (a+ī→e, Whitney §565a «ī blends with a»), rule 42 (a+e→ai, Whitney §127, та же таблица) |
| `bud` (отпочкование, истинное удвоение — источник остаётся, клон появляется рядом) — устоявшийся санскритский термин (в традиции — dvitva) НЕ подтверждён дословно по источнику для этого конкретного случая | геминация *(временно кириллицей, открытый пункт)* | rule 61 (n→nn, hasan+agacchat→hasannagacchat) |

Ещё не построено ни разу (будет своя подпись при первом реальном примере,
не проектировать вслепую заранее): «Ослабление» (samprasāraṇa, см. Часть
6, старые прототипы `docs/effects/rule-samprasarana-*.html`), возможный
третий подтип `transform` для правила 36 (RUKI, см.
`docs/reference/interaction-registry.md`, Часть В, п.3 — правило 5 уже
закрыто, оказалось `split`, не `transform`, см. статус правила 5/6 в
Части 5.2).

**Термины, остающиеся НЕ решены — требуют проверки по источнику, не
гадать:**
- «ассимиляция» (кириллица, временно) — taddhiraṇyam шаг1, h→dh,
  место-ассимиляция вне варги.
- «слияние» (кириллица, временно) — rule 50, -as→o, P6.1.113 — НЕ
  savarṇadīrgha, та формула строго для одинаковых гласных.
- «геминация» (кириллица, временно) — rule 61, n→nn, истинное удвоение.
- «огласовка ṛ» (кириллица, временно) — rule 5, pṛ-+ā→purā, ṛ→i/u+r.
- **РЕШЕНО. `lopa` у rule1 — сомнение пользователя было верным, термин
  ДЕЙСТВИТЕЛЬНО не подходил.** Проверено (Pāṇini 6.1.84/6.1.87/6.1.88,
  sanskritdictionary.com — см. таблицу выше): `lopa` строго означает
  замену на ноль (P. 1.1.60 adarśanaṃ lopaḥ), а в rule1 результат ЕСТЬ
  (au) — просто зрительно совпадает с одним из исходных звуков. Верный
  термин — `ekādeśa` («единая замена», P. 6.1.84, адхикара на весь блок
  6.1.72–6.1.111, включая и 6.1.87 (rule2, ād guṇaḥ), и 6.1.88 (rule1,
  vṛddhir eci)). Подпись добавлена в `rule1-devau-slots.js` (на `elide`
  звука a — тот, что физически исчезает с экрана, хотя грамматически
  «исчезновением» это не является).

**Текущие рабочие значения `.slot-label-pill`** (после нескольких раундов
коррекции по обратной связи — полная история в CHANGELOG.md): font-size
**15px**, padding 7px 16px, дыхание scale **1↔1.10**, свечение blur
**6px**/spread 1px/alpha .32. `elide`-пилюли (below) сдвинуты вправо на
`CUBE_SIZE×0.9` (компенсация отрицательного `holdOffset.x`, который есть
у всех `elide`). Пилюля `transform` гаснет строго за `dur` (не
`dur+holdDur` — сигнальный цвет к тому моменту уже сменился на истинный).
Рабочий процесс на будущее (прямая формулировка пользователя): сообщать
текущие числовые значения явно перед правкой, не только менять их молча.

### Глоссарий терминов (`GLOSSARY`, `data.js`) — прямой запрос пользователя

Пилюля-подпись видна несколько секунд во время 3D-анимации — зритель не
обязан помнить/знать, что значит «yaṇ» или «ekādeśa» уже к моменту, когда
она погасла. Прямой запрос: КАЖДЫЙ санскритский термин, показанный
пилюлей, должен объясняться прямо в постоянном текстовом блоке под
алфавитом (`#role-steps-text`), со ссылкой на растущий глоссарий — не
только мелькнуть в сцене и пропасть.

**Архитектура — ПЕРЕСМОТРЕНА ПОСЛЕ ЖИВОЙ ПРОВЕРКИ (первая версия,
попап внутри тренажёра, была одним из двух рекомендованных вариантов,
явно предъявленных пользователю заранее — но по факту, увидев её на
экране, пользователь предпочла другой, ранее отклонённый вариант).
Прямая правка: «более уместно оставить кликабельную ссылку, которая
будет вести на страницу глоссария — и стандартный текст в описании в
блоке под алфавитом», НЕ всплывающее окно поверх интерфейса — ссылка
как ПОСТОЯННЫЙ, копируемый артефакт (можно открыть в новой вкладке,
поделиться прямой ссылкой на термин), не разовое действие.**

1. **Показ — обычная ссылка на отдельную страницу `docs/app/glossary.html`**
   (НЕ попап, НЕ секция `docs/index.html` — третий из трёх изначально
   предъявленных вариантов). Страница рендерит ВСЕ записи `GLOSSARY`
   через тот же импорт данных, что и остальное приложение (не
   дублирует контент вручную в HTML) — заголовок термина, определение,
   источник, оговорка `openTerm`; якорь `#term-<glossarySlug(term)>` на
   каждой записи, `:target`-подсветка при переходе по прямой ссылке.
   Ссылка из тренажёра — `target="_blank" rel="noopener"`: зритель не
   теряет место в текущем примере.
2. **Привязка термина к шагу — явное поле `glossaryTerm`** в
   `EXAMPLES[n].roleDemo.steps[i]` (строка ИЛИ массив строк — шаг может
   объяснять НЕСКОЛЬКО событий разом, см. rule1/rule2/rule42/rule50/
   rule70, где merge+transform или два transform уместились в ОДИН 3D-шаг
   с двумя разными `op.label`), НЕ автоматическое распознавание слова в
   прозе `step.text` — тот же принцип, что и весь остальной проект (label
   у 3D-операции тоже явный, не выводится угадыванием). Это поле НЕ
   менялось между версиями UI — только способ ПОКАЗА термина (п.1 выше)
   пересмотрен, привязка данных осталась прежней.

`GLOSSARY` (`data.js`, перед `EXAMPLES`) — объект `{термин: {definition,
source, openTerm?}}`, ключ ТОЧНО совпадает со строкой `op.label`, включая
временные кириллические плейсхолдеры («слияние», «ассимиляция»,
«геминация», «огласовка ṛ» — санскритский термин для этих случаев ещё не
найден, см. «Термины, остающиеся НЕ решены» выше). `openTerm:true`
показывает на странице глоссария честную оговорку («устоявшийся термин
пока не найден»), не выдаёт временное обозначение за готовый ответ.
Растёт ПОСТЕПЕННО вместе с новыми примерами — не собирается заранее для
ненаписанных правил, тот же принцип, что и у «Словаря уже применённых
подписей» выше (это по сути ОДИН И ТОТ ЖЕ список фактов в двух формах:
markdown-таблица для разработчика читающего CLAUDE.md, `GLOSSARY` для
зрителя тренажёра — оба обновляются в один момент, не порознь).
`glossarySlug(term)` (`data.js`, экспортируется рядом с `GLOSSARY`) —
общая формула превращения термина в URL-безопасный якорь (пробел→дефис,
нужен для плейсхолдеров вида «огласовка ṛ») — ОДНА функция на обе
стороны ссылки (`role-demo.js` строит `href`, `glossary.html` строит
`id`), не дублируется по формуле в двух местах.

`role-demo.js`: `renderGlossaryTerms(step)` рендерит ряд ссылок
`.glossary-term-link` СРАЗУ ПОСЛЕ `step.text` (внутри того же
`#role-steps-text`), каждая — `<a href="glossary.html#term-...">`. Термин
без записи в `GLOSSARY` тихо не рендерит ссылку (`console.warn`, не
бросает) — защита от опечатки в данных, тот же класс защиты, что и
`activateRoleCell`'s warn на отсутствующую клетку алфавита. 6 тестов
(`tests/roleDemo.test.mjs`) — рендер строки/массива термина (текст+href
+target+rel), отсутствие регрессии без `glossaryTerm`, защита от
опечатки, `glossarySlug` на термине с пробелом.

**Ретроактивно добавлен `glossaryTerm` во ВСЕ уже интегрированные
примеры, имеющие соответствующую 3D-пилюлю** (rule1, rule2, rule3/agnayas,
rule4, rule5, rule6/pitros, rule7/āsīt, rule15/śādhi, rule42, rule50,
rule55, rule61, rule70, rule71 — 14 файлов `roleDemo`, только ДАННЫЕ, ни
одна 3D-механика не тронута, риск нулевой для уже подтверждённых
визуально анимаций).

**НАЙДЕННЫЙ РЕАЛЬНЫЙ ПРОБЕЛ, попутно:** `EXAMPLES[71][1]` (vāk asti,
второй элемент массива правила 71) имел `module`, но НЕ имел `roleDemo`
ВООБЩЕ — 3D-пилюля `jaśtva` показывалась без единого слова объяснения под
алфавитом (`renderRoleDemoSynced`/`renderRoleDemo` не вызываются вовсе,
если `ex.roleDemo` не задан, см. `rule-panel.js`). Добавлен `roleDemo` с
нуля (`type:'voicing'`, `source:'ka'`, `trigger:'a'`, `result:'ga'`,
`glossaryTerm:'jaśtva'`) — тот же грамматический факт, что уже в
`SECTIONS`-тексте правила 71 (глухой смычный перед звонким звуком, ЛЮБЫМ,
включая гласные, озвончается). Ранее это была тихая, никем не замеченная
прореха в уже «интегрированном и подтверждённом» примере — обнаружена
только при системной проверке «а что показывается под алфавитом для
каждой 3D-пилюли», не через живую проверку пользователем (тот текст
раньше просто не существовал, смотреть было не на что).

**РЕАЛЬНЫЙ НАЙДЕННЫЙ БАГ (тот же заход, отдельно от переделки UI
глоссария): rule6 был визуально подтверждён пользователем («работает,
ок») ПОСЛЕ переклассификации transform→split, но интеграция в
`EXAMPLES[6]` (добавление `module`) была пропущена** — карточка на
`docs/index.html` при этом уже стояла в разделе «в работе», а
`module` в данных так и не появился. Обнаружено пользователем по
скриншоту («не запускается пример») при попытке открыть правило 6 в
самом приложении — 3D-анимации не было вообще, только 2D-текст, потому
что `rule-panel.js` не монтирует 3D без `ex.module`. Исправлено —
`module` добавлен, карточка перенесена в «готовые примеры» (14 правил).
**Урок:** подтверждение пользователем механики на тестовом полигоне —
не то же самое, что фактическая интеграция; проверять по данным
(`grep module`), не полагаться на память о том, что «наверное, уже
сделано» между разнородными задачами одной сессии (переключение на
запрос глоссария разорвало естественную последовательность
«подтвердили→сразу интегрировали», которая соблюдалась для rule1-5).

224/224 тестов, lint/check-conventions/typecheck чисто.

**ПЕРЕОСМЫСЛЕНА ЦЕЛЬ ГЛОССАРИЯ (прямое требование пользователя, после
работы над данной/padânta у rule9-bharan): «не только обозначить
взаимодействия, мы должны уяснить язык Панини — понять буквально каждый
из используемых терминов... впоследствии мы расширим программу до
грамматики — всё пригодится».** Раньше `GLOSSARY` собирал ТОЛЬКО термины-
названия конкретных операций сандхи (то, что показывает 3D-пилюля,
`op.label`) — теперь ЦЕЛЬ ШИРЕ: полноценный терминологический аппарат
языка САМОГО Панини, включая метаязыковые понятия его грамматики (не
названия отдельных правил, а то, ЧЕМ Панини описывает СТРУКТУРУ своей
грамматики) — задел под будущее расширение приложения до полноценной
грамматики, не только сандхи.

**Добавлено деванагари** (`entry.devanagari`, новое поле) — ВСЕМ реальным
записям (не `openTerm`-плейсхолдерам — деванагари у ВРЕМЕННОГО русского
обозначения ненайденного термина не имеет смысла). `glossary.html`
рендерит его рядом с ИАСТ-названием термина (`.term-devanagari`,
шрифт «Tiro Devanagari Sanskrit», тот же, что уже используется на
`docs/index.html` — добавлен `<link>` Google Fonts и в `glossary.html`,
раньше там девангари не было вообще).

**Новые термины (9), найденные при построчной сверке `EXAMPLES[]`
(конкретно — суффикс/бахуврихи/дата у suhārt/bharan, но заодно
захвачены другие уже-используемые в видимом тексте `roleDemo.text`
термины, найденные тем же проходом):**
- `sandhi` (सन्धि) — САМ предмет программы, общий термин, раньше не имел
  собственной записи вообще (только назывался в прозе).
- `avasāna` (अवसान, Pāṇini 1.4.110) — пауза/конец высказывания; локативная
  форма `vāvasāne` (8.4.56, уже в `cartva`'s тексте) СВЁРНУТА в эту же
  запись, не отдельный ключ — это грамматическая форма ТОГО ЖЕ слова, не
  другой термин.
- `saṃyogāntasya lopaḥ` (संयोगान्तस्य लोपः, Pāṇini 8.2.23) — ОТДЕЛЬНАЯ от
  общего `lopa` запись: конкретная сутра (словофинальное скопление
  согласных сокращается ИТЕРАТИВНО), а не любое исчезновение-на-ноль.
- `pada` (पद, Pāṇini 1.4.14) — оформленное слово; `padânta`/`padasya`,
  встречающиеся в прозе, СВЁРНУТЫ в эту же запись — сама сутра для
  «padânta» не найдена (это описательное сочетание, не термин Панини,
  указано честно в определении).
- `bahuvrīhi` (बहुव्रीहि, Pāṇini 2.2.23) — тип сложного слова.
- `khar` (खर्, Pāṇini 8.4.55, пратьяхара) — все глухие согласные разом;
  локативная форма `khari` (уже в `cartva`'s определении) СВЁРНУТА сюда же.
- `adhikāra` (अधिकार) — метаязыковой термин: заголовочное правило,
  действие которого продолжается на ряд последующих сутр.
- `varga` (वर्ग) — группа смычных по месту образования (jaśtva/cartva —
  обе операции ВНУТРИ одной варги).
- `nimitta` (निमित्त) — метаязыковой термин: формальная причина/условие
  операции (то, что вызывает изменение, не обязательно само меняется).

Каждый термин верифицирован WebSearch (Pāṇini/sanskritdictionary/
wisdomlib) ПЕРЕД записью в глоссарий — та же дисциплина цитирования, что
и везде в проекте (Часть 1 п.3), не по памяти.

**Ссылки в пояснениях под примерами (`glossaryTerm`, третье требование
пользователя) — добавлены ТАМ, ГДЕ термин РЕАЛЬНО упоминается в видимом
`roleDemo.text` буквально** (не везде, где технически применима та же
операция) — grep по всем `EXAMPLES[]` на точные строки (`avasāna`,
`бахуврихи`, `padânta`, `сандхи`, `варг`), не наугад: три копии suhārt
(`EXAMPLES[8][0]`/`[9][1]`/`[31][2]`) получили `['lopa','avasāna',
'bahuvrīhi']` на шаге lopa и `['cartva','avasāna','varga']` на шаге
cartva; bharan (`EXAMPLES[9][0]`) — `['lopa','saṃyogāntasya lopaḥ',
'avasāna','pada']`; budh-/bhotsyate (`EXAMPLES[31][1]`) —
`['cartva','varga']`; ещё 4 отдельных места, где в прозе упоминается
общее слово «сандхи» (правила 3/грам.-шаг, 16, 47, 57) получили
`glossaryTerm:'sandhi'` (или расширили уже существующий массив).

**ОТКРЫТЫЙ ПУНКТ, ЧЕСТНО НЕ ЗАКРЫТЫЙ ЦЕЛИКОМ:** это НЕ полный аудит всех
71 правил на предмет ЛЮБОГО Пāṇини-термина, который технически можно бы
упомянуть (например, `dhātu`/«корень» упоминается в комментарии к `iyaṅ`,
но не в видимом тексте ни одного `roleDemo.text` буквально — не добавлен
как отдельная запись; `varga` не проверен на упоминания за пределами
suhārt/budh- — например, в правиле 33 (wholevarga) или 32/33's
описаниях; сама секция «Внутренние сандхи»/«Внешние сандхи» на левой
панели правил тоже не тронута). Сделан РЕАЛЬНЫЙ, но ОГРАНИЧЕННЫЙ проход —
то, что явно всплыло при работе над rule9-bharan (данда/padânta) плюс
быстрый grep на явные кандидаты. Полный систематический аудит всех
71 правила на предмет любого Пāṇини-термина — отдельная, ещё не начатая
задача, если/когда до неё дойдёт очередь (тот же принцип, что и у
«Фазы 2» Часть 1 п.8 — большие аудиты явно называются отдельным заходом,
не молча растягиваются на середину другой задачи).

Новый тест-предохранитель (`tests/dataModule.test.mjs`, +3): каждая
неоткрытая (без `openTerm`) запись `GLOSSARY` имеет непустое
`devanagari`; `glossarySlug` даёт непустой, без пробелов слаг для
каждого ключа; каждый `glossaryTerm` (строка или массив) в ЛЮБОМ
`EXAMPLES[]`.roleDemo.step по всем 71 правилам резолвится в реально
существующую запись `GLOSSARY` (тот же класс защиты, что уже был у
`role-demo.js`'s тихого `console.warn`, но теперь автоматической
регрессией, не только ручной проверкой на глаз). 236/236 тестов,
lint/check-conventions/typecheck чисто.

- **Серебро/золото (`matsSignal`/`matsGold`, `op.signal:'silver'|'gold'|'blank'`)**
  — серебро ТОЛЬКО гунация, золото ТОЛЬКО вриддхи (решено пользователем
  напрямую, отдельные, не путать друг с другом). Не декоративное «что-то
  происходит» — конкретный сигнал «идёт огласовка (гуна/вриддхи)».
  `TRANSFORM_KIND.vrddhi` = `{spinTurns:2, signal:'gold'}` реализован.
  **Материал — металлический, не плоская заливка** (`buildMetallicMaterials`
  в `chalk-module.js`, вызывается напрямую из `matsSignal`/`matsGold` в
  `slot-engine-cube.js`): многостоповые градиенты по роли грани (верх/
  блик-полоса/тень, `METALLIC_ROLE_BY_FACE`) + ambient occlusion, палитра
  портирована из референса пользователя (готовый SVG/React-компонент
  изометрического куба, силуэт не переносился — только hex-стопы
  градиентов). Это ручная 2D-имитация, **не настоящее отражение** —
  единственная попытка честного `envMap` (`THREE.PMREMGenerator`,
  `metalness`) не довелась до убедительного результата без возможности
  видеть рендер вживую (доводка шла по скриншотам, раунд за раундом, и не
  сошлась). **Открытый пункт на будущее: вернуться к настоящему отражению
  отдельным заходом**, когда появится способ смотреть на живой рендер
  (интерактивная сессия/скрин-шеринг), не только по присланным
  скриншотам — само направление (envMap) технически рабочее, просто
  требует другого канала обратной связи, не блок на архитектуре.
- **`GROUP_COLOR`** — нейтральный, структурные события (вспышки merge/elide,
  рамка группы, сжатые многобуквенные блоки).
- **Категории `transform`** (угол поворота И цвет промежуточной фазы —
  ОДНА система, не два разных факта). **С заходом 54 — именованные
  пресеты в коде** (`export const TRANSFORM_KIND` в `slot-engine.js`),
  не только описание здесь: `{ type:'transform', ..., ...TRANSFORM_KIND.vargaPair }`
  вместо голых чисел вручную в каждом примере — снижает риск ошибки
  (заход 46: `spinTurns` был перепутан именно из-за ручного повторения).
  - Гунация → `spinTurns:1` (360°) + `signal:'silver'` (дефолт движка —
    в `TRANSFORM_KIND` НЕ включена, передавать нечего, agnayas как был
    без явных параметров, так и остался).
  - Вриддхи → `TRANSFORM_KIND.vrddhi` = `spinTurns:2` (720°) +
    `signal:'gold'` — реализовано в `TRANSFORM_KIND` и в движке
    (`matsGold`, `signalMats` в `applyTransform`).
  - **Идея на будущее, пока НЕ реализована:** то же различие
    серебро/золото — не только для сигнального цвета САМОЙ буквы при
    `transform`, но и для рамки-подчёркивания ГРУППЫ-причины
    (`GROUP_COLOR`/`updateGroupFrame`, сейчас один нейтральный цвет на
    все случаи) — серебряная рамка под группой, вызывающей гунацию,
    золотая — под группой, вызывающей вриддхи. Даёт зрителю дополнительный
    сигнал ДО того, как начнётся сам оборот буквы, какая огласовка
    предстоит. Реализовать вместе с первым реальным примером вриддхи, не
    заранее вслепую (тот же принцип, что и у `signal:'gold'` самого
    `transform`).
  - Парная замена внутри варги (k↔g, глухая↔звонкая, придых↔непридых,
    любая↔назальная) → `TRANSFORM_KIND.vargaPair` = `spinTurns:0.5`
    (**180°, противолежащая грань** — геометрически материализованная
    «пара») + `signal:'blank'`.
  - Общая ассимиляция ПОД СОСЕДА (буква вне всех варг — h/ṃ/ḥ — целиком
    меняет место образования) → `TRANSFORM_KIND.assimToNeighbor` =
    `spinTurns:1` (**360°, не 180°** — нет «противолежащей грани», буква
    не парная сама по себе) + `signal:'blank'`.
- **Тяжесть/скорость вращения `transform` — ОБРАТНО пропорциональна углу
  поворота, не общая формула spinTurns×MS_PER_360 везде одинаково.**
  Прямое решение пользователя (заход 82): чем МЕНЬШЕ угол поворота, тем
  ЗАМЕТНЕЕ должно быть медленным и тяжёлым вращение — небольшой поворот
  сам по себе даёт мало визуального сигнала, нужна медленная, весомая
  подача, чтобы факт превращения осознавался (обучающее приложение, не
  просто красивая анимация). Уже реализовано для `vargaPair` (180°) —
  1800мс, НЕ `0.5×MS_PER_360` (700мс) — портировано из эталона
  `docs/effects/rule-assimilation-varga-t-d.html`, см.
  `landsOnOppositeFace` в `applyTransform` (`slot-engine-ops.js`).
  **РЕШЕНО (заход 98). `MS_PER_360` = 2000 (было 1400)** — прямое
  замечание пользователя после живой проверки agnayas: гунация читалась
  слишком быстро, «нельзя быстро понять, что происходит». Одна общая
  константа на гунацию И вриддхи (не отдельная скорость для 720°) —
  вриддхи автоматически получает ту же угловую скорость, вдвое дольше по
  времени (2×2000=4000мс), тем же принципом, каким прямо попросил
  пользователь. Все ПОСЛЕДУЮЩИЕ (после transform.start) числа в
  `rule3-agnayas-slots.js` сдвинуты на +600мс (разница длительности одного
  оборота) — иначе следующий шаг стартовал бы всего через 100мс после
  реального приземления вращения (было 700мс запаса) — тот же ритм,
  просто позже. **taddhiraṇyam/vāk asti — НЕ пересчитаны** (их transform-
  категории — vargaPair/assimToNeighbor с ОТДЕЛЬНЫМИ фиксированными
  длительностями, не завязанными на MS_PER_360 напрямую тем же образом,
  кроме assimToNeighbor — h→dh в taddhiraṇyam использует spinTurns:1,
  формулу-таки заденет; открытый пункт — сверить при следующей визуальной
  проверке этого примера).
- **Один кубик = одна фонема** (принцип с самого начала проекта, IAST
  сегментален по построению). Многобуквенный `tr` — ТОЛЬКО для сжатых
  крайних блоков инертного контекста (см. реестр), различать от
  однофонемных многобуквенных записей (придыхательные, дифтонги ai/au) по
  точному списку `MULTI_CHAR_PHONEMES`, не по длине строки.
  **ЕДИНСТВЕННОЕ СОЗНАТЕЛЬНОЕ ИСКЛЮЧЕНИЕ (rule31/suhārt, тестовый
  полигон) — маркер паузы `avasāna`, `tr:'।'` (U+0964 DEVANAGARI DANDA),
  НЕ фонема.** Нужен как физический источник `influence` для позиционно
  мотивированного `transform` (событие правила 31/cartva триггерится не
  соседней буквой, а концом слова перед паузой — показать причину нечем
  без реального объекта в сцене). Danda — не изобретённый символ: Pāṇini
  определяет `avasāna` («пауза») как границу высказывания (1.4.110
  avasānaṃ viramaḥ), а danda традиционно ставится письменно ИМЕННО на
  этой границе — связь взята из самой традиции, не придумана для этого
  примера. Рендерится без единой новой строчки в движке — `colorFor`
  штатно даёт нестандартному глифу нейтральный `COL_DIM` (тот же путь,
  что уже проверен на аваграхе, rule50, «'», W175a). Присутствует С
  САМОГО НАЧАЛА примера (падает вместе со всеми буквами) — пауза не
  «наступает», она есть всегда как граница. Использовать этот приём для
  ДРУГИХ позиционных (не звуковых) триггеров — по аналогии, не
  автоматически: каждый новый случай проверять, есть ли действительно
  подходящий, не изобретённый традиционный символ для нужного понятия.
- **Один цвет для всего не-главного.** Цвет чипа шага определяется ТОЛЬКО
  тем, главный ли шаг (`primary:true`), не его `kind`. Всё не главное
  (грамматика И вспомогательное правило в цепочке) — один и тот же
  стандартизированный охровый (`is-grammar`, `#CDA84E`), не новый цвет.
- **Сжатые крайние блоки — лимит ЖЁСТКИЙ, ровно 3 буквы.** Единственное
  согласованное исключение — 4 буквы, только для одного случая (правило
  62, «yati»), не прецедент для произвольного превышения.
- **Направление бокового раскачивания `transform`** (`op.clearance`,
  дефолт +0.35, вправо) — проверять для КАЖДОГО нового `transform`, кто
  сосед с какой стороны; раскачивание должно идти к пустому месту, не к
  занятому. Отрицательное значение — то же самое поле, зеркально.

### Зарезервированные факты по центрированию раскладки и FOV камеры

Полная история находки (заходы 89–90) — CHANGELOG.md.

**Центрирование — одна формула на все примеры, не подбор на глаз.**
`centeredStart(span)` (`slot-engine-core.js`) = `Math.floor((N_SLOTS -
span) / 2)` — единственный источник этой арифметики; `centerSlots`
(одно слово) и `layoutWords` в `slot-engine-generate.js` (несколько слов
+ зазоры) оба вызывают её, не дублируют формулу.

**Масштаб — адаптивный FOV, НЕ единая уменьшенная константа.** Эталон —
максимально заполненный случай (taddhiraṇyam), не собственная ширина
каждого примера — иначе короткие примеры визуально крупнее длинных до
сужения окна.
- `HALF_WORLD_H` (`slot-engine-core.js`, =2.3) — общая константа для всех
  примеров без исключения.
- `HALF_WORLD_W_MAX` = `halfWorldW(0, N_SLOTS-1)` — ТОЖЕ общая константа,
  не своя под каждый пример (половина мирового габарита самого широкого
  теоретически возможного случая, сейчас = taddhiraṇyam, весь стенд 0..9).
  `mountSlotExample` передаёт в `computeFitFov` всегда ЭТУ константу,
  независимо от того, какой пример показан — иначе короткие примеры
  оставались бы крупнее длинных до тех пор, пока узость окна не заставляла
  именно их ужиматься.
- `computeFitFov(aspect, camZ, hw, baseFov)` — `camera.fov = max(baseFov,
  fovForHeight, fovForWidth)`, пересчитывается в `resize()`
  (`slot-engine-mount.js`) на каждый `ResizeObserver`.
- Проверено `tests/cameraFit.test.mjs` (12 тестов) — FOV идентичен у всех
  примеров на любом aspect ratio, визуальная разница не зависит от того,
  какой пример сейчас на экране.

### Зарезервированные факты по живой проверке rule42/agnayas

Полная история трёх итераций поиска этих формул — CHANGELOG.md, заход
98. Здесь — только то, что ДЕЙСТВУЕТ СЕЙЧАС и нужно знать для любого
будущего `transform`/`elide`/падения:

- **Момент смены буквы при вращении — ПОЗИЦИОННАЯ схема, не доля
  времени и не фиксированный угол.** `disappearDeg=180` (всегда, «зад»
  первого оборота), `revealDeg=(spinTurns−1)×360+180` (совпадает с
  disappearDeg при одном обороте — гунация меняется без разрыва; даёт
  540° при двух — вриддхи ждёт целый лишний оборот в пустой грани).
  Два независимых порога (`op._disappeared`/`op._revealed`) в
  `applyTransform`.
- **Буква на кубике-`transform` — ТОЛЬКО на фасаде (`idx===4`), НИКОГДА
  на противолежащей грани (`idx===5`)** в `buildMetallicMaterials` —
  иначе задняя грань показывает старую/новую букву ВТОРОЙ раз в своих
  собственных окнах поворота. Исключение — `landsOnOppositeFace`
  (`vargaPair`, 180°): там противолежащая грань закономерно возвращается
  к зрителю и ДОЛЖНА нести букву — это отдельный, не задетый путь.
- **Золотая россыпь искр** — автоматически при `op.signal==='gold'`
  внутри активной фазы вращения, ничего доп. в данных примера не нужно.
  Только золото (вриддхи) — гунация без искр, намеренно.
- **`FALL_START_Y=5.0`** (не 3.6, не 6) — калибровано под РЕАЛЬНУЮ
  панель приложения (aspect~1.5–1.8, не только широкий тестовый
  полигон), проверено проекционной математикой камеры на диапазоне
  900×440..1150×620. Общая константа, затрагивает ВСЕ интегрированные
  примеры разом.
- **`MS_PER_360=2000`** (не 1400) — одна константа на гунацию и
  вриддхи.
- **Отстойник `elide`: `holdOffset={x:-0.45, y:-2.2, z:-0.4}`.** y
  считается как `FLOOR_Y(-0.55) − CUBE_SIZE(зазор) − CUBE_SIZE/2
  (половина кубика) = -2.2` — ЦЕНТР кубика в отстойнике, не его верхний
  край (легко перепутать, уже было перепутано однажды).
- **Центрировать раскладку по ИТОГОВОМУ слову, не по временной
  раскладке до событий** (rule42: сдвинута на +1 слот, чтобы
  центрировался финальный 7-буквенный результат «tatraiti») — финальное
  состояние держит внимание зрителя дольше всего, на нём и стоит
  фокусировать центр. Тот же принцип применён во всех примерах фазы
  автопостройки (Часть 5.2).

### Зарезервированные факты по эффекту elide (полная история — CHANGELOG.md)

**Текущее поведение `applyElide`:** прозрачность полная на всём
погружении+паузе, угасание сосредоточено в финальной фазе (`fade`) —
резкое, не размазанное; масштаб синхронно сжимается до `0.12` на той же
ускоренной кривой (`visT=t×1.8`), что и угасание — кубик реально
«рассыпается», не тает и не стреляет искрами из отдельной точки.
`spawnSparkleBurst` (искры) — count **200** по умолчанию (elide не
передаёт явно), у КАЖДОЙ искры своя случайная стартовая точка по всей
площади кубика (не общая `frontAnchor`) и свой случайный размер/свечение;
цвет — `ringColorFrom(colorFor(cube.tr))`, СОБСТВЕННЫЙ цвет буквы, не
нейтральный `GROUP_RGB`. Финального кольца-вспышки ПОСЛЕ угасания нет
(убрано — избыточный повтор); вспышка-удар в НАЧАЛЕ реакции (`op.start`)
остаётся — отдельный обязательный сигнал (Часть 2). Для вриддхи
(`applyTransform`, тем же `spawnSparkleBurst`) — явный `count:15` (НЕ
общий дефолт 200 — многоточечный разброс уместен для elide, не для
плавного сопровождения оборота).

**`ElideOp.quiet` — критерий выбора уже закреплён как строка реестра
(Часть 4 ниже, не повторяется здесь).** Проверенный найденный баг (уже
исправлен): `applyStepDim` раньше каждый кадр перебивал прозрачность
elide для любого слота из `activeSlots` шага — угасание было НЕВИДИМО
во всех уже построенных на тот момент примерах (śādhi/rule42/rule55).

**Открытый пункт, НЕ решён:** rule42 («tatraiti») — по скриншоту
пользователя «неровно встало»; полный таймлайн проверен численно (все
позиции/повороты/масштабы корректны), причина не в геометрии данных —
вероятно, в отрисовке самого глифа «ai» (авто-подгонка шрифта для
2-буквенных диграфов) или в чём-то, что численная проверка структуры не
покрывает. Ждёт более точного описания того, что именно выглядит неровно.

---

## Часть 4 — Реестр эталонных эффектов (справочник, не источник для
копирования кода вручную)

**Правило пользования:** прежде чем писать `ops` для нового примера —
сверить каждую ситуацию с этой таблицей. Ситуация уже есть — берётся
готовая техника. Ситуации нет — после того как решена для нового примера,
таблица дополняется В ТОТ ЖЕ МОМЕНТ. Эта таблица покрывает только 5 уже
ПОСТРОЕННЫХ правил — для предварительного плана механики любого из
остальных 66 правил сначала смотреть
`docs/reference/interaction-registry.md` (заход 79), только потом сюда
за деталями реализации, если тип ситуации уже встречался.

| Ситуация | Обязательная техника | Где реализовано |
|---|---|---|
| Триггер непрерывно воздействует на цель | Текстурный пульс на триггере (`setFacePulse`/`redrawPulseFace`), НЕ отдельный DOM-слой | `approach.holdPulse` (śādhi), `influence` (agnayas, унифицировано заход 43) |
| Конкретный сигнал долетает от триггера к цели (дискретный пакет) | `spawnWave` | `influence`, `approach.holdPulse` — уже единообразно |
| Буква превращается в другую на месте | `transform`, 4 категории — см. Часть 3 | agnayas, rule 71, taddhiraṇyam |
| Сближение вызывает реакцию у цели | Двухотрезочный `approach` с паузой | śādhi |
| Момент начала реакции | Вспышка+скачок масштаба РОВНО в момент начала, не только в конце | `elide` |
| Буква исчезает без замены, И ЭТО СОДЕРЖАНИЕ ПРАВИЛА (никакого другого главного события в шаге нет) | `elide`, погружение вниз, полная непрозрачность на rise+hold, угасание+россыпь искр СОБСТВЕННОГО цвета буквы (`ringColorFrom(colorFor(tr))`) сосредоточены в fade — драматично, это и есть то, что мы показываем | śādhi (s→∅), rule 55 (s→∅) |
| Буква исчезает без замены, но ПОБОЧНО — спутник другого, уже показанного главного события в ТОМ ЖЕ шаге (nimitta, поглощённая результатом чужой трансформации) | `elide` с `quiet:true` — тот же погружение вниз, но БЕЗ россыпи искр и БЕЗ ускоренного сжатия (линейное угасание на весь `fadeDur`); вспышка-удар в НАЧАЛЕ реакции остаётся (отдельный обязательный сигнал, не декоративный эффект) — яркий эффект здесь конкурировал бы вниманием с уже отыгранной кульминацией | rule 42 (a — nimitta вриддхи, главное событие — золотое вращение e→ai) |
| Одна буква становится двумя | `split`, подъём вверх | agnayas (e→a+y) |
| Одна буква ПОЛНОСТЬЮ заменяется ОДНОЙ категориально другой (не сдвиг по своей же оси, как гуна/вриддхи/парная замена — вращение здесь НЕ годится) | `split` с ОДНИМ `arrivals`-элементом, приземляющимся в ТОТ ЖЕ исходный слот — движок уже поддерживает произвольную длину массива, нового кода не требуется | rule 6 (ṛ→r, pitṛ+os→pitros) |
| Две буквы САМОПОДОБНЫ и сливаются в одну (a+a→ā) | `merge`, едущий кубик вдоль ряда | āsīt (a+a→ā) |
| Два РАЗНЫХ звука сливаются в один НОВЫЙ (не самоподобие) | ДВА параллельных `split` (каждый источник поднимается сам, синхронизированы гаснуть одновременно) — `arrivals` только у ОДНОГО из них (пустой массив у второго — законно, не ошибка) | rule 50 (-as→o, devas+asti→devo'sti) |
| Буква не исчезает, а становится ДРУГИМ ВИДИМЫМ символом (не звуком с фонетическим цветом) | `transform`, `signal:'blank'` — `colorFor` для нестандартного глифа падает в нейтральный `COL_DIM` без нового кода | rule 50 (a→аваграха, `toGlyph:"'"`) |
| Морфологическая граница внутри примера | Части разделены с начала, событие — следствие сближения | āsīt, śādhi |
| Внешние сандхи, граница слов не схлопывается | Зазор остаётся навсегда, ничего его не трогает | vāk asti, taddhiraṇyam |
| Длинный пример (>10 фонем) | Сжатые крайние блоки, авто-подгонка шрифта, лимит 3 (искл. 4 для правила 62) | taddhiraṇyam («yam») |
| Цвет вспомогательного/цепочного rule-шага | Стандартный охровый `is-grammar`, не новый цвет | taddhiraṇyam (шаг 2, без primary) |
| Зазор между двумя `rule`-шагами ЦЕПОЧКИ с ОДИНАКОВЫМ activeSlots | Проявление НЕ вставляется — `buildRuntimeSteps` сравнивает activeSlots соседних шагов (`sameActiveSlots`), при совпадении зазор пропускается без волны. Заход 52, реальный найденный баг: короткий зазор (250мс) + широкая раскладка (9 кубиков) → волна проявления (REVEAL_STAGGER×9 > зазора) не успевала докатиться, обрывалась на середине и откатывалась — левый и правый край ряда оказывались на разной стадии прерванной волны (до 0.128 разницы прозрачности у ОДНОЙ и той же буквы в двух позициях) | taddhiraṇyam (заход 52) — проверено численно, расхождение в проблемном окне сведено к 0.000000; agnayas (контроль, activeSlots РАЗНЫЕ между шагами) — проявление сохранилось как было, фикс не задел |
| Направление бокового раскачивания transform | К пустому месту, не к занятому — проверять заранее | agnayas, taddhiraṇyam (оба шага, разные знаки) |
| Чисто позиционный шаг (звук не меняется) | БЕЗ притенения — движение само рассказывает историю | āsīt (шаг 1), śādhi (пауза приближения) |
| Шаг реально меняет звук | Притенение уместно, activeSlots только для участников | agnayas, āsīt, śādhi, rule 71, rule 70 (шаг 2) |
| Одноразовый флаг блокирует продолжающийся хвост анимации (класс бага) | Финализация — под своим guard'ом; хвост/спад — БЕЗ guard'а по тому же флагу | Исправлено трижды: `applyTransform`, `applyMerge` (дважды), `applyElide` |
| Буква исчезает без замены НА ВНЕШНЕЙ границе слов (граница никогда не схлопывается — в отличие от śādhi, approach с физическим сближением не нужен для самой сандхи) | `influence`+`elide` НА МЕСТЕ, без approach — комбинация уже проверенных техник (`influence` через постоянный внешний зазор, как у vāk asti/taddhiraṇyam, но реакция — `elide`, а не `transform`) | rule 55 (devās āsate, s исчезает перед звонким ā) |
| Исчезнувшая буква стояла ВПЛОТНУЮ к внешней границе — зазор после elide на кадр становится ШИРЕ стандартного (2 пустых слота вместо 1) | Финальный `approach` (`retreat:false`) возвращает соседнее слово на стандартную ширину внешней границы (1 слот) — это не отмена принципа «граница не схлопывается», а его восстановление после побочного расширения; шаг остаётся активным (притенение держится) до конца этого approach, развидка/settle — уже после | rule 55 (āsate целиком подъезжает к devā после исчезновения s) |
| Попытка действия (обычно `elide`) БЛОКИРУЕТСЯ другим правилом-исключением — нужно показать саму попытку и её провал, не только текст об этом | `resist` — тем же движением, что elide, кубик начинает уходить, не долетает, коротко задерживается на пике и пружинисто отскакивает обратно (`easeOutBack`); `groupSlots` рисует нейтральную рамку под ВСЕЙ защищающей группой (переиспользует `updateGroupFrame`) — показывает, что защищена группа целиком, не одна буква. Без собственного `label` — исключение (в отличие от сандхи-правила) обычно не имеет устоявшегося санскритского термина, придумывать не по чему | rule 8 (suhārt: d пытается отсечься следом за суффиксальным -s правила 9, но не может — корневой согласный после r, всё hārda- подчёркнуто как неделимое целое) |

---

## Часть 5 — Состояние примеров (ДВЕ РАЗНЫЕ системы, не путать)

**Важно: в проекте есть ДВЕ независимые системы иллюстрации правил** — эта
часть документа раньше смешивала их в одну таблицу, что давало ложное
впечатление о масштабе готового.

### 5.1 — 2D-подсветка алфавита (`roleDemo`) — ОСНОВНОЙ охват, готов почти весь

**64 из 71 правила** имеют хотя бы один кликабельный пример с подсветкой
источник/триггер/результат прямо на алфавитной панели — НЕ через 3D-кубики,
через `roleDemo` в `EXAMPLES[n]`. Технический аудит (полный клик-свип по
всем правилам и чипам) дал 0 проблем.

**Подробный, точный статус — в отдельном файле `second-examples-todo.md`,
это НЕ дублируется здесь** (список длинный — 41 правило с точным числом
некликабельных «вторых примеров» по каждому). Держать этот файл среди
project knowledge ОБЯЗАТЕЛЬНО — без него список придётся пересчитывать
скриптом заново (алгоритм пересчёта записан там же).

Коротко, что в нём: 41 из 64 правил имеют ЕЩЁ ХОТЯ БЫ один пример в тексте
цитаты Эменó, который остаётся обычным некликабельным текстом (не ошибка —
сознательно отложено). Отдельная находка того же аудита — правила 19, 24,
43, 45 используют тот же переход i→e/i→ya, что уже подсвечивает таблицу
гуна/вриддхи у правил 2 и 6 (`gunaCells`), но сами этой связи не имеют —
не добавлено по инерции, стоит добавить при следующем заходе по каждому.

**Открытые методологические вопросы — ✅ ВСЕ ТРИ РЕШЕНЫ (заход 57, прямое
решение пользователя, реализовано в коде).** Полный текст решений и
реализации — в Части 3, разделе «Профессионализация», не дублируется
здесь второй раз.

### 5.2 — 3D слот-движок — статус по правилам

18 правил (считая по номерам, задействованным хотя бы в одном примере —
правила 8, 9 и 31 делят ОДИН физический 3D-модуль, но у каждого своя
точка входа, см. запись 8/9/31 ниже) интегрированы в основное приложение.
Полная история построения каждого — CHANGELOG.md, раздел «Фаза
автоматической постройки правил по номерам» (плюс более ранние заходы
для правил 3/7/15/42/50/55/61/70/71). Отсутствие пометки «визуально
подтверждено» в таблице означает, что живая проверка пользователем ещё
не проведена — численная проверка (`applyX` на полном таймлайне,
структурная проверка `EXAMPLES[]`/`GLOSSARY`) не заменяет её (Часть 1,
п.6).

**Зарезервированное методологическое ограничение движка (найдено
численной симуляцией при планировании rule2, не гипотетически):
`transform` и `approach` НЕ компонуются на ОДНОМ кубике одновременно.**
Во время активной фазы вращения `applyTransform` каждый кадр
принудительно пишет `position.x` вокруг СВОЕГО слота — если на этом же
кубике уже отработал `approach` и сдвинул его физически, `transform`
следующим кадром перетирает позицию обратно. **Решение:** если гунация/
вриддхи происходит НА кубике, который в этом же шаге ещё и физически
сближается с соседом — сначала `transform` целиком, на месте, БЕЗ
approach; approach запускается СТРОГО после `t>=1` (`op._landed` —
после этого момента `applyTransform` больше не трогает `position.x`).
Не применимо, когда approach управляет ПОЗИЦИЕЙ одного кубика, а реакция
(elide/transform) происходит на ДРУГОМ (как в śādhi, где DH подъезжает,
а реагирует S) — конфликт только когда ОБЕ операции пишут в
`position.x` ОДНОГО И ТОГО ЖЕ кубика.

**Правило 61 (hasannagacchat) — первый реальный пример гэпа DOUBLE
(истинная геминация).** Новая операция движка `bud` («отпочкование», см.
Часть 3, «Типы операций») — источник остаётся на месте, клон появляется
рядом и едет в свой слот. Два найденных и исправленных бага при
постройке, оба общие (покрывают любой будущий пример с `bud`, не только
rule61): (1) `easeOutBack` — не настоящая 0→1 функция (спроектирована
под обратную форму `approach`-отскока), прямой `lerp` с ней телепортирует
кубик — добавлена отдельная `easeOutBackProgress`; (2) автогенерация
`settle.slots` в `mountSlotExample` не учитывала клоны `bud` — клон
навсегда оставался в сигнальном фонетическом цвете, не уходил в
`READY_COLOR` на финальной волне `settle`.

| Правило | Слово | Статус |
|---|---|---|
| 1 (devau) | deva- + au → devau | Интегрировано в `EXAMPLES[1]`, **визуально подтверждено** («ДЕВАУ — нормально, можно интегрировать»). Первый пример фазы автопостройки по номерам. Механика (после 4 пересмотров): a и au физически сближаются на общий зазор и сливаются (`merge`, ekādeśa), вриддхи (`TRANSFORM_KIND.vrddhi`) стартует сразу по завершении слияния. Находки этого и следующего (rule2) правил дали ряд новых полей движка (см. `slot-engine-types.js` — источник правды по сигнатурам): `TransformOp.atX`/`startBlank`/`labelX`, `ApproachOp.fromX`/`blankAtProgress`, `InfluenceOp.ringRgb`, `TransformOp.signalHoldDur` (250мс, пауза сигнального цвета после посадки). |
| 2 (bhavet) | bhava- + -ī- + -t → bhavet | Интегрировано в `EXAMPLES[2]`, **визуально подтверждено** («оба примера нормально, можно интегрировать») после восьми раундов живой правки. Механика перенесена с rule1 (merge a+ī → guṇa). Сутра исправлена (§338a → верная §565a, Whitney). |
| 3 (agnayas) | agni+as → agnayas | Интегрировано в `EXAMPLES[3]`, текст переписан под академический регистр. Образец применения методологии — см. Приложение в конце документа. |
| 4 (striyā) | strī- + ā → striyā | Интегрировано в `EXAMPLES[4]`, **визуально подтверждено** («отлично, без замечаний»). `split` (не merge) — геометрия скопирована с agnayas. Термин пилюли `iyaṅ` (Pāṇini 6.4.77). **Открыто:** точная сутра именно для strī- (не только для dhātu/bhrū общего случая) не подтверждена. |
| 5 (purā) | pṛ- + ā → purā | Интегрировано в `EXAMPLES[5]` (создан с нуля), **визуально подтверждено** («все хорошо»). `split`, структурно как rule4 (r вместо y/v). Источник (Whitney §242) честно процитирован с оговоркой: правило Эменó — рабочее упрощение для «хорошо ведущих себя» корней на губной (pṛ/mṛ/vṛ), не универсальный закон на весь список ṛ-корней Уитни. **Открыто:** термин пилюли временный («огласовка ṛ», кириллица) — санскритский термин для ṛ→ir/ur не найден; сутра Панини для этого случая тоже не найдена. |
| 6 (pitros) | pitṛ- + os → pitros | Интегрировано в `EXAMPLES[6]`, **визуально подтверждено** («работает, ок») после переклассификации `transform`→`split` (ṛ→r ПОЛНОСТЬЮ, без остатка гласного — качественно другая замена, не сдвиг по своей же оси, как гуна/вриддхи/парная замена внутри варги). Термин пилюли `yaṇ` (Pāṇini 6.1.77 iko yaṇ aci). |
| 7 (āsīt) | a+as+īt → āsīt | Интегрировано в `EXAMPLES[7]`, текст переписан. |
| 8, 9 и 31 (suhārt) | su- + hārd- + s → suhārt | Интегрировано в `EXAMPLES[8]`/`EXAMPLES[9]`/второй элемент `EXAMPLES[31]` (три тонкие обёртки над общей `suhart-slots.js` — дедупликация монтирования в `rule-panel.js` требует разных путей импорта). **Интегрировано БЕЗ живой визуальной проверки перед сдачей** (единственное исключение среди всех 18 правил) — ждёт подтверждения пользователя. Три правила показаны как отдельные шаги одной анимации: правило 9 (итеративный `lopa`, elide суффиксального -s), правило 8 (исключение — новая операция `resist`: кубик тем же движением, что elide, начинает уходить, но пружинисто отскакивает обратно; рамка вокруг всего корня h-ā-r-d), правило 31 (cartva, `TRANSFORM_KIND.vargaPair`, d→t). Маркер паузы **avasāna** — данда «।» (U+0964), сознательное и честно оговорённое исключение из принципа «один кубик = одна фонема» (см. Часть 3), физический источник причины для позиционных, не звуковых, триггеров. r отдельно отмечен как условие правила 8 (`influence`, и в 2D через `type:'compound'` без `result`). Подсветка родственных правил в левой панели по клику на чип (`ex.relatedRules`). |
| 9 (bharan), родной пример | bharant- + s → bharan «несущий» | Интегрировано в `EXAMPLES[9][0]`, **визуально подтверждено** («правило 9 БХАРАН можно интегрировать»). Тот же lopa, что у suhārt, но обе итерации (-s, затем -t) без исключения — самый чистый пример правила 9. Тот же маркер avasāna. Раскладка центрирована по НАЧАЛЬНОЙ (не итоговой) 8-тайловой раскладке — она видна дольше, чем итоговый кадр. |
| 10 (gacchati) | gach- + ati → gacchati | Интегрировано в `EXAMPLES[10]`. Геминация ch→cch с деаспирацией первой копии показана ДВУМЯ отдельными шагами с настоящей паузой между ними (`bud` с одинаковым `toGlyph` → пауза (автоматический `_reveal`-сегмент `buildRuntimeSteps`, без нового кода) → `TRANSFORM_KIND.vargaPair` левой копии, «придых↔непридых» уже входило в описание этой категории — недостающий механизм из более раннего гэпа DOUBLE, «перемещение придыхания», на деле не потребовался). **Открыто:** точный номер сутры Панини именно для геминации ch после гласного не найден (соседние по номеру сутры — про другие явления); термин пилюли «деаспирация» — временный, санскритский эквивалент не найден. |
| 15 (śādhi) | śās+dhi → śādhi | Интегрировано в `EXAMPLES[15]`. Раскладка центрирована формулой (`centeredStart`, слоты 2..7). **Требует повторной визуальной проверки** — момент исчезновения S переработан ПОСЛЕ первого подтверждения (см. Часть 3, находка «applyStepDim перебивал прозрачность elide») — S теперь держит полную непрозрачность на погружении и угасает резко в конце с россыпью искр, раньше угасала незаметно рано. |
| 42 (tatraiti) | tatra+eti → tatraiti | Интегрировано в `EXAMPLES[42]`. Первый настоящий пример вриддхи в проекте. e→ai (`TRANSFORM_KIND.vrddhi`), a исчезает (`elide`, `quiet:true`) как часть того же события. **Визуально подтверждено** (несколько раундов — темп вращения, позиционная схема смены буквы, золотые искры, высота падения, см. находки в Часть 3). Подпись исправлена (`lopa`→`ekādeśa` — a и e равнозначные участники eka-ādeśa, не «условие, которое исчезает»). **Открыто:** точный номер сутры для ИМЕННО пары a+e не переподтверждён (файл ссылается на 6.1.104, найден другой близкий текст 6.1.109 про другой порядок звуков); отдельно — пользователь отметила «неровно встало» на скриншоте уже собранного слова, численно НИКАКОГО отклонения позиции/поворота/масштаба не найдено, причина не установлена (см. Часть 3, находки по elide). |
| 50 (devo'sti) | devas + asti → devo'sti | Интегрировано в `EXAMPLES[50]`. Первый реальный тест гэпа «слияние ДВУХ РАЗНЫХ звуков» (Часть 6, закрыт) — ДВА параллельных `split` вместо `merge` (тот остаётся проверен только на самоподобии). Термины пилюль («слияние», «ассимиляция» у rule70) — временные, кириллица, санскритский эквивалент не найден. |
| 55 (devās āsate) | devās (готовое) + āsate → devā āsate | Интегрировано в `EXAMPLES[55]`, **визуально подтверждено** («работает»). Внешняя граница слов, `influence`+`elide` на месте (без approach) для самого правила, затем один `approach` возвращает стандартную ширину границы. |
| 61 (hasannagacchat) | hasan + agacchat → hasannagacchat | Интегрировано в `EXAMPLES[61]`. Первый реальный тест гэпа DOUBLE — подробности см. отдельную запись выше этой таблицы. |
| 70 (taddhiraṇyam) | tat+hiraṇyam → taddhiraṇyam | Интегрировано в `EXAMPLES[70]` — `module` добавлен к уже существующей 2D `roleDemo`. Оба ранее отложенных визуальных замечания закрыты ещё на полигоне до интеграции. |
| 71 (vāk asti) | vāk+asti → vāgasti | Интегрировано в `EXAMPLES[71]` — второй элемент массива переключён с замороженного рукописного модуля на слот-движковую версию. |

---

## Часть 6 — Что ещё не отработано (гэпы) и планы на будущее

0. **ЗАКРЫТО. Асимметричная прозрачность притенённых кубиков** (полная
   история расследования, включая честно отвергнутые гипотезы — заходы
   63–65, 80, 85–86 — CHANGELOG.md). **Настоящая причина:** материалы
   кубика (`buildChalkMaterials`) не задавали `side` вообще — дефолт
   THREE.js (`FrontSide`) не рисует внутреннюю сторону грани, у
   притенённого (`opacity<1`) кубика физически нечему было просвечивать.
   Фикс — `side: THREE.DoubleSide`, общий для любого кубика. **Урок для
   будущих расследований такого класса: сначала проверять простые,
   легко забываемые дефолты самого объекта, только потом сложные внешние
   гипотезы (сортировка/освещение/соседняя геометрия).**
   Попутно найдены и исправлены две побочные, но реальные находки того
   же расследования: (0а) грань пульса (`buildPulseFace`) создавалась
   без `transparent:true` — THREE.js молча игнорирует `opacity` без этого
   флага; (0б) буква при `transform` красилась только на одной торцевой
   грани (idx4) — верно для гуны/вриддхи (целый оборот, исходная грань
   возвращается), но НЕ для `vargaPair` (180°, к зрителю возвращается
   ПРОТИВОЛЕЖАЩАЯ idx5) — кубик визуально «пропадал» на taddhiraṇyam/
   rule71. Исправлено — обе торцевые грани красятся всегда. **Визуально
   подтверждено.**
1. **ЗАКРЫТО. Слияние двух РАЗНЫХ звуков** (не самоподобие) — rule 50
   (devas+asti→devo'sti), см. Часть 5.2. Решено НЕ через `merge` (тот
   остаётся проверен только на самоподобии a+a→ā), а через ДВА
   параллельных `split`, синхронизированных гаснуть одновременно —
   результат прилетает только у одного (`arrivals` у второго осознанно
   пуст, найден и исправлен баг валидатора, который это запрещал).
2. **ЗАКРЫТО. Удвоение (гемина́ция)** — rule 61 (hasan+agacchat→
   hasannagacchat, n→nn) и rule 10 (gacchati, ch→cch с деаспирацией),
   см. Часть 5.2. Новая операция `bud` (зеркало `merge`), деаспирация
   покрывается уже существующим `TRANSFORM_KIND.vargaPair`, новой
   механики не потребовалось. **Открыто:** правило 49 (tava cchāyā —
   та же механика, что rule10, ещё не построена как отдельный 3D-пример),
   65 (чистая механика `bud` без деаспирации), `INSERT` (эпентеза,
   ни одно из 71 правил не требует). `DISTANT` — см. п.4а, отдельной
   операции не требует.
3. **Общий механизм ассимиляции «взять букву у соседа автоматически»** —
   открыто. Сейчас решается только жёстким `toGlyph` в данных примера.
4. **Групповая цель** — открыто, но ТОЛЬКО для абстрактной 2D-иллюстрации
   правила на всём алфавите разом; для конкретного 3D-примера одна цель
   уже достаточна (реальное слово задействует ровно одну согласную на
   правило). Не блокирует ни одно из 71 правил.
4а. **DISTANT (дальние правила вроде RUKI)** — переосмыслено:
    `influence`/`spawnWave` не требуют соседства слотов по конструкции,
    новая операция не нужна — нужно только ПРОВЕРИТЬ, что волна визуально
    корректно тянется через несколько слотов (не тестировалось). Кандидат
    — правило 34 (закон Бартоломэ).
5. **Составные эффекты** (elide+double и т.п.) — открыто.
6. **ЗАКРЫТО. Вриддхи на слот-движке.** `signal:'gold'`/`matsGold`/
   `TRANSFORM_KIND.vrddhi` реализованы и покрыты тестами. Первый пример
   — правило 42 (tatraiti), выбрано по номеру правила (не stu-+-ti→
   stauti из старого прототипа — тот не привязан ни к одному из 71
   правил, остаётся нетронутым эталоном техники).
7. **`arrive`** — код готов, ни разу не применён по-настоящему.
7a. **Упражнения (26 штук) — НЕ НАЧАТО ВООБЩЕ, крупный отдельный пласт.**
    Полный текст есть в PDF Эменó (стр. 31–60), но база «упражнение →
    правило → разбор» не собрана, режимы «сделать самому / посмотреть
    ответ» не спроектированы. Отдельный, более тяжёлый вопрос объёма.
8. **ЧАСТИЧНО ЗАКРЫТО.** Правило 61 построено (сжатые блоки на краях).
   Остаются 64/65 (та же механика `bud`). Правило 62 не влезает даже со
   сжатием — нужно ручное исключение (4 буквы «yati»).
9. **ЗАКРЫТО. Интеграция готовых примеров в `EXAMPLES[]`** и
   **визуальная проверка taddhiraṇyam** — все построенные правила
   подключены к приложению, см. Часть 5.2.
10. **ЗАКРЫТО. `docs/index.html` — единый индекс проекта.** Переделана из
    описания «в разработке» в реальный статус проекта; секция
    «Слот-движок · в работе» заводится по мере надобности и убирается,
    когда пуста (принцип минимализма); панель `#dev-links` внутри
    приложения убрана, её роль — секция «Инструменты разработки» той же
    карточной вёрсткой, что и весь остальной сайт. **Открытый пункт,
    решённый сознательно:** деванагари для целых словоформ на карточках
    (agnayas, taddhiraṇyam и т.п.) не добавлена — нет проверенного
    источника для транслитерации целых форм после сандхи, риск ошибки
    выше пользы.
11. **Дизайн `docs/index.html` — палитра «Рукописная бумага · индиго»**
    (по прямому запросу, отобрана из 4 вариантов через `Artifact`).
    Токены: бумага `#F3EEE2`, чернила `#221F1A`, индиго-акцент `#2B3A6B`,
    куркума `#9A6B2E`. Шрифты: Fraunces (заголовки), Source Serif 4
    (тело), Tiro Devanagari Sanskrit (деванагари), Work Sans (лейблы).
    Базовые размеры (после правки по обратной связи): тело 20px, `.lede`
    21px (без узкого `max-width`-столбца), `.card .d` 17px, `h1` 47px.
12. **ЗАКРЫТО. «Карта эффектов» слита в `docs/index.html`** (раздел 04),
    не отдельная ссылка (`effects-map.html` на диске не удалён — эталон
    техники, как и прочие замороженные прототипы).

    **Реестр цвета точек `.dot-row`/`.card` (карточки, раздел 02/04) —
    единая, непротиворечивая таблица, каждый цвет имеет видимую
    расшифровку на странице (легенда `.dot-legend`):** зелёный =
    согласный/основа; обесцвеченный зелёный (`--c-stem`, opacity:.42) =
    выпадение (lopa) — тот же сегмент, теряющий существование, НЕ другой
    цвет; серебро = гуна; золото = вриддхи; охристый (`--c-weak`) =
    слабая ступень/сампрасарана СТРОГО (не аваграха — та переехала на
    `--c-ending`, найденная и исправленная путаница двух несовместимых
    значений одного токена в старом файле); бирюза = новый сегмент (НЕ
    ассимиляция rule70/71 — там ничего физически нового не возникает,
    меняется звонкость/место уже существующего согласного, тоже
    `--c-ending`); горчичный (`--c-ending`) = граница/окончание/
    аваграха/ассимиляция места; терракота (`--c-trigger`) = внешний
    триггер-катализатор; лак-краситель (`--c-annotation: #7A3B4E`) —
    ОТДЕЛЬНЫЙ сигнал уровня карточки целиком (не точки), «это наше
    собственное дополнение, не только материал источника» (`.card.
    annotated`/`.status.annotated`) — единственный пример на сегодня —
    suhārt.

### Текущая фаза — автоматическая постройка по номерам правил
Все механики из `interaction-registry.md` (Часть Г, итог) закрыты, кроме
двух узких пунктов (правило 36 — третий вариант `transform` без гуна/
вриддхи-семантики, RUKI; правило 23 — пара синхронных `transform` для
compound 2→2), ни один из которых не встречается в правилах 1–20 (правило
5 ЗАКРЫТО — при реальном построении оказалось не `transform`, а `split`,
см. статус правила 5 в Части 5.2 и `interaction-registry.md`, Часть В,
п.3). Прямое решение
пользователя: продолжать строить правила ПО ПОРЯДКУ НОМЕРОВ, начиная с
правила 1 (`grade`, `influence`+`transform`, гуна — механика уже доказана
на agnayas). Тот же процесс, что и раньше (Часть 7: план → подтверждение
→ данные → численная проверка → тестовый полигон → визуальная проверка →
интеграция), но БЕЗ отдельного обсуждения новой механики для каждого —
только когда правило действительно требует что-то новое (5/36/23 выше).

### Открытые вопросы по пилюлям-подписям (не решены, ждут пользователя)

12. **rule42 — пилюли «Вриддхи» и «Элизия» полностью перекрываются по
    времени, не идут последовательно.** Численно: «Вриддхи» видна
    7040→11740мс (начало активного вращения → конец паузы-фиксации),
    «Элизия» — 7340→10540мс (весь тихий elide `a`) — ВТОРАЯ целиком
    внутри окна ПЕРВОЙ. Варианты: (а) оставить как есть — above/below
    пространственно разнесены, не путаются визуально; (б) убрать подпись
    у `quiet`-elide вовсе — та же логика, что уже убрала у него искры и
    ускоренное сжатие («не спорить вниманием с главным событием»); (в)
    отложить появление подписи elide до конца показа «Вриддхи» — но тогда
    подпись оторвётся по времени от самого действия (a уже давно исчезла).
    Склоняюсь к (б) — решение за пользователем.
13. **rule70/71 — «Озвончение» терминологически ВИД «Ассимиляции»
    (ассимиляция по звонкости), не параллельная, а вложенная категория.**
    Сейчас `assimToNeighbor` (h→dh, полное место образования) подписана
    просто «Ассимиляция», `vargaPair` (t→d, k→g — только звонкость/
    придыхание/назальность внутри той же варги) — «Озвончение»/
    «Оглушение»/«Назализация». Оставить раздельными специфичными
    подписями (педагогически точнее момент за моментом) или явно
    показать вложенность (например, переименовать `assimToNeighbor` в
    «Ассимиляция места», чтобы читалось как парная категория с
    «Озвончение» под общим корнем «ассимиляция») — не решено, ждёт
    пользователя. Правило 71 (vāk asti) использует ту же «Озвончение» —
    тот же вопрос, не отдельный.

---

## Часть 7 — Как построить новый пример (методология)

0. **Сверить формулировку правила с Уитни и найти точный номер сутры
   Панини, ПРЕЖДЕ чем писать Поток А.** Прямое требование (найдено при
   rule1/rule10 — короткая формулировка Эменó несколько раз оказывалась
   односторонней/неполной без этой сверки, а `interaction-registry.md`
   `effects-map.html` заранее — по сутрам Панини — предсказывали
   механики, которые иначе пришлось бы изобретать заново). Не отдельный
   аудит по всем 71 правилам разом (сознательно отклонено пользователем
   как слишком крупный, непроверяемый объём за один присест) — а
   обязательный шаг ВНУТРИ построения КАЖДОГО отдельного примера, по
   порядку автопостройки. Порядок источников при неясности — Часть 2а.
   Сутра Панини (не только Уитни-номер) идёт в комментарии данных, в
   `roleDemo.text` (см. п.8а ниже) и в реестр (`interaction-registry.md`).
1. Заполнить **Поток А** — грамматический разбор правила без оглядки на
   технику: пронумерованные события, что происходит и почему. Каждое
   грамматическое/сандхи-событие — отдельная строка.
2. Для каждого события Потока А сверить с **реестром (Часть 4)** — если
   подходящая техника уже есть, использовать её. Если нет — это пункт в
   гэпах (Часть 6): сначала решить, добавляется ли новый `op`-тип в
   движок или комбинация существующих, и только потом писать данные.
3. Прежде чем писать `initial`/`ops` — явно ответить на вопрос **Главного
   принципа (Часть 2)**: где граница, что физически происходит при
   контакте?
4. Проверить **зарезервированные значения (Часть 3)**: категория
   transform (какой угол/сигнал), направление раскачивания, лимит на
   сжатые блоки, цвет вспомогательных шагов.
5. Только когда каждая строка Потока А имеет пару в технике (или
   осознанно оставлена «данными, не движком») — переходить к числам
   таймлайна. Считать формулами (падение → буфер → длительность операции
   → следующий буфер), не подбирать на глаз.
6. Прогнать **численную симуляцию реального кода** (вырезанного из
   файла) на всём таймлайне — не только на happy path, включая моменты,
   где цель/сосед могут исчезнуть или пересечься.
7. Собрать тестовую страницу (`test-slot-engine-ruleN.html`), добавить
   ссылку в секцию «Слот-движок · в работе» на `docs/index.html` (секция
   заводится заново, если её сейчас нет — см. Часть 6, гэп 11, держится
   пустой не по умолчанию). Ждать визуальной проверки пользователем —
   численная проверка НЕ равна визуальной готовности.
8. После подтверждения — интегрировать в `EXAMPLES[N]`, переписать текст
   пояснения под академический регистр (Часть 1, п.3).
8а. **`roleDemo.text` обязательно называет части речи и грамматические
   категории (Часть 1, п.3а), не только фонетический механизм** — часть
   речи, род/число/падеж, тип основы и т.п. для каждого задействованного
   слова/морфемы, плюс сутра Панини из шага 0.
8б. **Каждый `op.label` (3D-пилюля) получает пару `glossaryTerm` на
   СООТВЕТСТВУЮЩЕМ `roleDemo.step`, и запись в `GLOSSARY` (`data.js`),
   если термина там ещё нет** (см. «Глоссарий терминов» выше, Часть 3) —
   термин, который зритель видит на пилюле, не должен остаться
   необъяснённым в постоянном тексте. Если шаг объясняет НЕСКОЛЬКО
   3D-событий разом (merge+transform в одном шаге и т.п.) —
   `glossaryTerm` принимает массив, не только строку.

---

## Приложение — agnayas (правило 3) целиком, как образец применения
методологии выше

Формула: **agni + as → agnaye (гуна) → agnayas (правило 3)**.

| # | Что происходит | Разряд |
|---|---|---|
| A0 | Исходная форма: `agni` + `as` | — |
| A1 | Окончание `-as` требует гунации предшествующей i-основы | [Грамм.], не сандхи (Whitney §338a) |
| A2 | `i → e` (гунация) | [Грамм.] |
| A3 | Промежуточная форма `agne`+`as` — несовместимое сочетание гласных на стыке | — |
| A4 | Правило 3: `e + a → a + y + a` | [Сандхи 3] |
| A5 | Итог: `agnayas` | — |

A1–A2 (гунация) и A4 (правило 3) — ДВА разных, независимых события с
разными причинами → два отдельных `step` с независимыми `activeSlots`,
не один растянутый шаг.

**Слоты:** 1=a, 2=g, 3=n, 4=i→e, 5=(пусто, под будущий Y), 6=a, 7=s.

**Таймлайн (мс):**

| мс | Событие |
|---|---|
| 0–2600 | Падение всех 6 букв |
| 2600–6700 | Шаг «грам.»: `influence` от {a,s} к i, `transform` i→e (серебро→серебро→истинный цвет) |
| 6700–8300 | Пауза-проявление (авто) |
| 8300–16150 | Шаг «правило 3»: `approach` (несовместимость, отскок) → `split` (пауза-осознание → подъём E → прилёт A+Y по дуге → сравнение → исчезание E) |
| 16150–17630 | Хвостовая пауза-проявление (авто) |
| 18630–20310 | `settle` (авто-старт = конец проявления + 1с) |

Точные параметры каждой операции — в `docs/app/examples/rule3-agnayas-slots.js`.
