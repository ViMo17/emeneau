// acchid- + vṛddhi + s + ta → acchaitta «вы (мн.ч.) рассекли» (правило 12) —
// s между двух смычных → 0.
//
// РАЗБОР ФОРМЫ. chid- «рассекать, раскалывать» (корень VII класса,
// конечный согласный — дентальный d) + augment a- (маркер прошедшего
// времени, здесь — аориста) + сигматический показатель аориста -s- (siC)
// + личное окончание 2 л. мн.ч. действ. залога (вторичный ряд, аористно-
// имперфектный) -ta. «acchid-» дан УЖЕ готовым (a+ch удвоено до «cch» —
// ch не может стоять после гласного внутри слова без удвоения, Whitney
// §227 — ТА ЖЕ формула, что уже построена в правиле 10, повторно
// проигрывать её здесь незачем, geminate-состояние взято как исходные
// данные, тот же приём, что и у rule10 с готовой основой «gach-»).
//
// ПРАВИЛО 12 (Whitney §233e, цитируется в §882–883 — прямая парадигма
// корня chid: «ácchāitsam» (1 sg.), «ácchāittam, ácchāittām, ácchāitta»
// (2 дв./3 дв./2 мн.) — «the omission of s in the active persons... is a
// case of very rare occurrence»). Pāṇini 8.2.26 झलो झिल jhalo jhali
// (анувритти sasya от 8.2.24, lopaḥ от 8.2.23 — «[deletion of] s after a
// root ending in a stop consonant and before an ending beginning with a
// stop consonant», сверено по независимому источнику — конспект «The
// Sanskrit Aorist», Columbia University, с самим текстом сутры на
// деванагари, не только резюме). «Jhal» — все смычные (звонкие и глухие)
// + свистящие + h, БЕЗ носовых/полугласных — оба фланга (t слева, t
// справа) входят в этот класс.
//
// НАЙДЕННАЯ ОШИБКА В РАНЕЕ ЗАПИСАННОМ ПРИМЕРЕ (interaction-registry.md):
// прежняя запись «acchid- + s-ta → acchitta» (без вриддхи) не находит
// подтверждения ни в одном источнике — единственная реально засвиде-
// тельствованная форма для chid — «acchaitta» (с вриддхи i→ai, Pāṇini
// 7.2.1 सिचि वृद्धिः परस्मैपदेषु sici vṛddhiḥ parasmaipadeṣu, «vṛddhi of a
// root before the suffix siC and before parasmaipada endings» — тот же
// независимый источник). Найдена и структурная параллель на ДРУГОМ корне
// (vas- «жить», активный аорист: avāttam/avātta — конечный смычный
// основы + пропавшее s + начальное t окончания → поверхностное «tt») —
// подтверждает, что механизм «смычный+пропавшее s+смычный→удвоенный
// смычный» регулярный, не изобретён специально под chid.
//
// ГЛАВНЫЙ ПРИНЦИП (CLAUDE.md, Часть 2) — ИСПРАВЛЕНО ПОСЛЕ ПРЯМОЙ ПРАВКИ
// ПОЛЬЗОВАТЕЛЯ («слова падают отдельно acchid + s + ta... почему опять
// падает готовое слово, ведь мы не видим взаимодействия»). Первая версия
// этого файла ошибочно роняла все 8 фонем ОДНИМ сплошным рядом без
// единого зазора — граница корень|показатель-аориста+окончание никак не
// была видна, реакции (вриддхи/cartva/lopa) читались как декорация над
// уже собранным словом, не как следствие сближения. Исправлено: ДВА
// физических блока, разделённые зазором с самого начала —
//  (1) корень+augment «acchid-» (a,c,ch,i,d) — НЕПОДВИЖНЫЙ якорь, как
//      śās в śādhi/agni в agnayas;
//  (2) сигматический показатель -s- ВМЕСТЕ с окончанием -ta (s,t,a) —
//      падают И движутся ОДНИМ жёстким блоком. Показатель -s- и
//      личное окончание грамматически неразделимы как единый акт
//      присоединения (в реальной синтетической форме нельзя присоединить
//      «просто -s-» без одновременного указания, какое именно окончание
//      следует — это не два последовательных события, а один морфоло-
//      гический комплекс), поэтому здесь один блок, не два.
// Блок (2) физически ПОДЪЕЗЖАЕT к блоку (1) (`approach`, зазор закры-
// вается) — это прибытие И ЕСТЬ причина вриддхи (Pāṇini 7.2.1, s ещё не
// физически касается корня, но уже рядом) и cartva (d→t, теперь s
// действительно соседняя буква). Личное окончание (t,a), приехавшее в
// ТОМ ЖЕ блоке, оказывается СРАЗУ по другую сторону от s — ровно тот
// момент, когда s зажато между двумя смычными (t слева от cartva, t
// справа от окончания) — лопа (правило 12 само) следует уже из
// состоявшегося прибытия, второго сближения не требуется. После лопа —
// ЕЩЁ ОДИН `approach` закрывает опустевший слот (два t становятся
// геометрически смежными — прямой смысл правила).
//
// ТРИ СОБЫТИЯ ЦЕПОЧКОЙ, НИ ОДНОГО НОВОГО В ДВИЖКЕ (все три уже построены
// на других правилах):
//  B1 [Грамм., НЕ сандхи] — корневой гласный i получает вриддхи (i→ai) —
//     та же операция, что вриддхи в правиле 42 (TRANSFORM_KIND.vrddhi,
//     золото, 720°).
//  B3 [Правило 31, cartva] — корневой d перед глухим s оглушается в t —
//     ТА ЖЕ операция, что уже дважды показана на suhārt/taddhiraṇyam
//     (TRANSFORM_KIND.vargaPair, 180°, Pāṇini 8.4.55 khari ca).
//  B5 [Правило 12] — само s, оказавшееся между двумя смычными, исчезает
//     без остатка — обычный полный (не quiet) `elide`, label:'lopa' —
//     то же явление, что уже показано на śādhi/rule55/suhārt/rule9.
//
// ГЕОМЕТРИЯ ВРАЩЕНИЯ: оба transform (B1 на i/ai, B3 на d/t) стоят между
// двумя занятыми соседями без единого зазора (после прибытия блока 2) —
// та же ситуация, что уже многократно встречалась (rule2/rule6/suhārt):
// `clearance:0`, `clearanceZ:-0.5`.
//
// fromX у второго approach — блок (2) уже физически сдвинут первым
// approach (t,a стоят не на своих домашних слотах-ключах) — тот же
// приём, что уже применён в rule1 (au на зазоре после merge).
import { mountSlotExample, slotX, SLOT, TRANSFORM_KIND } from '../lib/slot-engine.js';

// Раскладка (НЕ центрирована формулой для ВРЕМЕННОГО, начального
// состояния — центрируется ИТОГОВОЕ 7-буквенное слово, тот же принцип,
// что и у rule42): START=1 даёт итоговые «acchaitta» ровно на слотах
// 1..7 (centeredStart(7)=1) после того, как окончание закроет
// освободившийся слот 6. Слот 0 остаётся неиспользованным в начальной
// раскладке — сознательная асимметрия временного состояния ради
// центрирования финального кадра, на котором взгляд задерживается дольше.
const START = 1;
//   START+0 = a  ⎫
//   START+1 = c  │
//   START+2 = ch ⎬ БЛОК 1 (неподвижный якорь): acchid- (готово, см. rule10)
//   START+3 = i  │ (→ai, вриддхи, B1)
//   START+4 = d  ⎭ (→t, cartva, B3)
//   [START+5 — зазор: показатель+окончание ещё отдельная морф. единица]
//   START+6 = s  ⎫
//   START+7 = t  ⎬ БЛОК 2 (падает и движется ЕДИНЫМ жёстким блоком):
//   START+8 = a  ⎭ -s- (siC) + -ta (личн. оконч. 2 л. мн.ч. действ.)
export const data = {
  initial: [
    { slot: START + 0, tr: 'a' },
    { slot: START + 1, tr: 'c' },
    { slot: START + 2, tr: 'ch' },
    { slot: START + 3, tr: 'i' },
    { slot: START + 4, tr: 'd' },
    { slot: START + 6, tr: 's' },
    { slot: START + 7, tr: 't' },
    { slot: START + 8, tr: 'a' },
  ],

  steps: [
    // B1 — грамматический факт (вриддхи), не одно из 71 правил сандхи —
    // тот же kind:'grammar', что у agnayas. Покрывает approach (прибытие
    // блока 2 — физическая причина) И сам transform. activeSlots — цель
    // (i/ai) и s (конкретная буква-причина, не весь блок — вриддхи
    // вызвана именно siC, Pāṇini 7.2.1).
    { kind: 'grammar', start: 3220, end: 13020, activeSlots: [START + 3, START + 6] },
    // B3 — правило 31 (cartva), вспомогательное в этой цепочке.
    { kind: 'rule', ruleNum: 31, start: 13020, end: 19270, activeSlots: [START + 4, START + 6], color: 0xAFBFD4, primary: false },
    // B5 — правило 12 само, целевое правило этого экрана. Покрывает лопу
    // И финальный approach, закрывающий освободившийся слот.
    { kind: 'rule', ruleNum: 12, start: 19270, end: 26320, activeSlots: [START + 4, START + 6, START + 7], color: 0xAFBFD4, primary: true },
  ],

  ops: [
    // ПРИБЛИЖЕНИЕ: показатель+окончание (единый блок) подъезжают к
    // корню, закрывая исходный зазор (distance:1.0, зазор ровно в один
    // слот). retreat:false — доехали и остались (совместимое присоединение,
    // не столкновение несовместимых звуков — та же логика retreat:false,
    // что у āsīt). jitterAmp:0 — не несовместимость, дрожь здесь неуместна.
    // pulse (дефолт true) — заметный момент прибытия (Часть 2: «нужен
    // ЗАМЕТНЫЙ момент прибытия», не просто тихо подъехали).
    { type: 'approach', movers: [START + 6, START + 7, START + 8], target: START + 4, start: 3220, distance: 1.0, retreat: false, jitterAmp: 0 },

    // Причина B1: теперь, когда -s- физически рядом, оно требует вриддхи
    // корневого гласного (Pāṇini 7.2.1). 200мс буфер после прибытия
    // (approachDur дефолт 1150: 3220+1150=4370, +200=4570).
    { type: 'influence', from: START + 6, to: START + 3, start: 4570 },

    // B1: i → ai, вриддхи. Пауза чистой пульсации перед реакцией — 2600мс
    // (тот же интервал, что и везде в проекте): 4570+2600=7170.
    { type: 'transform', at: START + 3, toGlyph: 'ai', start: 7170, ...TRANSFORM_KIND.vrddhi, clearance: 0, clearanceZ: -0.5, label: 'vṛddhi' },

    // Причина B3: s (глухой), теперь физически соседняя буква, требует
    // оглушения предшествующего d (khar/cartva, Pāṇini 8.4.55). Старт —
    // сразу по завершении B1 (op._done трансформа выше, 13020).
    { type: 'influence', from: START + 6, to: START + 4, start: 13020 },

    // B3: d → t, cartva. Пауза — 2600мс (13020+2600=15620).
    { type: 'transform', at: START + 4, toGlyph: 't', start: 15620, ...TRANSFORM_KIND.vargaPair, clearance: 0, clearanceZ: -0.5, label: 'cartva' },

    // Причина B5: ДВА условия, оба уже физически на месте с самого
    // прибытия блока 2 — смычный слева (t, только что возникший через
    // cartva) И смычный справа (t окончания, приехавший В ТОМ ЖЕ блоке
    // сразу за s). Та же формула двух источников одной цели, что и в
    // rule10 (bud)/rule50/rule61. ringHoldDur:2500 (явно, как в rule10).
    // Старт — сразу по завершении B3 (19270).
    { type: 'influence', from: START + 4, to: START + 6, start: 19270, ringHoldDur: 2500 },
    { type: 'influence', from: START + 7, to: START + 6, start: 19270, ringHoldDur: 2500 },

    // B5 (правило 12 само): s исчезает без остатка — полный `elide` (НЕ
    // quiet — это и есть всё содержание показываемого правила). Старт —
    // когда ring гаснет (19270+2500=21770). Готово (cube.mesh удалён) на
    // 21770+riseDur(1300)+holdDur(800)+fadeDur(1100)=24970.
    { type: 'elide', at: START + 6, start: 21770, label: 'lopa' },

    // Окончание (t,a — те же два кубика, что приехали в блоке 2, только
    // они физически уже НЕ на своих домашних слотах-ключах после первого
    // approach — fromX отражает их реальную текущую позицию, тот же
    // приём, что в rule1) подъезжает и закрывает освободившийся слот —
    // прямой смысл правила 12: два смычных становятся видимо соседними
    // («поверхностное tt»). 200мс буфер после того, как elide реально
    // завершился (24970+200=25170).
    { type: 'approach', movers: [START + 7, START + 8], target: START + 4, fromX: [slotX(START + 7) - SLOT, slotX(START + 8) - SLOT], start: 25170, distance: 1.0, retreat: false, pulse: false, jitterAmp: 0 },

    // settle не прописан — считается автоматически, от step.end выше
    // (плотно, без запаса — совпадает с моментом завершения approach,
    // 25170+1150(approachDur, дефолт)=26320).
  ],
};

export function mount(container) {
  return mountSlotExample(container, data);
}
