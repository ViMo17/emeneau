// acchid- + vṛddhi + s + ta → acchaitta «вы (мн.ч.) рассекли» (правило 12) —
// s между двух смычных → 0.
//
// РАЗБОР ФОРМЫ. chid- «рассекать, раскалывать» (корень VII класса,
// конечный согласный — дентальный d) + augment a- (маркер прошедшего
// времени, здесь — аориста) + сигматический показатель аориста -s- (siC)
// + личное окончание 2 л. мн.ч. действ. залога (вторичный ряд, аористно-
// имперфектный) -ta. «acchid-» дан УЖЕ готовым (a+ch удвоено до «cch» —
// ch не может стоять после гласного внутри слова без удвоения, Whitney
// §227 — ТА ЖЕ формула, что уже построена в правиле 10, повторно
// проигрывать её здесь незачем, geminate-состояние взято как исходные
// данные, тот же приём, что и у rule10 с готовой основой «gach-»).
//
// ПРАВИЛО 12 (Whitney §233e, цитируется в §882–883 — прямая парадигма
// корня chid: «ácchāitsam» (1 sg.), «ácchāittam, ácchāittām, ácchāitta»
// (2 дв./3 дв./2 мн.) — «the omission of s in the active persons... is a
// case of very rare occurrence»). Pāṇini 8.2.26 झलो झिल jhalo jhali
// (анувритти sasya от 8.2.24, lopaḥ от 8.2.23 — «[deletion of] s after a
// root ending in a stop consonant and before an ending beginning with a
// stop consonant», сверено по независимому источнику — конспект «The
// Sanskrit Aorist», Columbia University, с самим текстом сутры на
// деванагари, не только резюме). «Jhal» — все смычные (звонкие и глухие)
// + свистящие + h, БЕЗ носовых/полугласных — оба фланга (t слева, t
// справа) входят в этот класс.
//
// НАЙДЕННАЯ ОШИБКА В РАНЕЕ ЗАПИСАННОМ ПРИМЕРЕ (interaction-registry.md):
// прежняя запись «acchid- + s-ta → acchitta» (без вриддхи) не находит
// подтверждения ни в одном источнике — единственная реально засвиде-
// тельствованная форма для chid — «acchaitta» (с вриддхи i→ai, Pāṇini
// 7.2.1 सिचि वृद्धिः परस्मैपदेषु sici vṛddhiḥ parasmaipadeṣu, «vṛddhi of a
// root before the suffix siC and before parasmaipada endings» — тот же
// независимый источник). Найдена и структурная параллель на ДРУГОМ корне
// (vas- «жить», активный аорист: avāttam/avātta — конечный смычный
// основы + пропавшее s + начальное t окончания → поверхностное «tt») —
// подтверждает, что механизм «смычный+пропавшее s+смычный→удвоенный
// смычный» регулярный, не изобретён специально под chid.
//
// ТРИ СОБЫТИЯ ЦЕПОЧКИ, НИ ОДНОГО НОВОГО В ДВИЖКЕ (все три уже построены
// на других правилах):
//  B1 [Грамм., НЕ сандхи] — корневой гласный i получает вриддхи (i→ai) —
//     та же операция, что вриддхи в правиле 42 (TRANSFORM_KIND.vrddhi,
//     золото, 720°), причина — предстоящий сигматический показатель -s-.
//  B3 [Правило 31, cartva] — корневой d перед глухим s оглушается в t —
//     ТА ЖЕ операция, что уже дважды показана на suhārt/taddhiraṇyam
//     (TRANSFORM_KIND.vargaPair, 180°, Pāṇini 8.4.55 khari ca).
//  B5 [Правило 12] — само s, оказавшееся между двумя смычными (t слева
//     от cartva, t справа — начало окончания), исчезает без остатка —
//     обычный полный (не quiet) `elide`, label:'lopa' — то же явление,
//     что уже показано на śādhi/rule55/suhārt/rule9.
// Причина B5 — ДВА одновременных условия (t слева И t справа), та же
// формула двух источников одной цели, что уже применена в rule10 (bud)/
// rule50/rule61.
//
// ПОСЛЕ lopa — окончание (t,a) подъезжает и закрывает ОСВОБОДИВШИЙСЯ слот
// (та же техника approach-закрытия зазора, что в rule42/rule9/suhārt) —
// сознательное решение, не автоматическое поведение движка: смысл самого
// правила 12 именно в том, что два смычных становятся ВИДИМО СОСЕДНИМИ
// (поверхностное «tt»), оставлять постоянный зазор здесь противоречило
// бы содержанию примера (в отличие от śādhi/rule55, где после elide
// ничего не обязано физически сближаться).
//
// ГЕОМЕТРИЯ ВРАЩЕНИЯ: оба transform (B1 на i/ai, B3 на d/t) стоят между
// двумя занятыми соседями без единого зазора — та же ситуация, что уже
// многократно встречалась (rule2/rule6/suhārt): `clearance:0`,
// `clearanceZ:-0.5` (выдвижение от камеры, спасает от прохождения сквозь
// текстуру соседа на диагоналях вращения — сам эффект не зависит от угла
// поворота, только от того, что угловая точка кубика при вращении дальше
// от центра, чем его грань в покое).
import { mountSlotExample, centeredStart, TRANSFORM_KIND } from '../lib/slot-engine.js';

// Раскладка: centeredStart(8)=1, все 8 фонем даны с самого начала без
// зазора (морфологическая граница корень|показатель+окончание НЕ
// разведена — та же логика, что у agnayas: causing-ending уже сидит
// рядом с самого начала, реакция показана через influence, не approach,
// см. Часть 2/Главный принцип — approach нужен только там, где стороны
// физически СБЛИЖАЮТСЯ, а не там, где причина просто соседствует).
const START = centeredStart(8); // = 1
//   START+0 = a  — augment (маркер аориста)
//   START+1 = c  ⎫
//   START+2 = ch ⎭ acchid- — геминированное ch (готово, см. правило 10)
//   START+3 = i  — корневой гласный (→ai, вриддхи, B1)
//   START+4 = d  — конечный согласный корня (→t, cartva, B3)
//   START+5 = s  — сигматический показатель аориста (→∅, lopa, B5)
//   START+6 = t  — начало окончания -ta (2 л. мн.ч. действ.)
//   START+7 = a  — конец окончания -ta
export const data = {
  initial: [
    { slot: START + 0, tr: 'a' },
    { slot: START + 1, tr: 'c' },
    { slot: START + 2, tr: 'ch' },
    { slot: START + 3, tr: 'i' },
    { slot: START + 4, tr: 'd' },
    { slot: START + 5, tr: 's' },
    { slot: START + 6, tr: 't' },
    { slot: START + 7, tr: 'a' },
  ],

  steps: [
    // B1 — грамматический факт (вриддхи), не одно из 71 правил сандхи —
    // тот же kind:'grammar', что у agnayas (без ruleNum, стандартный
    // охровый is-grammar, не притворяется главным событием примера).
    { kind: 'grammar', start: 3220, end: 11670, activeSlots: [START + 3, START + 5] },
    // B3 — правило 31 (cartva), вспомогательное в этой цепочке (не то,
    // ради чего открыт этот экран) — primary:false, тот же стандартный
    // охровый цвет, что и у любого не-главного rule-шага.
    { kind: 'rule', ruleNum: 31, start: 11670, end: 17920, activeSlots: [START + 4, START + 5], color: 0xAFBFD4, primary: false },
    // B5 — правило 12 само, целевое правило этого экрана.
    { kind: 'rule', ruleNum: 12, start: 17920, end: 24970, activeSlots: [START + 4, START + 5, START + 6], color: 0xAFBFD4, primary: true },
  ],

  ops: [
    // Причина B1: предстоящий сигматический показатель -s- требует
    // вриддхи корневого гласного (Pāṇini 7.2.1) — influence не требует
    // физического соседства (s стоит через d от i, гэп DISTANT уже решён
    // как «просто работает», см. CLAUDE.md Часть 6, п.4а).
    { type: 'influence', from: START + 5, to: START + 3, start: 3220 },

    // B1: i → ai, вриддхи. Пауза чистой пульсации перед реакцией — 2600мс
    // (тот же интервал, что и везде в проекте): 3220+2600=5820.
    { type: 'transform', at: START + 3, toGlyph: 'ai', start: 5820, ...TRANSFORM_KIND.vrddhi, clearance: 0, clearanceZ: -0.5, label: 'vṛddhi' },

    // Причина B3: s (глухой) требует оглушения предшествующего d — ТЕПЕРЬ
    // физически соседняя буква (khar/cartva, Pāṇini 8.4.55). Старт — сразу
    // по завершении B1 (op._done транформа выше, 11670).
    { type: 'influence', from: START + 5, to: START + 4, start: 11670 },

    // B3: d → t, cartva. Пауза — 2600мс (11670+2600=14270).
    { type: 'transform', at: START + 4, toGlyph: 't', start: 14270, ...TRANSFORM_KIND.vargaPair, clearance: 0, clearanceZ: -0.5, label: 'cartva' },

    // Причина B5: ДВА одновременных условия — смычный слева (t, только
    // что возникший через cartva) И смычный справа (t окончания) — та же
    // формула двух источников одной цели, что и в rule10 (bud)/rule50/
    // rule61. ringHoldDur:2500 (явно, как в rule10) — ring гаснет РОВНО к
    // моменту начала elide, не оставляя лишнего хвоста. Старт — сразу по
    // завершении B3 (17920).
    { type: 'influence', from: START + 4, to: START + 5, start: 17920, ringHoldDur: 2500 },
    { type: 'influence', from: START + 6, to: START + 5, start: 17920, ringHoldDur: 2500 },

    // B5 (правило 12 само): s исчезает без остатка — полный `elide` (НЕ
    // quiet — это и есть всё содержание показываемого правила, тот же
    // класс, что śādhi/rule55/rule9/suhārt). Старт — когда ring гаснет
    // (17920+2500=20420). Готово (cube.mesh удалён) на
    // 20420+riseDur(1300)+holdDur(800)+fadeDur(1100)=23620.
    { type: 'elide', at: START + 5, start: 20420, label: 'lopa' },

    // Окончание (t,a) подъезжает и закрывает освободившийся слот — прямой
    // смысл правила 12: два смычных становятся видимо соседними
    // («поверхностное tt»), см. комментарий в начале файла. 200мс буфер
    // после того, как elide реально завершился (23620+200=23820).
    // approachDur — дефолт движка (1150), holdDur:0/retreat:false —
    // доехало и осталось на месте (та же формула, что rule42/rule9/suhārt).
    { type: 'approach', movers: [START + 6, START + 7], target: START + 4, start: 23820, distance: 1, retreat: false, pulse: false, jitterAmp: 0 },

    // settle не прописан — считается автоматически, от step.end выше
    // (плотно, без запаса — совпадает с моментом завершения approach,
    // 23820+1150(approachDur, дефолт)=24970).
  ],
};

export function mount(container) {
  return mountSlotExample(container, data);
}
