// tatra eti → tatraiti (правило 42) на слот-движке — ПЕРВЫЙ настоящий
// пример вриддхи в проекте (гэп из CLAUDE.md, Часть 6, п.6, движковая
// часть — signal:'gold'/matsGold/TRANSFORM_KIND.vrddhi — уже готова).
// Взято НЕ по эстетическому выбору, а по прямому указанию пользователя:
// первое правило из 71 (по номеру), где механизм — настоящая вриддхи, не
// гуна. Правила 2/6 (guṇa) и 36 (RUKI-подобное, НЕ guṇa/вриддхи, см.
// docs/reference/interaction-registry.md) идут раньше по номеру, но не
// подходят; 42 — первое совпадение.
//
// Правило по источнику (data.js, emeno): «Сочетание a или ā с дифтонгом
// заменяется одной морфофонемой, соответствующей долгому дифтонгу»
// (Whitney §127 — та же единая таблица, что и у правила 1: и a+au→au, и
// a+e→ai — строки одной вриддхи-таблицы, обе под общей адхикарой Pāṇini
// 6.1.84 ekaḥ pūrvaparayoḥ. Конкретный номер под-сутры для ИМЕННО пары
// «a+e» указан здесь как 6.1.104 из более ранней, менее строгой сверки
// этой сессии — НЕ переподтверждён по первоисточнику при разборе rule1/
// rule2 (проверялся, но найденный там текст оказался про 6.1.109,
// pūrvarūpa, — это ДРУГОЙ порядок звуков, e/o+a, не наш a+e; точный номер
// для tatra+eti остаётся открытым, не пытаться угадывать дальше). e и o
// в санскритской грамматике — «дифтонги» (гуна-ступень), ai/au —
// «долгие дифтонги» (вриддхи-ступень) — то же вертикальное отношение,
// что между i/u (спад) и e/o (гуна) в правилах 2/6/43, только на ступень
// выше. a и e — РАВНОЗНАЧНЫЕ участники eka-ādeśa (та же операция, что и
// a+au→au в правиле 1) — a поглощается ВНУТРИ акта, что производит ai,
// не «условие», которое просто исчезает.
//
// ГЛАВНЫЙ ПРИНЦИП (CLAUDE.md, Часть 2): граница между tatra и eti —
// внешние сандхи, слова падают РАЗДЕЛЁННЫМИ обычным зазором в один слот
// (тот же приём, что vāk asti/taddhiraṇyam). В отличие от ТЕХ примеров
// (граница остаётся зазором навсегда), здесь результат — ОДНО слитное
// слово «tatraiti»: после того как a исчезает (elide) и e становится ai
// (transform, вриддхи), окончание -ti подъезжает (approach) и закрывает
// СРАЗУ два зазора одним движением — освободившийся слот a И исходный
// зазор границы слов (distance:2, не 1).
//
// Направление раскачивания transform — ВЛЕВО (clearance:-0.35): справа от
// e/ai сосед t (eti), место занято; слева на момент вращения — зазор,
// пусто.
//
// ЦЕНТРИРОВАНИЕ — взвешенное решение по прямому замечанию пользователя
// («пример заметно смещён от центра»): раскладка сдвинута на +1 слот
// относительно первой версии. Причина смещения не в формуле центрирования
// как таковой — occupied-диапазон ДО событий (9 слотов, с учётом
// временного «зазора в 2» под eti) центрировался верно, но ПОСЛЕ elide+
// approach итоговое слово занимает всего 7 слотов, и именно ЭТОТ,
// финальный результат — то, на чём взгляд задерживается дольше всего
// (устоявшееся состояние, а не переходный кадр). Раскладка теперь
// подобрана под centeredStart(7)=1 (итоговые 7 букв «tatraiti» занимают
// слоты 1..7, центр стенда), а не под временный 9-слотовый разлёт до
// начала событий.
import { mountSlotExample, TRANSFORM_KIND } from '../lib/slot-engine.js';

//   1 = t ⎫
//   2 = a │
//   3 = t ⎬ tatra — первое слово
//   4 = r │
//   5 = a ⎭ (nimitta — исчезнет, поглощена результатом)
//   [6 — зазор: граница между словами]
//   7 = e ⎫ (sthānin — станет ai)
//   8 = t ⎬ eti — второе слово
//   9 = i ⎭
export const data = {
  initial: [
    { slot: 1, tr: 't' },
    { slot: 2, tr: 'a' },
    { slot: 3, tr: 't' },
    { slot: 4, tr: 'r' },
    { slot: 5, tr: 'a' },
    { slot: 7, tr: 'e' },
    { slot: 8, tr: 't' },
    { slot: 9, tr: 'i' },
  ],

  // Один шаг — сама сандхи, без предварительной грамматики (как у vāk
  // asti/śādhi): activeSlots — только настоящие участники (a-nimitta,
  // e/ai-sthānin), остальные буквы обоих слов — контекст, притенены.
  // color: 0xC7BAA8 — тот же hex, что у .eg-chip.c4 (тир 41-49) в
  // sanskrit-sandhi-app.css, сверено перед интеграцией. end — с запасом
  // после approach (см. её start+approachDur ниже), не только после
  // transform — весь причинно-следственный ряд остаётся подсвеченным, не
  // только гунирование само по себе (у этого примера нет следующего
  // шага, торопиться с приглушением незачем).
  steps: [
    { kind: 'rule', ruleNum: 42, start: 3440, end: 12600, activeSlots: [5, 7], color: 0xC7BAA8, primary: true },
  ],

  ops: [
    // Влияние a→e: непрерывный текстурный пульс на a + бегущие волны.
    // frameSignal:'gold' — прямой запрос пользователя: часть слова,
    // вызывающая необходимость вриддхи, подчёркивается на всю длину
    // золотой сияющей полоской. Одиночный источник — раньше рамка не
    // показывалась бы вообще (нечего объединять), frameSignal форсирует
    // показ даже для одной буквы, потому что здесь это не группировка,
    // а сигнал «отсюда идёт вриддхи». ringHoldDur:4000 — тот же интервал,
    // что и в agnayas (гуна): от начала влияния через фиксированный
    // INFLUENCE_TO_TRANSFORM_GAP (2600) и немного в начало самого
    // вращения, не до полного приземления — тот же ритм, что уже
    // одобрен на гунации.
    { type: 'influence', from: 5, to: 7, start: 3540, ringHoldDur: 4000, frameSignal: 'gold' },

    // Само вриддхирование: e → ai, НА МЕСТЕ. TRANSFORM_KIND.vrddhi —
    // 720° на общей скорости MS_PER_360 (2000мс/оборот — замедлено по
    // прямому замечанию пользователя после проверки agnayas, тот же
    // множитель, что и у гунации, просто вдвое дольше по числу оборотов,
    // не отдельная скорость). Во время активного вращения — золотая
    // россыпь искр (spawnSparkleBurst внутри applyTransform, включается
    // автоматически при signal:'gold', ничего дополнительно указывать не
    // нужно). clearance:-0.35 — влево, к пустому зазору (слот 6); вправо
    // — занято (t из eti).
    { type: 'transform', at: 7, toGlyph: 'ai', start: 6140, ...TRANSFORM_KIND.vrddhi, clearance: -0.35, label: 'vṛddhi' },

    // Реакция: a поглощается результатом — та же механика elide
    // (погружение вниз в отстойник, вспышка в момент начала), что и s→∅
    // в śādhi. Старт — вскоре после начала активного вращения e→ai (не
    // одновременно с самим влиянием) — читается как СЛЕДСТВИЕ вриддхи,
    // не независимое совпадающее по времени событие. quiet:true — ЭТО
    // исчезновение не главное событие примера (главное — уже показанное
    // золотое вращение e→ai): визуальный акцент здесь на результате, не
    // на самом факте поглощения — яркая россыпь искр отвлекала бы от уже
    // отыгранной кульминации. Критерий закреплён в реестре (CLAUDE.md,
    // Часть 4). Вспышка-удар в момент НАЧАЛА реакции остаётся — это
    // отдельный, всегда обязательный сигнал, не декоративный эффект.
    // label:'ekādeśa' (НЕ 'lopa' — найденная и исправленная ошибка,
    // тот же вопрос, что решён для rule1/rule2: lopa означает замену на
    // НОЛЬ, P. 1.1.60 adarśanaṃ lopaḥ; здесь видимый результат есть,
    // просто он достался соседнему кубику — a РАВНОЗНАЧНЫЙ участник
    // eka-ādeśa, не «условие», см. коммент в начале файла).
    { type: 'elide', at: 5, start: 7340, quiet: true, label: 'ekādeśa' },

    // Только теперь, когда a исчезло и e уже стало ai, окончание -ti
    // (t, i) подъезжает и закрывает оба зазора одним движением —
    // distance:2 (не 1, как в большинстве примеров): один зазор — от
    // исчезнувшего a, второй — исходная граница слов. Старт — после
    // ПОЗДНЕЙШЕГО из двух событий: transform (активный старт 7040 +
    // dur 4000 = приземление 11040) и elide (~7340+3200=10540 общая
    // длительность) — transform заканчивается позже, +300 буфер = 11340.
    // Слово становится цельным именно теперь, не раньше.
    { type: 'approach', movers: [7, 8, 9], target: 4, start: 11340, distance: 2, retreat: false, pulse: false, jitterAmp: 0 },

    // settle не прописан — считается автоматически: 1с после того, как
    // последняя буква вернёт истинный цвет.
  ],
};

export function mount(container) {
  return mountSlotExample(container, data);
}
