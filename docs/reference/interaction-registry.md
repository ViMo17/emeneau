# Реестр взаимодействий и механик — все 71 правило

Полный реестр по замыслу пользователя: (1) типовые взаимодействия букв в
каждом правиле — уже зафиксированы 2D-системой (`roleDemo.type` в
`docs/app/data.js`) для 65/71 правил; (2) типовые механики слот-движка —
уже зафиксированы в 8 типах операций (`docs/app/lib/slot-engine-ops.js`).
Этого файла раньше не было: он сводит (1) и (2) в одну таблицу на все
71 правило, чтобы для НОВОГО 3D-примера не подбирать механику заново,
а смотреть готовое сопоставление. Не дублирует `CLAUDE.md` — там
находится реестр УЖЕ ПОСТРОЕННЫХ эффектов (Часть 4, только 5 правил),
здесь — реестр ЗАПЛАНИРОВАННЫХ механик на все правила, включая
непостроенные.

**Как читать:** колонка «Тип» — из существующей 2D-классификации (или новая,
если правило не классифицировано). Колонка «Механика» — какую операцию(-и)
слот-движка использовать при построении 3D-примера. Колонка «Заметки» —
расхождения, открытые вопросы, зависимость от непостроенной механики.

**Генератор (заходы 81, 83, 84, 87) покрывает ВСЕ 4 механики, задействованные
в 5 уже построенных примерах** — `docs/app/lib/slot-engine-generate.js`:
`buildInfluenceTransformExample`/`buildInfluenceTransformChain` (influence+
transform, один шаг или цепочка — voicing/place/часть grade),
`buildApproachMergeExample` (approach+merge — a+a→ā), `buildApproachElideExample`
(approach+elide — s+dh→∅), `buildGunaSplitExample` (guṇa+split — самая
сложная, две механики подряд). Каждый сверен численно с реальным эталоном
(раскладка побайтово, тайминги — в пределах 20–250мс постоянного смещения
по буферу после падения, не расползающейся ошибки). НЕ покрыты: elide/
merge/split/цепочки для правил, ещё не построенных ни разу (весь остальной
реестр ниже) — генератор пока обобщает только уже проверенные образцы,
не гарантирует правильность для незнакомой комбинации без отдельной
проверки.

---

## Часть А — Общее правило сопоставления (тип → механика)

| 2D-тип | Механика по умолчанию | Обоснование |
|---|---|---|
| `place` (место-ассимиляция) | `influence` + `transform(TRANSFORM_KIND.assimToNeighbor)` | Уже построено (rule 71 taddhiraṇyam-класс), совпадает 1:1 |
| `voicing` (звонкость/пары внутри варги) | `influence` + `transform(TRANSFORM_KIND.vargaPair)` | Уже построено (rule 71 vāk asti), совпадает 1:1 |
| `grade` (гуна/вриддхи/полугласный) | `influence` + `transform` (spinTurns 1/signal:silver — гуна; spinTurns 2/signal:gold — вриддхи, см. Часть В) | Построено на agnayas (грам. шаг i→e) |
| `elide` (исчезновение без замены) | `elide` | Построено на śādhi (s→∅), прямое соответствие |
| `compound`, source длиннее result (2→1) | `merge` | Построено на āsīt (a+a→ā, частный случай, см. ниже) |
| `compound`, result длиннее source (1→2) | `split` | Построено на agnayas (e→a+y) |
| `compound`, source и result — оба массивы РАВНОЙ длины (2→2) | **НЕ merge/split** — пара синхронных `transform` (каждая ячейка меняется на месте на свою пару) | Новый вывод этой сессии, см. правило 23 в таблице ниже |
| `lexical`, source/result — оба одна ячейка, БЕЗ видимого триггера | `transform` (без `influence` — нет отдельной клетки-причины для показа) | Большинство «lexical»-правил (11,13,14,16,17,18,25,26,28,29,35,57,58,60,65,66,67,68) |
| `lexical`, source — на деле два ОДИНАКОВЫХ соседних звука сливаются | `merge` | Правила 7 (уже построено, āsīt) и 44 (тот же механизм, внешнее сандхи) |
| `double` (удвоение, гемина́ция) | **ОТКРЫТО** — см. Часть В, п.1 | Правила 10, 12(частично), 49, 61, 65(частично) |
| `wholevarga` (весь ряд варги разом) | На деле НЕ требует новой механики — см. Часть В, п.2 | Правило 33 |

---

## Часть Б — Полная таблица по правилам

Формат: **N** — тип(ы) — механика — заметки.

| N | Тип (2D) | Механика (3D) | Заметки |
|---|---|---|---|
| 1 | grade | influence + transform (гуна/долгий дифтонг, spinTurns:1) | — |
| 2 | grade | influence + transform (гуна, silver) | — |
| 3 | compound (1→2) | split | **Построено** (agnayas) |
| 4 | compound (1→2) | split | Аналогично agnayas (ī+ā→i+ya) |
| 5 | (нет EXAMPLES) | influence + transform, НОВЫЙ подтип «замена гласного по соседу без гуны/вриддхи» | См. Часть В, п.3 — нужно решение по `signal` |
| 6 | grade | influence + transform (полугласный, spinTurns:1) | — |
| 7 | lexical (на деле слияние) | merge | **Построено** (āsīt, a+a→ā) |
| 8 | (нет EXAMPLES) | нет самостоятельной механики | Только пояснение-исключение к правилу 9; пример (`suhārt`) — это готовая иллюстрация правила 31 (девуазинг), см. Часть В, п.4 |
| 9 | (нет EXAMPLES) | elide (цепочка, по одной на каждую лишнюю согласную) | Механика уже поддержана (несколько `elide` подряд), нового узла не требуется |
| 10 | double (2D-тег, но фактически НЕ истинное удвоение) | split | **ПЕРЕКЛАССИФИЦИРОВАНО**: «cch» орфографически c+ch (РАЗНЫЕ буквы — деаспирация первой копии при геминации придыхательных, стандартное правило Уитни), не идентичное удвоение. Обычный `split` (agnayas-класс), не тест гэпа DOUBLE. См. Часть В, п.1 |
| 11 | lexical | transform | — |
| 12 | double + voicing + elide | elide (главный эффект — s→∅); double — общая иллюстрация правила 10 в этой же цепочке | Составной пример, каждый тег — за своё правило |
| 13 | lexical | transform | — |
| 14 | lexical | transform | — |
| 15 | (нет 2D type, есть 3D) | elide | **Построено** (śādhi) |
| 16 | lexical | transform | — |
| 17 | lexical | transform | Цепочка вместе с 31/33 в одном примере |
| 18 | lexical | transform | Цепочка вместе с 30/33 |
| 19 | voicing | influence + transform (vargaPair) | Цепочка вместе с 36 |
| 20 | compound (2→1) | merge | kṣ → gh, слияние двух ячеек в одну |
| 21 | compound (2→1) | merge | kṣ → k |
| 22 | compound (1→2) | split | ch → k+ṣ |
| 23 | compound (2→2) | **пара синхронных `transform`**, не merge/split | śc → kṣ: каждая исходная ячейка на месте меняется на свою (ś→k, c→ṣ), не единое слияние-с-разделением |
| 24 | compound (1→2) | split | ṣ → k+ṣ |
| 25 | lexical | transform | — |
| 26 | lexical | transform | — |
| 27 | (нет EXAMPLES) | НЕ СТРОИТСЯ | «Правило без примеров» в самом источнике — нет засвидетельствованного слова, строить не на чем |
| 28 | lexical | transform | — |
| 29 | lexical | transform | Цепочка вместе с 31 |
| 30 | voicing | influence + transform (vargaPair) | — |
| 31 | voicing | influence + transform (vargaPair) | Самое частое звено во множестве цепочек (12,17,18,29,71...) |
| 32 | voicing | influence + transform (vargaPair) | — |
| 33 | wholevarga (в собств. демо), voicing/lexical (в чужих цепочках) | influence + transform (vargaPair-подобная деаспирация в рамках варги) | Для КОНКРЕТНОГО 3D-примера «wholevarga» не требует групповой цели — см. Часть В, п.2. Расхождение типа между собственным EXAMPLES[33] и встраиваниями в 17/18/20 — see Часть В, п.5 |
| 34 | (нет EXAMPLES) | influence (дальний, `spawnWave` через несколько слотов) + transform ×2 (конечная согласная теряет придыхание, начальная — приобретает) | Закон Бартоломэ — эталонный пример категории DISTANT; **новой механики не требует**, см. Часть В, п.6 |
| 35 | lexical | transform | — |
| 36 | grade (в собств. демо), voicing (в чужих цепочках) | influence + transform, НОВЫЙ подтип (RUKI — не гуна/вриддхи, не варга-пара) | См. Часть В, п.3; также расхождение типа, Часть В, п.5 |
| 37 | place | influence + transform (assimToNeighbor) | — |
| 38 | place | influence + transform (assimToNeighbor) | — |
| 39 | place | influence + transform (assimToNeighbor) | — |
| 40 | place | influence + transform (assimToNeighbor) | — |
| 41 | (нет EXAMPLES) | `approach` БЕЗ последующей реакции — постоянный зазор | Прагрхья — «протективное» правило, ничего не происходит; переиспользует технику «зазор остаётся навсегда» (vāk asti/taddhiraṇyam), см. Часть В, п.7 |
| 42 | grade | influence + transform (долгий дифтонг) | Внешнее сандхи, аналог правила 1 |
| 43 | grade | influence + transform (гуна) | Внешнее сандхи, аналог правила 2 |
| 44 | lexical (на деле слияние) | merge | Внешнее сандхи, аналог правила 7 |
| 45 | grade | influence + transform (полугласный) | Аналог правила 6 |
| 46 | elide | elide | Аваграха — a исчезает |
| 47 | grade | transform (без influence — самостоятельное правило, не обратный ход гунации, см. заход 79) | -e/-ai → a/ā перед не-a гласным |
| 48 | compound (1→2) | split | au → ā+v |
| 49 | double (2D-тег, но фактически НЕ истинное удвоение) | split | Тот же случай, что 10 (ch→cch = c+ch, разные буквы) — «между словами», механика та же, обычный `split`. См. Часть В, п.1 |
| 50 | compound (2→1) | merge | -as+a → o |
| 51 | elide | elide | -as+гласный(не a) → -a |
| 52 | elide | elide | -s в sas/eṣas → ∅ |
| 53 | compound (2→1) | merge | -as/-aḥ+звонкий → o |
| 54 | elide | elide | -s в bhos → ∅ |
| 55 | elide (+ встроенное правило 7) | elide | **Построено** (тестовый полигон) — devās взято ГОТОВЫМ словом, правило 7 (упомянутое в 2D-пояснении как фоновая справка о происхождении формы) в 3D НЕ переигрывается; `influence`+`elide` на месте, внешняя граница не схлопывается |
| 56 | elide + lexical | elide + transform (компенсаторное удлинение предшествующего гласного) | Два эффекта одного правила, не противоречие |
| 57 | lexical | transform | -s/-r → висарга |
| 58 | lexical | transform | висарга+звонкий → r |
| 59 | place | influence + transform (assimToNeighbor) | висарга+глухой → сибилянт |
| 60 | lexical | transform | -m → анусвара |
| 61 | double (истинная геминация) | `bud` (НОВАЯ операция, не split/merge) | **Построено** (hasannagacchat, n→nn, тестовый полигон) — первый реальный тест гэпа DOUBLE, см. Часть В, п.1 (обновлено) |
| 62 | compound (1→2) | split | -n+глухой → ṃ+сибилянт |
| 63 | place | influence + transform (assimToNeighbor) | -n+звонкий → ñ/ṇ/n |
| 64 | compound (1→2) | split | -n+ś → ñ+ch |
| 65 | lexical + double (истинная геминация) | transform (n→ṃ) + `bud` (удвоение l, та же механика, что rule61) | Не построено, механика уже доказана на rule61 |
| 66 | lexical | transform | -t+ś → t+ch |
| 67 | lexical | transform | -t+нёб./цер. → c/ṭ |
| 68 | lexical | transform | -t+l → l |
| 69 | voicing | influence + transform (vargaPair-подобная назализация) | — |
| 70 | voicing | influence + transform (vargaPair) | **Построено** (taddhiraṇyam, только тестовый полигон) |
| 71 | voicing | influence + transform (vargaPair) | **Построено** (vāk asti, только тестовый полигон) |

---

## Часть В — Открытые вопросы, обнаруженные при построении реестра

1. **ЗАКРЫТО. DOUBLE (гемина́ция).** Первый реальный пример — rule 61
   (hasan+agacchat→hasannagacchat, n→nn). `split` (гипотеза «одинаковый
   toGlyph в arrivals») ОТКЛОНЕНА — подъём в отстойник неверно читается
   для истинного удвоения (буква не «превращается через паузу-разрыв», а
   отпочковывается сразу). Построена НОВАЯ операция `bud` — зеркало
   `merge`: источник остаётся на месте и вспыхивает в момент появления
   клона, клон появляется на позиции источника и едет по прямой в свой
   слот (см. CLAUDE.md, реестр типов операций, и `slot-engine-ops.js`).
   Заодно переклассифицированы 10/49 (ch→cch) — орфографически c+ch,
   РАЗНЫЕ буквы (деаспирация при геминации придыхательных, правило
   Уитни), это НЕ истинное удвоение, а обычный `split`. Остаются не
   построены (та же механика `bud`, что и у rule61): 65 (l→ll).
2. **wholevarga (правило 33) — переосмыслено, группового таргета НЕ
   требует.** Ранее (CLAUDE.md, гэп №4) считалось, что «групповая цель»
   — открытая механика движка. Но группа нужна только для АБСТРАКТНОЙ
   иллюстрации правила на всём алфавите (2D, статичная таблица) — в
   КОНКРЕТНОМ слове-примере (3D) обычно есть только ОДНА согласная,
   попадающая под условие, значит нужен обычный одиночный `influence`+
   `transform`, как у `voicing`. Гэп №4 в CLAUDE.md стоит сузить: групповая
   цель нужна ТОЛЬКО если решим показывать 3D-анимацию всей варги разом
   (не привязанную к конкретному слову) — отдельная, не обязательная фича.
3. **Правила 5 и 36 — тип `grade`, но НЕ гуна/вриддхи.** Правило 5
   (ṛ→ur/ūr/ir/īr по соседству с губным/иным согласным) и правило 36
   (RUKI, s→ṣ) структурно похожи на «сдвиг гласного/звука в своём
   столбце», но `TRANSFORM_KIND` пока определяет только `vargaPair`/
   `assimToNeighbor`, а «гуна»/«вриддхи» — голые параметры
   (spinTurns:1/signal:silver и spinTurns:2/signal:gold), не именованные
   пресеты. Для 5 и 36 нужен ТРЕТИЙ вариант — простая замена звука без
   гуна/вриддхи-семантики: скорее всего `spinTurns:1`+`signal:'blank'`
   (не серебро — серебро зарезервировано строго под гунацию), но
   ОКОНЧАТЕЛЬНО решить только на первом реальном примере (5 или 36),
   не проектировать вслепую заранее (тот же принцип, что и с вриддхи).
4. **Правило 8 не нуждается в собственном 3D-примере.** Это правило-
   исключение («правило 9 не действует, если взрывной — часть основы»),
   его же собственный пример (`suhārd+s→suhārt`) — это в чистом виде
   иллюстрация правила 31 (озвончение/оглушение). Предлагается: при
   построении реестра «второго примера» для правила 31 — рассмотреть
   `suhārt` как кандидат, а для правила 8 — не строить отдельный 3D-
   пример, вместо этого явно сослаться на пример правила 31 с пояснением-
   исключением в тексте.
5. **Расхождение классификации правил 33 и 36 между собственной
   демонстрацией и встраиванием в чужие цепочки.** Правило 33 в
   собственном `EXAMPLES[33]` типизировано `wholevarga`, но при встраивании
   как звено в цепочках 17/18/20 — типизировано `lexical`/`voicing`.
   Аналогично правило 36: собственная демонстрация — `grade`, встраивания
   в 19/21 — `voicing`. Технически это не баг (оба контекста осмысленно
   показывают правило по-разному), но стоит решить сознательно, не по
   инерции: годится ли `voicing`-подобная упрощённая подсветка для
   встроенных повторных показов того же правила, или нужно единообразие.
   Не исправлено в этой сессии — фиксируется как открытый вопрос
   классификации 2D-слоя, не мешает 3D-реестру (3D-механика для обоих
   контекстов одна и та же — `influence`+`transform`).
6. **DISTANT (правило 34, закон Бартоломэ) — переосмыслено, НЕ требует
   новой операции движка.** Ранее (CLAUDE.md, гэп №2) DISTANT
   планировался как отдельная категория для «неcоседних» правил вроде
   RUKI. Но `influence`/`spawnWave` уже по конструкции — «дальнодействие
   причина→цель» (см. CLAUDE.md Часть 3, описание `influence`), без
   требования физической соседности слотов. Для правила 34 достаточно:
   `influence` от конечной согласной (источник придыхания) к начальной
   согласной корня (получатель) — волна пролетает через промежуточные
   слоты как есть, — и два `transform`: конечная теряет придыхание,
   начальная приобретает. Новой механики не требуется, только пример,
   где `influence`/`spawnWave` реально применены НЕ к соседним ячейкам —
   стоит проверить, что геометрия волны (`spawnWave`) корректно тянется
   через несколько слотов визуально (не тестировалось ни разу на
   дистанции >1 слота).
7. **Правило 41 (прагрхья) — «протективный» показ, техника уже
   существует.** Ничего не меняется — сандхи не применяется. Показывать
   через уже готовую технику «зазор остаётся навсегда» (см. реестр
   CLAUDE.md Часть 4, строка «внешние сандхи, граница слов не
   схлопывается» — vāk asti, taddhiraṇyam), без слияния/трансформации
   после `approach`. Отдельный открытый вопрос (не решать сейчас,
   не блокирует реестр): нужен ли отдельный визуальный маркер «защищено
   от сандхи» (например, особая обводка/цвет зазора), чтобы отличить
   «здесь просто ещё не наступила пара» от «здесь сандхи НИКОГДА не
   произойдёт по правилу» — семантически разные вещи, сейчас неразличимы
   визуально.

---

## Часть Г — Итог: что реестр разблокировал

- **6 правил без единого зафиксированного взаимодействия** (5, 8, 9, 27,
  34, 41) теперь классифицированы (кроме 27 — подтверждено, что строить
  не на чем, и 8 — подтверждено, что не нуждается в отдельном примере).
- **2 из 4 ранее считавшихся «дырами» механик (Часть 6 CLAUDE.md)
  переосмыслены как НЕ требующие новой операции движка** — `wholevarga`
  (п.2 выше) и `DISTANT` (п.6 выше). **DOUBLE** (п.1) — ЗАКРЫТ, но, в
  отличие от wholevarga/DISTANT, потребовал НОВУЮ операцию (`bud`), не
  переиспользование существующей. **Вриддхи gold-сигнал** — тоже ЗАКРЫТ
  (rule42, tatraiti).
- **Все 71 правило** теперь имеют предполагаемую 3D-механику в одной
  таблице (Часть Б) — при выборе следующего правила для полноценного
  3D-примера не нужно заново анализировать взаимодействие с нуля, только
  свериться со строкой таблицы и (при необходимости) с открытым вопросом
  из Части В.
